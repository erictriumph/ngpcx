# NGPCX — ARM Readiness Scanner

## What this is
Free Windows ARM (Snapdragon) readiness scanner. Consumer-facing brand: CheckMyARM — live on the visitor-facing pages (see Branding & monetization section); redirect domain still pending. Learning project with real utility; affiliate monetization is a bonus, not the goal.

## Branding & monetization
- CheckMyARM is the consumer-facing name; NGPCX is the underlying project/codebase name — both are intentional, not a half-finished rename. CheckMyARM appears in visitor-facing text only: page `<title>`s, nav logos, and footers across `index.html`, `report.html`, `about.html`, `privacy.html`, `my-submissions.html`, `why.html`, `admin-research.html`, `admin-revisit.html`, `admin-dashboard.html`, `admin-users.html`. Everything else stays NGPCX: the `ngpcx-scanner.exe` filename, the GitHub repo, all variable/function names, code comments, the `ngpcx_admin_secret` localStorage key, and the scanner's hardcoded server URL (`get_server_url()` → `ngpcx.com` — must stay stable regardless of branding, since already-distributed exes can't be updated).
- Logo is live: a 64px PNG (`public/logo-64.png`) paired with the text wordmark (`CheckMy` + `<span>ARM</span>` on the main pages, `CheckMyARM` + `<span>Admin</span>` on admin pages) in every nav, plus a full favicon set (`favicon.ico`, 16/32/48px PNGs, `apple-touch-icon.png`, `android-chrome-*` sizes) wired via `<head>` tags on every page listed above. Source files (multiple sizes, the original SVG wrapper, and generation notes) live in `branding/` at the repo root — not served, kept for reference/future regeneration. `logo-source.svg` is not a true vector, it's an 817×817 raster PNG wrapped in SVG, so that's the resolution ceiling if a larger version is ever needed.
- Ko-fi is live at `https://ko-fi.com/checkmyarm` — used in `report.html`'s donation callout and both visitor-facing footers (`index.html`, `report.html`). Affiliate links are still **placeholder/nonexistent** — the "affiliate links" mention in `report.html`'s donation callout is plain text, not a link, until an actual affiliate program exists. Don't wire up real affiliate tracking without checking first.
- `public/about.html` was redesigned (Phase 12) around an origin-story opening (why CheckMyARM exists) rather than leading with methodology — sections now run: origin story, How it works, Community (links to `why.html`), Privacy (links to `privacy.html`), Open philosophy (folds in the NGPCX naming trivia, previously its own section), Support. A subtle one-line joke ("no blood pressure cuff required") sits at the very bottom, after a Microsoft/Windows trademark disclaimer — deliberately last and low-key, not a leading hook. `public/why.html` is new: a non-FAQ educational companion (why Windows on ARM, why compatibility isn't binary, why Microsoft can't just publish a perfect list, why community matters) — tone is explainer, not persuasion, no jargon.
- **™ usage is deliberately sparse, not blanket**: `CheckMyARM™` appears only in `index.html`'s primary nav logo and `about.html`'s opening eyebrow line — the two "first prominent reference" spots — not on every page's logo. Every visitor-facing footer carries a plain `© 2026 CheckMyARM. All rights reserved.` line; the Microsoft/Windows trademark disclaimer ("independent project, not affiliated with or endorsed by Microsoft") lives only on `about.html`, not repeated site-wide, to avoid legal boilerplate creep.
- Homepage stats (apps/drivers/native counts, `GET /api/stats`) were deliberately removed from `index.html` in Phase 12, not relocated — catalog size is an implementation detail that doesn't help a first-time visitor. `GET /api/stats` itself is untouched (still used by nothing else currently, kept as-is — not removed since it's a harmless, possibly-still-useful endpoint, just no longer called from the homepage). `my-submissions.html` gained a different, unrelated *personal* stats grid instead (a visitor's own submission counts + Researcher status) — computed entirely client-side from `GET /api/community/mine` and `GET /api/auth-status`/`GET /api/researcher/status`, all already fetched by that page; no new endpoint was added. Device-compatibility submissions were considered for that personal-stats grid and deliberately omitted: `community_submissions` is app-only (`app_name`/`arm_support`), there is no device-submission concept anywhere in the schema or `community.js` — don't add a "device submissions" stat that would just always read zero.

## Stack — do not deviate without asking
- Server: Node.js + Express, **plain JavaScript, no TypeScript**. SQLite (better-sqlite3). Runs on Railway, auto-deploys from GitHub on push.
- Scanner: Rust .exe (`scanner/src/main.rs`), runs locally on the user's Windows machine only.
- No React, no Next.js, no Tailwind. No running the scanner from the web server. No running scrapers on Windows in production.

## Architecture essentials
- Scan level (quick/standard/full) travels through the localhost:7878 handshake between browser and scanner — never CLI-flag-only, never separate exe builds. CLI flags exist for dev convenience and always win over the browser-provided level (see `get_cli_scan_mode()` precedence in `main.rs`).
- **"Light/Standard/Advanced Assessment" is product-facing terminology layered over the same `quick`/`standard`/`full` wire values** — deliberately not a rename of the wire protocol, matching the existing NGPCX/CheckMyARM precedent (internal names stay stable, consumer-facing names can diverge). `results.scan_mode`, the `?level=` URL param, the scanner's CLI flags (`--quick`/`--standard`/`--full`), and `get_server_url()` all keep their original values; only display text in `index.html`/`report.html` changed. See "Assessment levels" below for what actually differs between them beyond naming.
- `--local` flag shortens the 7878 timeout to 3s (vs 20s prod) for faster dev iteration.
- Session IDs use `crypto.randomUUID()` (Node). Sessions expire after 24h — this is enforced server-side in both `/api/session/:id` and the scan-submission handler in `scan.js`, not just written and ignored.
- All HTML rendering of app/device names goes through an `escapeHtml()` helper (defined separately in each of `report.html`, `admin-research.html`, `admin-revisit.html` — no shared JS file) — never insert external data into `innerHTML` unescaped. This project has a real stored-XSS history (device FriendlyName is attacker-influenceable via USB descriptors); do not reintroduce raw interpolation. `escapeHtml()` encodes `"`/`'` in addition to `&`/`<`/`>` — needed because app/device names get interpolated into HTML *attributes* (`data-name="..."`, etc.) in the admin pages, not just text nodes, and a name containing a quote character could otherwise break out of the attribute. Found and fixed this gap once already; don't reintroduce a version that only handles text-node context.
- Readiness score (`classifyApps()` in `scan.js`) counts Unknown apps as **presumed emulated** (same 60-point weight as confirmed Emulated), not excluded — Windows on ARM runs ordinary x86/x64 apps under emulation by default, so true incompatibility is the rare case, not "no data either way." The separate confidence indicator (`report.html`, "High/Moderate/Limited Confidence — N of M apps matched") deliberately stays scoped to native+emulated+unsupported only, excluding Unknown — it answers "how much do we actually know," a different question from the score's "how likely is this to work." Don't collapse these two back into one number.

## Data sources for apps (in `server/scrapers/`)
- `winget.js` — GitHub-search-based, ARM64 installer detection, confidence 0.95
- `community/worksonwoa.js` — WorksOnWoA DB, confidence 0.75–0.90. Only maps `compatibility: 'no'` → `unsupported`; genuinely captures negative data when the source has it, but the source itself is ~99.9% "yes" (checked the raw feed: 6 "no" entries out of 8,772) — it's a "here's what works" community database, not a "here's what's broken" one. Don't assume there's untapped negative data being discarded here; there isn't.
- `github-lookup.js` — on-demand, single-app GitHub repo search + latest-release asset check (arm64/aarch64/win-arm64 in filenames), confidence 0.70. Triggered fire-and-forget from `scan.js` *after* the scan response is sent, for Unknown apps (cooldown via `unknown_apps.last_lookup_attempt`) and Emulated/Unsupported apps (cooldown via `apps.last_updated`) — never blocks the triggering user's report. Requires a strict normalized-name match on repo name (not `full_name`) plus a minimum star count (`MIN_STARS`, currently 20) before accepting a match — a wrong match is worse than no match. Confidence is deliberately kept below WorksOnWoA's floor (0.75) so it can never silently override a real source, only fill gaps or reinforce agreement with another github-auto entry.
- ARP (`get_arp_apps()` in `main.rs`) — Windows Uninstall registry, supplements winget for apps it misses (Office, Adobe, vendor tools). No confidence scoring; feeds `merge_arp_into_apps()` which dedupes against winget-found apps by normalized name — mode-agnostic (see App enumeration section). Reads `HKLM` (native + `Wow6432Node`) **and `HKCU:\...\Uninstall\*`** (Scanner Enrichment milestone, below) — no `HKCU\Wow6432Node` equivalent is needed, since WOW64 registry redirection only applies to `HKLM`, not per-user hives. HKCU coverage surfaces genuinely per-user installs invisible to HKLM: Chrome/Edge-installed PWAs (`Publisher` reads e.g. `Google\Chrome`), per-user dev-tool installs, and similar — confirmed on real hardware (several genuine Chrome PWAs, e.g. Google Calendar, TripIt, a stale ChatGPT PWA, previously invisible to every scan regardless of assessment level).
- **Never build a scraper against Windows Update Catalog or Microsoft Store's undocumented catalog API** — both investigated and rejected: Catalog requires POST+viewstate (a plain GET returns a static shell) and conflicts with Microsoft's ToU on reproducing catalog content; Store's catalog API is undocumented with unclear ToS. If asked to revisit either, flag the prior investigation before proceeding.

## Report refresh + on-demand lookups
- The classification loop lives in `classifyApps(apps)` (`scan.js`), extracted specifically so it can be re-run later against current DB state without a new physical scan. `POST /scan` calls it once at submission time; `sessions.raw_apps` persists the exact submitted app list so `POST /api/session/:id/refresh` (`server.js`) can call `classifyApps()` again later and pick up anything an admin resolved, a community submission accepted, or `github-lookup.js` found in the meantime.
- `report.html` shows a "Refresh results" banner whenever anything's still Unknown/Emulated/Unsupported, with copy that's deliberately honest about the mechanism: the automated check already ran once (fire-and-forget, right after the original scan), not an ongoing background process — repeatedly clicking refresh with no change is expected, not broken, since nothing new happens until the 14-day lookup cooldown elapses or a human (admin/community) acts.
- **Idempotency gotcha, don't reintroduce:** `renderReport()` can run more than once per page load (initial poll, then again on every refresh click). Any note it inserts into the DOM (`quick-mode-note`, `standard-mode-note`, `system-component-note`) must be removed before being re-added (`document.getElementById(id)?.remove()`), or it duplicates on every refresh — this was a real bug found via actual use, not just review.

## Device/driver detection (`main.rs`)
- Classes scanned: Printer, Image, MEDIA, Biometric, SmartCardReader, Camera, HIDClass, **Net, Display**. The last two were added specifically to catch USB-Ethernet/WiFi adapters and DisplayLink-style USB docks/graphics dongles — historically the worst ARM Windows compatibility pain point (Surface Pro X dock issues) — previously invisible to the scanner entirely. Filtered to `VID_xxxx&PID_xxxx` pattern InstanceIds (excludes `SW\` virtual bus devices); this same filter is the primary defense against virtual/software network adapters (VPN TAP, Hyper-V vswitches) showing up as Net-class noise, since those don't carry real USB VID/PID identifiers.
- Composite devices (one physical device, multiple USB interfaces) are deduped by VID/PID. Display name prefers non-generic interface names; "fido" is explicitly preserved even though it matches the generic-name filter (security keys must not display as "Keyboard Generic"). Classification uses the **most conservative class across all interfaces** — a device with any Biometric/SmartCardReader interface stays flagged for review even if it also exposes a generic HID interface.
- Recency signal is `IsPresent` OR `LastRemovalDate` OR `LastArrivalDate` (in that fallback order) — never arrival date alone. A continuously-connected device never updates arrival date; a recently-unplugged device is better signaled by removal date than by a stale arrival date. Tiers: Quick = present only (no per-device property lookups — this is what makes Quick fast); Full = everything. **Standard's window is per-class**, via `standard_threshold_days()`: 14 days for Camera/HIDClass/MEDIA/Biometric/SmartCardReader (these tend to cluster into "near-daily" or "effectively abandoned" — printers already bypass this filter entirely), 60 days for Net/Display (a travel dock or backup-drive adapter used monthly is a real pattern, closer to how apps are tiered than to a webcam).
- **Gotcha, don't reintroduce:** `Get-PnpDevice -PresentOnly:$false` means "show ONLY non-present devices," not "ignore presence." Omit the parameter entirely for true unfiltered enumeration.
- Printers via `Get-Printer` (not `Get-PnpDevice` — network printers have no USB VID/PID at all). Deduped by `DriverName`. Always included regardless of scan tier (no reliable recency signal exists for them).
- "Likely native" driver-check heuristic: device class is Camera or HIDClass (standard USB device classes with Windows-inbox drivers — UVC, HID) — validated against real hardware (webcam, BT-dongle-as-USB-audio, HID input), unconditionally "Likely native." **MEDIA is handled separately** (see Scanner Stabilization milestone below): it covers both plain USB Audio Class devices (mic/headset — genuinely fine) and professional audio interfaces (often vendor ASIO drivers, not fine) which the class alone can't distinguish, so it requires a `driver_provider` observation containing "Microsoft" before claiming native, falling back to "Check" without that evidence (e.g. Quick mode, where `driver_provider` isn't collected at all). Stays "worth a check" (search link, no claim) for Biometric/SmartCardReader and vendor-specific printer drivers — validated as genuinely ambiguous via real search results (Kensington fingerprint reader case).
- Driver/app "Check" links point to a plain web search (`"<name> ARM64 driver"` or similar) — **not** Windows Update Catalog (see rejection above).

## App enumeration is comprehensive and mode-agnostic (`main.rs`)
**Every mode returns the complete practical application inventory.** `get_installed_apps()` and `merge_arp_into_apps()` no longer take a `mode` parameter and no longer filter which discovered apps get pushed into the outgoing payload — `winget export` and the ARP registry read were already unconditional regardless of mode (confirmed by direct inspection: no mode-dependent enumeration cost exists for apps), so the old per-mode inclusion filter was only ever discarding apps *after* the expensive work was already done, for no real time savings. This used to be `include_by_recency(mode, is_running, days_ago)`, gating both the winget push site and `merge_arp_into_apps()` — deleted entirely, not deprecated, once both call sites stopped needing it. The "Quick mode found no apps (Prefetch unavailable) — falling back to standard" retry in `main()` is gone too, since that condition can no longer occur.
- `days_ago`/`is_running`/`recently_used` are still computed and attached to every app as enrichment (unchanged) — they just no longer decide inclusion. **The scanner observes and reports; `assessment.js` decides which applications are emphasized or counted toward a Light Assessment's recommendation** (see Assessment levels below). Don't reintroduce a mode-based app filter in `main.rs` — if a future need arises to reduce apps sent over the wire, that's a payload-size optimization, a different problem from what this section used to conflate it with.
- Devices are the only remaining place scan mode changes actual observation cost or scope — see Device/driver detection above (`get_devices(mode)`'s two genuinely different PowerShell paths, Quick present-only vs Standard/Full per-device property lookups). Measured directly: a Light-mode scan on a real machine completed in ~15s; the identical machine at Standard took ~86s, almost entirely spent in per-device `Get-PnpDeviceProperty` calls. Don't collapse this distinction back into the app-side one just deleted above — they're now deliberately different concerns with different owners (scanner owns device-observation depth; `assessment.js` owns application emphasis).

## Assessment levels (`assessment.js`, `report.html`, `workspace.html`)
Application population selection is now entirely the assessment layer's job, operating on the full inventory the scanner always reports (see above) — there is no scanner-side app filter left to conflict with it. "Light/Standard/Advanced Assessment" is product-facing terminology layered over the same `quick`/`standard`/`full` wire values (see Architecture essentials) — assessment level and scanner observation depth are related (the same `scan_mode` value drives both) but conceptually distinct: scan mode controls device-observation depth only; assessment level controls application emphasis, synthesis population, and Workspace complexity.

- **`isLightPriorityApp(app)`** (assessment.js): `is_running || is_startup || recently_used`. Deliberately excludes `has_start_menu_entry` — real-scan data (52 classified apps once app enumeration stopped being mode-filtered) showed it matches roughly half a typical machine's apps, which would make Light indistinguishable from Standard. `recently_used` is the older, coarser Prefetch-derived flag (pre-dates the Observation Enrichment Guidance Signals) — kept as a bonus signal, not load-bearing, since it's frequently empty on Prefetch-disabled machines. Verified against a real 52-app classified population: 11 apps qualify as Light-priority, 41 deprioritized-but-visible — a real, meaningfully curated result, not the ~7-8-total-app degenerate case from before this section's app-enumeration fix.
- **`isLightDeprioritized(app, decision, scanMode)`**: only ever `true` when `scanMode === 'quick'` — Standard and Advanced never deprioritize anything. A deprioritized app is excluded from `computeSynthesis()`'s weighted population entirely (same mechanism as `no_longer_use`, tracked separately as `deprioritizedCount`/`deprioritized`) — it still appears in its normal Native/Emulated/Unsupported/Unknown table on Results, unchanged; only the Score/Confidence/Primary Reasons math skips it.
- **Promotion is "This matters to me" setting `critical_to_me: true`** — not a new stored field or a second importance concept, the same flag `computeSynthesis()` already uses for weight-doubling and confidence-capping elsewhere. Once set, `isLightDeprioritized()` returns `false` for that item regardless of the heuristic. Devices are never deprioritized (a Light-scanned device list from `get_devices("quick")` is already just "currently connected," which is already Light's natural population) — "This device matters to me" is offered anyway, for symmetry and to future-proof a currently-inert flag, but has no computational effect today since devices don't feed the score in any mode.
- **Results**: a collapsed "Not Currently Prioritized" section (`renderNotPrioritized()`, only rendered when non-empty) lists every deprioritized app with a read-only Workspace deep-link — same pattern as System Components. Orthogonal to the pre-existing reduction-narrative ("Needs Attention" preview): that's about research status, this is about scoring inclusion; an app can be in both, neither, or either.
- **Workspace**: a 4th grouping tier, "Not Currently Prioritized" (`bucketItems()`/`renderGroups()`), inserted before Already Considered — only shown for the apps container, only under Light, only when non-empty. Cards in this group render minimally (name, evidence badge, research link, one promote button) with the full decision panel (findings, evidence detail, notes) still present underneath but gated behind Deeper Research Mode like everywhere else, not a new disclosure mechanism.
- **Store/AppX apps now participate in classification, not just as Guidance Signals — fixed in the Scanner Enrichment milestone (below).** This section previously documented WhatsApp/LinkedIn/ChatGPT Desktop as invisible to every classified bucket at every assessment level, since Store/AppX apps have no ARP/winget registration and `appx_apps` was deliberately never merged into `classifyApps()`. Superseded: `scan.js`'s `POST /scan` handler now merges `appx_apps` into the same population `classifyApps()` scores everything else against (`classifyPopulation = apps.concat(appxApps)`) — verified end-to-end on real hardware: WhatsApp now resolves `native`, LinkedIn resolves `x64-emulated`, ChatGPT Classic lands in `unknown` (no catalog entry — the honest outcome for an app the DB has no data on, not a bug). The scanner already guarantees `appx_apps` is disjoint from `apps` (deduped before the payload is sent), so no additional dedup was needed server-side. `sessions.raw_apps` now persists the merged population, not just the original `apps` array, so a later `/refresh` re-classification doesn't silently drop AppX apps back out. `appx_apps` is no longer passed through as a separate report field — redundant with the classified buckets now (confirmed nothing in any UI ever read `results.appx_apps` before removing it).
- **Accepted trade-off, not fixed:** merging `appx_apps` into `classifyApps()` means AppX component-shim noise — already documented as accepted residual noise in the Observation Enrichment milestone below, packages `get_appx_apps()`'s `IsFramework`/`SignatureKind` filtering can't catch — now also calls `trackUnknownApp()`, inflating the admin research queue with entries no Researcher can ever meaningfully resolve. Verified on real hardware: version-pinned WinAppRuntime sub-packages match `isSystemComponent()`'s existing `'runtime'` name pattern and land in `systemComponents` (excluded from the queue, as designed), but PowerToys' individual context-menu shims (`Microsoft.PowerToys.FileLocksmithContextMenu` and siblings) match no pattern and land in `unknown`, hitting the queue like any real unrecognized app. Not mitigated this pass — a hardcoded name-pattern list would fix it but conflicts with this project's established "principles over hardcoded exclusions" preference; flagging honestly as a known, small, ongoing admin-queue noise cost rather than silently accepting it or improvising a fix.
- `get_arp_apps()`'s former HKCU gap (per-user installs, e.g. Chrome PWAs, invisible regardless of assessment level) is also fixed — see the ARP bullet in Data sources for apps, above.

## Scanner Stabilization milestone
Small, low-risk pre-signing hardening pass across the whole scanner (`main.rs`) and its immediate server-side support, based on two architecture reviews (discovery quality/noise reduction/metadata/future-proofing generally, then a device-specific follow-up). Deliberately scoped to *improving observation quality*, not expanding discovery scope or touching scoring/recommendations — both reviews concluded the scanner's existing discovery categories were already the right ones; the value left was in identifying what's already found more precisely.

- **Payload/schema versioning**: `ScanPayload` gained `payload_version` (currently `1` — bump only on an actual breaking shape change, not every additive field) and `scanner_version` (`env!("CARGO_PKG_VERSION")`). Added *before* more scanner generations ship without either, since already-distributed exes can never be updated and the server previously had no way to distinguish "old scanner, known shape" from "malformed payload." Passed through into the report object in `scan.js` (`scanner_version`/`payload_version`, `null` for pre-this-change exes) — not interpreted anywhere yet, just preserved for future debugging.
- **`discovery_source`** (`"winget"` | `"arp"`, later also `"running-process"` | `"appx"` — see Observation Enrichment milestone below) added to `AppEntry`, previously computed implicitly by every collector and then discarded.
- **Publisher propagation fix**: winget-sourced apps previously always sent `publisher: None` (`winget export`'s JSON has no Publisher field), which meant `isSystemComponent()`'s Microsoft-gate (below) could only ever fire for ARP-sourced apps — most Microsoft noise slipping past the small Rust-side `skip_prefixes` list (e.g. `Microsoft.EdgeWebView2Runtime`) had no second line of defense. Fixed by deriving a coarse publisher hint from the winget package ID's vendor segment (already computed for name-derivation, zero new cost) — verified end-to-end with a synthetic `Microsoft.SomeRuntime`-style ID reaching `systemComponents` correctly.
- **Publisher merge-order bug, found while fixing the above — don't reintroduce:** `classifyApps()` merges matched apps as `{...entry, ...app}` (DB row, then scanner observation). Once winget apps started carrying a publisher hint, that spread order would have silently overwritten a *real* DB catalog publisher with the scanner's coarse guess for every matched app. Fixed via `mergeEntryWithApp()` (`scan.js`), which keeps `entry.publisher` when present and only falls back to `app.publisher` otherwise — verified against `Microsoft.PowerToys` (DB: "Microsoft Corporation", scanner hint: "Microsoft") surviving intact.
- **Device evidence quality**: `DeviceEntry` gained `manufacturer` and `driver_service` (free on the base `Get-PnpDevice` object, populated in both Quick and Standard/Full) and `driver_provider` (requires `Get-PnpDeviceProperty`, so Standard/Full only — matches the existing arrival/removal-date cost tier; Quick stays presence-only by design). `manufacturer` is used immediately: the "Check →" driver-search link (`report.html`) prepends it when known, for materially better search results on generic device names (e.g. "Focusrite Scarlett 2i2 USB ARM64 driver" instead of a bare device name).
- **MEDIA-class "Likely Native" heuristic corrected** — see the updated bullet in Device/driver detection above. **Known, accepted UX change**: MEDIA-class devices scanned in Quick mode always show "Check" now, including genuine plain USB mics that would previously have shown "Likely native" — a deliberate trade favoring accuracy over optimism, per explicit instruction to "favor conservative conclusions over optimistic assumptions."
- **`is_network` clarified, not renamed**: only ever populated by `get_printers()` (network vs. local printer, via port-name check) — always `false` for `get_devices()` rows, since a Net-class USB-Ethernet/WiFi adapter is already unambiguously labeled via `class`. Documented via comments rather than renamed: a wire-field rename would silently drop the printer "(network)" annotation for any already-distributed scanner exe still sending the old shape, since old exes can never be updated.
- **PowerShell boilerplate deduplicated**: `run_powershell_json()`/`json_as_items()` extracted in `main.rs`, used by every collector (`get_devices`, `get_printers`, `get_arp_apps`, and all four Observation Enrichment collectors below) — previously each collector hand-rolled its own run/parse/error-handling block.

## Observation Enrichment milestone (Guidance Signals)
Originally declared the final planned scanner enhancement before code signing — superseded by one further pass, the Scanner Enrichment milestone (below); see the Distribution section for the current, reaffirmed freeze declaration. Adds relevance signals for a *future* Workspace to weigh — **none of it changes readiness scoring, confidence, or recommendation synthesis today**, and that boundary is structural, not conventional (see below).

**What "Guidance Signal" means, as a standing principle for this codebase:** corroborating relevance evidence for a future recommendation layer to interpret, never a compatibility or recommendation determination made by the scanner itself. The scanner's job stays "observe and report facts"; interpreting what those facts mean for a purchase decision stays server/Workspace-side — the same separation of concerns `isSystemComponent()`/`apps.type` already established for compatibility classification (see System component classification section below), now extended to relevance signals rather than compatibility verdicts.

**New `AppEntry` fields** (apply to every app from every source, existing and new):
- `is_running` — process observed with a visible window at scan time (`Get-Process` + `MainWindowTitle` presence check).
- `is_startup` — name fuzzy-matches a Run-key value name (`HKCU`/`HKLM`/`HKLM\...\Wow6432Node` `...\CurrentVersion\Run`) — **value names only**, never the command strings those values hold (full file paths).
- `has_start_menu_entry` — name fuzzy-matches a `.lnk` base filename under either Start Menu Programs folder (all-users + current-user). No shortcut-target resolution — purely "does a launch entry exist with roughly this name."
- All three use the same fuzzy normalize+contains matching already established for ARP merging (`apply_signals()` in `main.rs`) — same known limitation (imprecise for apps whose registry/shortcut naming differs a lot from their display name), same tolerance level already accepted elsewhere in this codebase. A fourth signal, `is_pinned_taskbar`, was added later in the Scanner Enrichment milestone (below) using the identical pattern — see there for the full writeup.

**New payload arrays, deliberately unscored:**
- `unlisted_apps` — apps discovered via a visible-window running-process scan (`get_running_gui_apps()`) that don't match anything already in `apps` (same fuzzy containment dedup as ARP merging). This is the mechanism for discovering portable/sideloaded software (no installer, no ARP/winget registration) — real-hardware testing found genuine examples this way (a running browser, a game, a chat app) that winget/ARP couldn't see.
- `appx_apps` — AppX/Store packages (`get_appx_apps()`, `Get-AppxPackage`), filtered using the API's own authoritative flags — `IsFramework`/`IsResourcePackage`/`IsBundle`, and `SignatureKind != 'System'` — **not a hardcoded name-pattern list**. `SignatureKind` was added after real-hardware testing showed the first three flags alone left ~141 packages, dominated by deep OS-shell plumbing (`CredDialogHost`, `ShellExperienceHost`, `PinningConfirmationDialog`, and dozens like them) all carrying `SignatureKind = 'System'`; excluding that value cut it to 97 while verified safe. **Don't also exclude `SignatureKind = 'None'`** — that's where PWA-installed web apps (Plex, Home Assistant, etc. — confirmed on real data) show up; excluding it would silently drop genuine user software. Known, accepted residual noise: some component sub-packages (PowerToys' individual context-menu shims, version-pinned WinAppRuntime packages) don't carry any of these flags and still get through — not solved with a name-pattern list, per this project's established "principles over hardcoded exclusions" preference.
- Both arrays were passed through `scan.js` **without ever touching `classifyApps()`** at the time of this milestone — `report = { ...classified, ..., unlisted_apps: req.body.unlisted_apps || [], appx_apps: req.body.appx_apps || [] }`. This guaranteed they couldn't affect `score` or the classified buckets *by construction* (they were never in scope of the function that computes those), not by every future call site remembering not to include them. `report.html` was not touched in this milestone at all. **Superseded for `appx_apps` in the Scanner Enrichment milestone (below)** — `appx_apps` is now deliberately merged into `classifyApps()`'s population (see Assessment levels, above) and no longer passed through as a separate report field. `unlisted_apps` is unaffected and still described accurately by this bullet.

**Privacy design, applied identically across all four new collectors** — the explicit brief was "identify applications, not behavioral information":
- Never capture full file paths (can reveal personal folder/username structure) — only vendor/OS-authored labels (process `Description`/`Company`, registry value names, shortcut filenames, AppX manifest metadata), the same privacy tier ARP/winget already send.
- `MainWindowTitle` is used **only** as a PowerShell-side filter (`Where-Object { $_.MainWindowTitle }`) inside `get_running_gui_apps()` — never selected into the object returned to Rust, so window-title content (document names, browser tab titles, conversation previews) never leaves the machine.
- `get_running_gui_apps()` self-excludes its own process name (`ngpcx-scanner`) and dedupes by process name (one exe can own several windows, e.g. a browser's tabs).

**Verification note**: real-hardware runs before and after this milestone produced byte-for-byte identical `report.html` output (same score, same "Workable with Caveats, Limited Data" text, same app counts) — confirms the new arrays and fields have zero effect on the existing report, not just in theory.

**Deferred / consciously parked observation ideas** (preserve the reasoning, don't re-litigate from scratch):
- **Scheduled Tasks** (logon/boot-triggered, via the Task Scheduler API) — a real, Windows-maintained, comparable sibling to the Run-key startup signal above, and genuinely still open as a small future addition. Not implemented this milestone: unlike Run keys, there's no single authoritative flag (comparable to AppX's `IsFramework`) to separate a 3rd-party logon task from the many built-in Microsoft maintenance tasks Windows ships — would need per-task trigger-type inspection plus new noise filtering, which didn't clear the "lowest-risk, lowest-overhead" bar this milestone was held to.
- **Rejected, with reasons, so they aren't re-proposed:** `Win32_Product` (WMI) — well-documented Microsoft anti-pattern, triggers a full MSI reconfigure/repair as a side effect of querying it; ARP already covers the same ground without that hazard. Event Log install/uninstall history — crosses from "what's present" into "what did the user do and when," conflicting with this milestone's own behavioral-data boundary. File-type/protocol-handler associations — preference data about defaults, not software inventory, weak signal for a purchase decision.
- **Dock aggregation, Thunderbolt/PCIe enumeration, gaming/anti-cheat discovery** — carried forward from the device-focused review that preceded this milestone, still deferred, still for the same reasons: no unified-product identification is possible from VID/PID alone (a dock's sub-interfaces have unrelated vendor VID/PIDs); PCIe devices (true Thunderbolt controllers, eGPUs) have no USB VID/PID to filter on at all; anti-cheat compatibility is a per-game software problem needing a different discovery source (game library manifests) and a separately-maintained dataset, not a device-scanner concern.
- **ARM Optimization Scanner** (per-app PE-header architecture inspection, ARM-only) — see "Future direction: ARM Optimization Workspace" section near the end of this file; still blocked on real ARM64 hardware access, unchanged by either scanner milestone.
- **Device-side and Guidance-Signal score integration** — devices, and now Guidance Signals generally, still contribute nothing to the readiness score, by design. Whether/how a future Workspace should weigh any of this is explicitly a Workspace-redesign decision, not a scanner one — don't fold it into `classifyApps()` without that decision being made deliberately first.

## Scanner Enrichment milestone (computing-environment evidence)
One further scanner-side milestone after Observation Enrichment was declared the final planned enhancement before signing (see Distribution section — that declaration is now amended, not retracted: this milestone closes the loop it left open, and the freeze is reaffirmed going forward from here). Framed around a philosophy shift from "discovering installed software" to "gathering evidence about how the environment is actually used": no individual signal is authoritative, multiple independent signals accumulate into stronger evidence, and missing evidence is never interpreted as evidence of absence. Still scoped to observation quality and coverage, not scoring/recommendation redesign — the same discipline every prior scanner milestone held to.

- **HKCU ARP coverage** — see the ARP bullet in Data sources for apps, above, and the superseded-limitation note in Assessment levels, above.
- **AppX friendly names via `Get-StartApps`** — `get_appx_apps()` now runs `Get-StartApps` first to build a PackageFamilyName→human-readable-Name lookup (correlated via the AppID's `!`-delimited PackageFamilyName prefix), and uses it to set each AppX `AppEntry.name` — falling back to the raw, often hash-prefixed `Get-AppxPackage` package `Name` only when no Start Menu launcher exists for that package (the same residual-noise packages already documented as accepted in Observation Enrichment). Verified on real hardware: `5319275A.WhatsAppDesktop_...` → "WhatsApp", `7EE7776C.LinkedInforWindows_...` → "LinkedIn", `OpenAI.ChatGPT-Desktop_...!ChatGPT` → "ChatGPT Classic". AppX `AppEntry.id` is now the stable PackageFamilyName (previously always `None`) — gives Workspace's Personal Context a stable per-app key for AppX apps, the same way winget/ARP apps already have one.
- **Store/AppX apps now participate in classification** — see the Assessment levels section, above, for the full writeup (the `scan.js` merge, the WhatsApp/LinkedIn/ChatGPT Classic verification, and the accepted admin-queue-noise trade-off).
- **`is_pinned_taskbar` signal** — a fourth Guidance Signal (`AppEntry`), reading `.lnk` base filenames from `%AppData%\Microsoft\Internet Explorer\Quick Launch\User Pinned\TaskBar` (`get_taskbar_pinned_names()`, same fuzzy-match pattern and same doc-comment discipline as `get_start_menu_names()`), wired into `apply_signals()` and exposed in `assessment.js` (`isLightPriorityApp()`, `defaultImportanceForApp()` → `"Pinned to your taskbar"`, `'high'` level, alongside `is_running`/`is_startup`). Taskbar pinning is a deliberate user action — arguably stronger importance evidence than `is_running` (which only proves "happened to be open," not "core to my workflow"). The underlying `.lnk`-folder mechanism is an undocumented legacy Windows API, not a stable/guaranteed-future one; the accepted failure mode if Windows ever changes it is an empty list (missing evidence, never treated as evidence of absence — consistent with this milestone's own guiding philosophy), not a crash or a wrong answer. Verified working on real Windows 11 hardware. Considered and excluded: Start-menu ("Pin to Start") tiles — no functional/documented reader exists (`LayoutModification.xml`/`start2.bin`-adjacent files are Microsoft-undocumented binary blobs, unlike the taskbar's plain `.lnk` folder), so this stays an open, unimplemented idea rather than a "rejected" one — worth revisiting if Microsoft ever documents a reader.
- **Startup enrichment investigated, concluded not practically implementable this pass — self-correction, don't reintroduce the earlier (wrong) framing:** a prior investigation had characterized `HKCU:\...\CurrentVersion\BackgroundAccessApplications\<PackageFamilyName>` as evidence that a Store app "starts with Windows." Dumping the full key (~144 entries) and checking specific per-app values (including a known non-startup control app) showed this was **wrong** — the key lists nearly every installed AppX package regardless of actual startup configuration, with no distinguishing per-app enabled/disabled value in almost all cases. Not implemented as a startup signal; `is_startup` (Run-key based) remains Run-key-only and does not attempt to cover Store apps' startup behavior, which — as far as this investigation found — has no reliable per-app registry signal at all.
- **Research-only findings for the remaining investigated signals** (per this milestone's explicit "do not implement unless one is extremely inexpensive and obviously valuable" bar — only taskbar pinning cleared it):
  - **UserAssist** — deferred at the time this bullet was first written, on a characterization that turned out to be more pessimistic than reality: a follow-up research pass found only two GUIDs actually matter for Windows 10/11 (not a sprawling GUID→category table), and that UserAssist's real value isn't overlap with Prefetch/`is_running`/`is_startup` but a genuinely different axis — it specifically excludes non-interactive launches, which those signals can't distinguish. Implemented in the UserAssist Enrichment milestone, below — this bullet is superseded, not retracted, kept here so the corrected framing is traceable against the original (wrong) one.
  - **Start pinning** — see `is_pinned_taskbar` bullet above; deferred specifically on "no documented reader exists," not on value (it would likely be at least as strong a signal as taskbar pinning).
  - **Default file/protocol handlers** — re-reviewed this milestone, prior rejection (see Observation Enrichment's "Rejected" list) reaffirmed: preference data about defaults, not software inventory: a weak, indirect signal for "is this application part of the user's environment."
  - **Recent-document/Jump List presence** — feasible to read (`AutomaticDestinations`/`CustomDestinations` `.ms` files or the Recent Items folder), privacy-preserving only if scoped to "does a jump list exist for this app" and never its contents (target document names/paths). Deferred: real but modest value over existing signals, and the privacy framing needs more care than this milestone's time budget allowed to get right on the first pass — better to defer than ship a privacy boundary that wasn't carefully reasoned through.
  - **User-installed persistent services** (`Get-Service`, filtered to non-Microsoft `StartType = Automatic`) — cheap and stable, judged moderate-not-obvious value (a background service is weaker importance evidence than a pinned/running/startup app — it's evidence software is *installed*, which is already known, not evidence it *matters*). Explicitly declined despite clearing the cost bar, since the milestone's brief called for at most one addition and taskbar pinning was the stronger candidate.
  - **Notification-area (system tray) presence** — no clean enumeration API exists; the common approaches (parsing the tray's undocumented registry/shell state) are fragile and version-sensitive. Deferred on feasibility, not value.
- **`payload_version` was not bumped** for any of this milestone's changes — all additive fields (`is_pinned_taskbar`, richer AppX `name`/`id`), no breaking shape change, consistent with the existing versioning convention (Scanner Stabilization milestone, above).

## UserAssist Enrichment milestone
One further scanner-side pass, following directly from the Scanner Enrichment milestone's own "Research-only findings" list (above), where UserAssist was flagged as a candidate worth a deeper look rather than a rejected one. A dedicated research phase (Windows registry forensics literature, real byte-offset verification against this machine's own UserAssist data) preceded implementation and corrected an earlier, more pessimistic characterization — see the superseded bullet above.

**What UserAssist is, for this codebase's purposes:** Windows Explorer's own internal launch tracker (undocumented by Microsoft, but stable since Vista). The one thing that makes it worth adding alongside every existing Guidance Signal: real testing (both third-party forensic research and a direct verification pass against this machine's own registry data) confirmed it specifically **excludes non-interactive launches** — Startup-folder and Scheduled-Task launches reliably register a zero run count even though the program genuinely ran. That makes a nonzero UserAssist run count the one signal in this codebase that corroborates a *deliberate, Explorer-driven* launch (double-click, Start Menu, taskbar, search), distinct from every other signal here, all of which prove presence, configuration, or momentary state rather than sustained voluntary use.

**Scanner implementation** (`get_userassist_data()`, `main.rs`): reads two registry GUIDs under `HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\UserAssist\{GUID}\Count` — `CEBFF5CD-ACE2-4F4F-9178-9926F41749EA` (direct `.exe` launches) and `F4E57C4B-2036-45F0-A9AB-443BCFE33D9F` (shortcut/`.lnk` launches, including AppX/UWP apps, which appear as AUMIDs). Value names are ROT13-decoded (a documented, trivial reversal, not real obfuscation) purely to recover a program name or PackageFamilyName. Run count (byte offset 4) and last-run FILETIME (byte offset 60) are read from the binary value; **entries with a zero run count are discarded entirely**, not treated as weak evidence — this is the mechanism that filters out non-interactive launches. Focus Count/Focus Time are ignored completely: undocumented calculation, inconsistently incremented per the research, and the closest thing to real behavioral profiling among anything considered for this milestone.

**Raw observations, not scanner-generated bands — a deliberate, explicit choice:** `AppEntry` gained `launch_count: Option<u32>` and `days_since_last_launch: Option<u32>`, both un-interpreted numbers, not `"frequent"`/`"occasional"`/`"stale"` labels. This follows the same scanner/assessment split already established for compatibility classification (`isSystemComponent()`/`apps.type`) and Guidance Signals generally: **the scanner is responsible for gathering factual observations; the assessment is responsible for interpreting them.** Reducing to a band inside the scanner would bake a threshold choice (what counts as "frequent"?) into something that can't change without a new signed release; keeping the raw number lets `assessment.js` retune its interpretation freely. `None` on both fields means no UserAssist evidence was found — missing evidence, never evidence of zero use (consistent with this codebase's standing Guidance Signal philosophy).

**Exact timestamps never leave the scanner process, let alone the machine.** The last-run FILETIME is converted to a relative day count *inside the PowerShell script itself* and discarded immediately after — Rust never touches a raw timestamp for this signal, and only the relative age is ever sent to the server. This is a stricter reading than "don't transmit it over the network" — the exact value doesn't survive past the PowerShell subprocess that read it.

**Matching**: `apply_signals()` (extended, not a new function, matching the existing pattern for every prior Guidance Signal) prefers an exact PackageFamilyName match against `AppEntry.id` for AppX apps (unambiguous, since AppX ids are already stable PackageFamilyNames — see Scanner Enrichment milestone) before falling back to the same fuzzy normalized-basename containment check every other signal here already uses. Multiple raw UserAssist records can legitimately match one app (a direct-exe entry and a shortcut entry both tracking the same program) — counts sum, and the smallest (most recent) days-since-last-launch wins, the same "most informative evidence wins" fallback already used for device recency (`IsPresent` OR `LastRemovalDate` OR `LastArrivalDate`).

**Assessment integration** (`assessment.js`) — purely additive, the weighting model (`computeSynthesis()`) is untouched:
- `hasFrequentUserAssistLaunches(app)` (`launch_count >= 3`) and `hasRecentUserAssistLaunch(app)` (`days_since_last_launch <= 30`) are the only two places the raw numbers are interpreted — small, named, easily retuned constants, not a config system.
- Both feed `isLightPriorityApp()` (alongside `is_running`/`is_startup`/`recently_used`/`is_pinned_taskbar`) and `defaultImportanceForApp()` (`"Frequently launched"` at `'high'`, `"Used recently"` at `'normal'`, ranked between `is_pinned_taskbar` and `has_start_menu_entry`) — the same always-visible reason-chip mechanism every existing Guidance Signal already uses, no new UI concept.
- **Advanced/Deeper Research Mode presentation** (`workspace.html`, `evidenceDetailHtml()`): a new `userAssistEvidenceLines(app)` helper (`assessment.js`) returns every applicable interpreted phrase at once (an app can genuinely be both "Frequently launched" *and* "Used recently" — the single-winner reason chip can't express that), rendered as a small "Importance Evidence" checklist inside the existing `.deeper-detail` block, gated behind Deeper Research Mode exactly like Source/Confidence/Compatibility Notes already are. **Raw `launch_count`/`days_since_last_launch` numbers are never rendered anywhere, at any assessment level, including Deeper Research Mode** — only these interpreted phrases ever reach the DOM. Verified directly: searched the live-rendered Results and Workspace DOM for the raw field names and confirmed neither appears.

**Export**: originally shipped as "no changes made" (decision-only export, unrelated to this signal) — **superseded by the Environment Integration Evidence milestone, below**, which extends export to a three-layer format precisely so scanner evidence like `launch_count`/`days_since_last_launch` gets preserved, not just Personal Context.

**Real-scan validation.** Verified end-to-end against this machine's real UserAssist data (19 apps with evidence out of 171 total in a Standard scan): a direct raw-registry cross-check (bypassing the scanner, reading the same two GUIDs manually) confirmed the byte-offset parsing, ROT13 decoding, and zero-run-count filtering are all correct. Concrete example of the validation ask ("does UserAssist identify applications the current assessment would otherwise underestimate"): a Visual Studio installation (catalog-unknown) showed `is_running: false`, `is_startup: false`, `is_pinned_taskbar: false`, `has_start_menu_entry: false` — every existing signal gave it `{level: 'low', reason: null}` — but UserAssist showed a real launch the previous day, correctly promoting it to `{level: 'normal', reason: 'Used recently'}`, confirmed rendering in both the Results reason chip and the Workspace "Importance Evidence" checklist. Several already-prioritized apps (a taskbar-pinned, currently-running browser; a running editor with a Start Menu entry) also carried UserAssist evidence that didn't change their outcome — expected and correct, since they were already correctly prioritized by other signals; this is the "signals reinforcing rather than single-handedly deciding" philosophy working as intended, not a case where UserAssist happened to matter.

**Known limitation, found during validation, not fixed:** a direct raw-data cross-check on a real Chrome installation found the browser's true interactive-launch history split across several UserAssist identities that don't all fuzzy-match back to "Google Chrome" — some Chrome-created sub-entries (PWA/extension launcher shims, named like `Chrome._crx_<hash>.UserData.Profile1`) carry genuine nonzero run counts but don't share a normalizable substring with the browser's own name, so they're silently not counted toward it. This under-counts total interactive launches for apps with this kind of multi-identity registration pattern (Chrome and likely other Chromium-based browsers) — a real, honest gap in the same fuzzy-matching tolerance already accepted for every other Guidance Signal in this codebase, not something a stricter matcher would obviously fix without risking false merges elsewhere.

**Empirical observation worth flagging for future tuning, not acted on this pass:** real launch counts on this test machine were low across the board (max observed: 4; most evidenced apps showed exactly 1) even for genuine daily-driver software — plausible explanation is that UserAssist counts discrete launch *events*, not hours of use, and software that's opened once and left running for long stretches (a common pattern for browsers/IDEs/chat apps) accumulates launches slowly. This suggests the `launch_count >= 3` "Frequently launched" threshold may rarely fire in practice, while the `days_since_last_launch <= 30` "Used recently" threshold is carrying most of the practical value observed so far. Deliberately not retuned based on this single machine's data — the raw-value design exists precisely so this threshold can be adjusted later against broader real-world usage, without a scanner change.

### Focus Fields addendum
A same-evening follow-up to the milestone above, adding `focus_count: Option<u32>` and `focus_time_ms: Option<u32>` to `AppEntry`, explicitly labeled **experimental** and never wired into `isLightPriorityApp()`, `defaultImportanceForApp()`, `isLightDeprioritized()`, or `computeSynthesis()` — only into the Importance Evidence stack (below) as a single conservative line ("Focused when open (experimental)", gated on `focus_time_ms >= 60000`, i.e. at least a minute, to filter pure noise without pretending finer precision).

- **Byte offsets verified against real registry data before implementing**, not assumed from research alone: run count at offset 4, focus count at offset 8, focus time (milliseconds) at offset 12, last-run FILETIME at offset 60 — all within the existing `bytes.Length < 68` guard. Confirmed plausible on real apps (a browser actually used today showed ~47 accumulated focus-minutes; a Remote Desktop client opened briefly showed ~3 seconds).
- **Only trusted from the direct-`.exe` GUID (`CEBFF5CD-...`).** Real-data testing found the shortcut-launch GUID (`F4E57C4B-...`) carries near-zero, non-representative values at these same offsets even for apps with a genuine same-day launch recorded on their direct-`.exe` counterpart (e.g. a `.lnk` entry showing `FocusTimeRaw=1` for an app whose real exe-GUID entry showed 254,687ms) — so `apply_signals()` only ever populates focus fields from direct-`.exe`-sourced `UserAssistEvidence` records, never shortcut-sourced ones.
- **A real bookkeeping entry had to be excluded by name.** `UEME_CTLSESSION` (and the `UEME_CTL*` family) is Explorer's own internal session-control value, not an application — confirmed directly against this machine's registry (a ~1.6KB value, vs. 72 bytes for a real app entry, and a nonsense pre-1970 "last run" date when parsed as a normal record). Excluded by a `-like 'UEME_CTL*'` filter before any byte parsing runs. This also retroactively cleans up the original `launch_count`/`days_since_last_launch` collector, which had been silently absorbing this bogus entry without it ever fuzzy-matching a real app (harmless in practice, but not clean data hygiene).

## Environment Integration Evidence milestone (default handlers, evidence stack, export/import)
Same evening as the Focus Fields addendum, in direct response to a request to complete the evidence model before limited hands-on testing. Three parts: default-handler evidence (new scanner signal), a full Importance Evidence stack (assessment/UI), and a three-layer export/import format (client-side only, no new backend persistence).

### Default-Handler Evidence
`get_default_handlers()` (`main.rs`) reads Windows' per-protocol `UserChoice` registry entries and resolves the winning ProgId to a human-readable app identity, via the same `HKCU\Software\Classes\{ProgId}\Application` → `ApplicationName`/`AppUserModelId` convention Windows itself uses. **Deliberately scoped to protocol handlers only** (`http`, `https`, `mailto`) — file-extension `UserChoice` entries (`.pdf`, `.htm`, `.docx`, etc.) were checked against this machine's real, actively-used registry and found **consistently empty**, not merely hard to resolve, so that half of the original idea is reported as an investigated, deferred limitation rather than guessed at or shipped against zero positive examples. `AppEntry` gained `default_handler_count: Option<u32>` (how many of the checked protocols resolved to this app) and `default_handler_categories: Option<Vec<String>>` (a short coarse list — `"browser"`/`"mail"` today — never a raw ProgId or extension).

- **Matching** reuses the exact same exact-id-then-fuzzy-name contract `apply_signals()` already applies to UserAssist: an AppX-backed handler's `AppUserModelId` PackageFamilyName prefix matches `AppEntry.id` exactly; a Win32-backed handler's `ApplicationName` fuzzy-matches by normalized basename.
- **Real bug found and fixed during testing, don't reintroduce:** a Win32 app can set its own `AppUserModelId` for taskbar-grouping/jump-list purposes (Chrome sets a plain `"Chrome"`) that is *not* an AppX PackageFamilyName — it has no `!` delimiter. The original code treated any non-empty `AppUserModelId` as a PackageFamilyName, which misclassified Chrome's handler evidence as package-based with the wrong key (`"Chrome"` instead of a fuzzy-matchable name), silently failing to attach to the Chrome `AppEntry` at all. Fixed by only extracting a PackageFamilyName when the `AppUserModelId` actually contains `!` — the one structural marker that reliably distinguishes a real AppX AUMID from a Win32 app's own custom identifier. Verified after the fix: Chrome correctly shows `default_handler_count: 2, categories: ["browser"]`; Outlook (a real AppX AUMID) correctly shows `count: 1, categories: ["mail"]`.
- **Confidence framing**: Windows doesn't expose whether a `UserChoice` entry reflects a deliberate user pick vs. an installer-set default with equal certainty for every entry, so this is treated as modest, corroborating evidence — `hasMeaningfulDefaultHandler()`/`defaultHandlerEvidenceLines()` (`assessment.js`) feed `isLightPriorityApp()` and `defaultImportanceForApp()` at the same `'normal'` tier as a Start Menu entry, never `'high'`.
- **Browser/PWA relationship, verified rather than special-cased**: per explicit instruction, Chrome's own launch-count aggregation was *not* hand-tuned to chase every PWA/extension execution identity (see the UserAssist "Known limitation" above — still an accepted gap). Instead, the broader relationship was verified directly on real data: the browser itself is independently evidenced as installed/running/pinned/used (existing signals) *and* as the default handler for web links (this signal); installed browser-hosted PWAs (Google Calendar, TripIt, Home Assistant, etc. — see Scanner Enrichment milestone) already classify and render as separate AppX/PWA entries in their own right, not folded into the browser's count. PWA visibility, not a perfect Chrome execution total, was the goal — confirmed working.

### Importance Evidence stack
`importanceEvidenceStack(app, decision)` (`assessment.js`) replaces the narrower `userAssistEvidenceLines()`-only display built earlier the same evening — it now accumulates *every* independently-corroborating fact currently available (running, startup, taskbar-pinned, UserAssist launch/recency, experimental focus, Prefetch recency, Store/PWA, default-handler, Start Menu, explicit "This matters to me"), not just UserAssist's two facts. Rendered as a checklist in Deeper Research Mode / Advanced detail (`evidenceDetailHtml()`, `workspace.html`) — the ordinary Light/Standard experience is unchanged, still driven by `defaultImportanceForApp()`'s single-winner reason chip. **No single line in the stack determines importance by itself** — showing the accumulated stack instead of a number is the explicit point, not an incidental UI choice. As with every prior evidence surface in this codebase, only interpreted phrases are ever rendered — verified directly that no raw field name or number appears anywhere in the rendered DOM, at any assessment level.

**Known, not fixed:** a pre-existing ARP data-quality issue (two registry entries for the same game, `"Last War:Survival Game"` and `"Last War-Survival Game"`, colon vs. hyphen) produces two `AppEntry` rows whose `itemAnchorId()` (built by stripping non-alphanumeric characters) collapse to the identical DOM id, a real duplicate-id found via this session's own verification pass. Predates this milestone — `itemAnchorId()`'s collision behavior and/or ARP's dedup logic would need to change to fix it, out of scope for tonight's evidence-model work.

### Export/Import: three-layer format
The pre-existing `exportAssessment()`/`handleImportFile()` were decision-only (Personal Context: verified status, notes, `critical_to_me`), preserving no scanner evidence at all for any signal. Rebuilt around three explicit layers (`assessment.js`: `buildExportPayload()`, `buildObservationSnapshot()`, `buildAssessmentSnapshot()`, `sanitizeExportPayload()`), under a new `export_format_version` (currently `1`):

1. **`observation_snapshot`** — the scanner's own facts, stripped of every catalog/classification field via an explicit allowlist (`OBSERVATION_APP_FIELDS`/`OBSERVATION_DEVICE_FIELDS`) rather than trusting the classified bucket objects (which also carry `arm_support`/`source`/`confidence`/etc.) not to leak catalog data into what's supposed to be pure observation. Built from the classified buckets client-side (every classified app already carries its original scanner fields — `mergeEntryWithApp()` in `scan.js` spreads the scanner observation *after* the catalog row) rather than needing a new server endpoint.
2. **`personal_context`** — unchanged shape, the pre-existing decision entries.
3. **`assessment_snapshot`** — a frozen historical record (readiness label, score, confidence, primary reasons reduced to `{app_name, app_id, bucket, kind}`, bucket counts, `model_version`, `catalog_snapshot_at`) computed once at export time via the existing `computeSynthesis()`, never recomputed later.

**Import re-classifies against current catalog state — deliberately, not incidentally.** `handleImportFile()` (`index.html`) now posts the preserved `observation_snapshot.apps`/`unlisted_apps`/`devices` straight through the *existing*, unmodified `POST /api/scan` → `classifyApps()` pipeline (no new server code at all), which is exactly what lets "future versions of CheckMyARM reinterpret older assessments using newer recommendation formulas" work for free — the imported apps get scored against whatever the catalog says *today*, not a replayed old verdict. `scan_mode` is preserved from the original export (previously always hardcoded to the `'imported'` sentinel, which would have silently disabled Light-Assessment deprioritization on any reimported Light session) — falls back to the sentinel only for legacy-format files that never captured it.

**Historical comparison, not replacement.** The frozen `assessment_snapshot` is stashed one-shot (`ngpcx_imported_assessment_snapshot_v1`, consumed exactly once outside `renderReport()` since that function can legitimately run more than once per page load) and rendered as a read-only comparison banner (`renderHistoricalComparison()`, `report.html`) showing the at-import numbers next to the freshly recalculated ones — both remain visible, neither overwrites the other.

**Backward compatible by construction**: `sanitizeExportPayload()` recognizes both the new three-layer shape (`export_format_version` present) and the pre-existing bare `{format_version, entries}` decision-only shape — an already-saved old export file still imports correctly, just without an observation/assessment snapshot to restore or compare against.

**Real end-to-end round-trip verified**, not just unit-level: exported a real Standard-assessment session, fed the file back through `handleImportFile()`, and confirmed the re-imported session reproduced identical bucket counts (34/18/0/96) and carried every scanner field intact (including `launch_count`/`focus_time_ms`) through the reclassification pass, with the historical comparison banner correctly showing matching before/after numbers (same-day reimport, no catalog drift).

**Deliberately not done, per explicit scope boundary:** no server-side retention of exported evidence, no encrypted/obscured format (ordinary versioned JSON), no per-Recalculate-trip history — an export is the saved milestone, not a continuous log.

### Product copy: observed vs. influencing vs. not-prioritized
`computeSynthesis()` now also returns `totalAppCount` (every classified app, matching what the scanner reported) and `countedTotal` (previously an internal-only local variable) alongside the existing `deprioritizedCount`. `report.html`'s reduction-summary sentence gained a second clause — shown only when `deprioritizedCount > 0` (i.e., only under Light Assessment with something actually deprioritized; would be pure redundant noise under Standard/Advanced, where nothing is ever deprioritized) — naming the distinction explicitly: apps identified, apps currently showing strong evidence and shaping the recommendation, and apps not currently prioritized but still visible/promotable. Verified rendering correctly on a real Light-mode session ("Of 148 applications identified, 37 show strong evidence... 111 more are not currently prioritized...").

## System component classification
- Server-side in `scan.js` (moved off the exe deliberately, so pattern tweaks don't need a new signed release). `AppEntry` carries a `publisher` field — real registry data for ARP-sourced apps, a coarse ID-derived hint for winget-sourced apps (see Scanner Stabilization milestone above; the server's `mergeEntryWithApp()` never lets this hint overwrite a better catalog value) — the scanner just reports facts, the server decides what they mean. `isSystemComponent()` in `scan.js` does the Microsoft-publisher-gate + name-pattern heuristic; `apps` table entries with `type: 'system'` (set via the admin tool) override the heuristic going forward.
- System components get their own report bucket (separate from Unknown), muted styling, excluded from score calculation, with a disclaimer noting they're not evaluated and don't affect the score.
- **Public terminology is "Unknown," not "Unclassified"** — the internal `arm_support` value was always `'unknown'`; an earlier phase renamed only the public label to "Unclassified," creating a code/copy mismatch. Reverted site-wide (visitor and researcher-facing copy, plus comments/docs describing the vocabulary) after a compatibility-model review flagged the inconsistency. No behavior, schema, or stored-value change — label only.
- **`apps.architectures` is intentionally left `NULL` on insert** (`merge.js`) — it used to be hardcoded to the literal string `'arm64'` on every row regardless of reality, which is misleading, not just unused. Nothing reads this column today; it stays in the schema, reserved for real per-app architecture detection if that's ever built, but no source currently determines that per-app, so writing a fake value was worse than writing nothing.
- **The homepage's "What We Check" card that used to claim per-executable architecture inspection was corrected** — CheckMyARM has never actually parsed executables or determined per-app x86/x64/ARM64 architecture (the scanner's own `architecture` field is the *system's* CPU architecture, not any installed app's). The card now describes the real capability: comparing installed apps against compatibility data from multiple sources.
- **`apps.notes` is presented as "Compatibility Notes,"** not a general-purpose notes field — same column, same `resolve-app` write path, same `requireResearcherOrAdmin` authorization, just relabeled with an editorial rule in the placeholder ("only include what could materially affect a purchase or deployment decision") and promoted into its own visually distinct callout in `report.html` (previously inline with the Source/Confidence metadata line, which buried the most decision-relevant text in the row under the least important formatting).

## Admin tooling (`server/routes/admin.js`, `server/routes/community.js`)
- Auth is per-route, not a single blanket gate (see User roles & Researcher workflow section for why): `requireAdminAuthOrOAuth` (secret **or** `role = 'admin'`) guards user/role management and destructive deletes; `requireResearcherAuthOrOAuth` (secret **or** `role = 'researcher'`/`'admin'` via rank) guards the research-reading/resolving routes (`GET /unknown-apps`, `GET /stale-apps`, `POST /resolve-app`). The legacy shared secret (`ADMIN_SECRET` env var, via `x-admin-secret` header or `?secret=` query param) is a strict superset of both — it's checked first in each combinator — and stays active alongside OAuth; this is a deliberate, long-running migration window, not a temporary state to "finish." The shared-secret path is **not real security** — deliberately low-stakes — and is meant to be retired via `ADMIN_SECRET_ENABLED=false` only after OAuth/sessions/Railway Volume persistence have all been validated in production. Do not remove or upgrade either path without being asked.
- Five admin/research pages: `admin-research.html` (research unknown apps from scratch — `unknown_apps` table, populated by `trackUnknownApp()` in `scan.js`, sorted by count), `admin-revisit.html` (re-verify existing Emulated/Unsupported verdicts that real scans still hit — gated on `apps.times_matched > 0`), and `admin-community-review.html` (review community evidence and propose/confirm catalog changes — see Community Review section) are all Researcher-or-Admin accessible; `admin-dashboard.html` (work-queue counts, Admin-only) and `admin-users.html` (Researcher request review + role management, Admin-only) are Admin-only — see User roles & Researcher workflow section. All five bootstrap the same way: attempt to load via the visitor's session cookie first (transparent — no secret needed if their role already qualifies), falling back to the `x-admin-secret` entry form only on a 401/403. `admin-research.html`/`admin-revisit.html` hide their `Delete` button client-side for non-Admins (the underlying `DELETE` routes are server-enforced Admin-only regardless) — this is convenience only, per the nav-visibility rule below.
- `admin-research.html`/`admin-revisit.html` share the same resolve action (`POST /resolve-app` → `mergeApp()` with `source: 'admin'`, `confidence: 1.0`, always wins future conflicts) and a `Delete` button for clearing queue/DB entries outright (mainly for test-data cleanup, Admin-only) — this delete only clears the `unknown_apps` queue row, it deliberately does **not** delete the `community_submissions` rows that reference it (see Community trust layer section, "Removed" state).
- Community submissions (`community_submissions` table, `POST /api/community/submit`) let end users report what they found for an app that appeared in their own scan session. See the Community trust layer section below for identity, dedup, weighting, and OAuth — this used to be purely session-scoped and anonymous-only; it isn't anymore.
- Philosophy: **no automated bulk research, no paid search API.** Admin/Researcher manually researches (using the same "Check →" search links end users see) and submits verdicts; `github-lookup.js` (see Data sources) is the one automated exception, and it's deliberately low-confidence/conservative rather than authoritative. Don't propose further automating admin research without being asked.

## Community trust layer: anonymous identity + optional OAuth login
Two phases, both built and both live. Phase 1 (anonymous identity + dedup) works standalone with zero external dependencies. Phase 2 (OAuth) is gated behind `OAUTH_ENABLED` — off by default everywhere, but **now enabled in Railway production** (flipped and tested there directly) as well as locally — see "Deploy-readiness safeguard" below.

**Anonymous identity (`server/identity.js`, Phase 1):**
- First community submission from a browser gets a `ngpcx_anon_id` cookie (`crypto.randomUUID()`, HttpOnly, Secure in production, SameSite=Lax, 400-day maxAge — not a round "2 years": Chrome and the current cookie spec hard-cap `Max-Age` at 400 days regardless of what's requested, so 400 days is the actual ceiling, not an arbitrary choice). Contains no personal data.
- `community_submissions` dedups by identity, not scan session: `anonymous_id`/`user_id` columns, `CHECK (anonymous_id IS NULL OR user_id IS NULL)`, plus partial unique indexes `idx_community_anon_app`/`idx_community_user_app` on `(identity, normalized_name)`. A browser or logged-in user has exactly one submission per app — resubmitting `UPDATE`s the existing row (and resets `state` to `'active'` if it had been superseded), it never inserts a duplicate. This replaced the old `UNIQUE(app_name, session_id)` scoping, which had a real integrity gap: the same browser could submit repeatedly across multiple scan sessions.
- `session_id` stays on the table but now means only "proof this app was in the caller's own scan" (the existing anti-abuse check) — it no longer defines identity or uniqueness. Don't conflate the two again.
- Legacy rows from before this migration (both `anonymous_id`/`user_id` NULL) are preserved as-is by the `db.js` schema-rebuild migration (`schema_version` 2) — visible historically, never counted toward consensus (see weighting below).
- Graceful degradation: if cookies are blocked/cleared, submissions still work but aren't recognized as returning — the existing per-IP rate limit (`isRateLimited()`, 20/24h) is the explicit backstop for this, unchanged.

**OAuth login (`server/passportConfig.js`, `server/routes/auth.js`, `server/sqliteSessionStore.js`, Phase 2):**
- Providers: Google, GitHub via Passport (`passport-google-oauth20`, `passport-github2`). Login is always optional — the anonymous workflow above is never blocked or degraded by OAuth being enabled or disabled.
- `users` table: `id, provider, provider_id, display_name, role ('user'|'reviewer'|'admin'), disabled, contact_opt_in, contact_email, created_at`, `UNIQUE(provider, provider_id)`. New accounts always default to `role='user'` — role elevation is **never automatic** anywhere in this codebase (not by contribution count, agreement rate, or any other metric). Only `email` is deliberately *not* requested as an OAuth scope (`profile`/`read:user` only) — provider_id + display name are sufficient for login, and requesting email merely for a future contact channel would be an unnecessary scope. `contact_opt_in`/`contact_email` exist now but are unused — reserved for a future opt-in reviewer-invitation path (see Known open items).
- Sessions: **not** the scan-tracking `sessions` table — a dedicated `auth_sessions` table (`sid`, `session_json`, `expires_at`), backed by a hand-rolled `express-session` `Store` subclass (`SqliteSessionStore`) over the existing `better-sqlite3` connection. Deliberately not `connect-sqlite3` (pulls in a second native SQLite driver, `sqlite3`, alongside `better-sqlite3` — doubles the native-build surface on Railway) or `better-sqlite3-session-store` (single-maintainer npm package, last published 2022). Session cookie name `ngpcx.sid`, `saveUninitialized: false` (so anonymous visitors who never log in never get an `auth_sessions` row), `rolling: true`, 30-day maxAge.
- **Authorization always reflects live database state, never a cached session value.** `passport.serializeUser` stores only `user.id`; `passport.deserializeUser` re-reads the full `users` row from the DB on *every request*. Revoking a role or setting `disabled=1` takes effect on that user's very next request — no logout, no session expiry needed. Verified directly (not just by code review): a live session held across a direct `UPDATE users SET disabled = 1` immediately started returning 403 on the same cookie, no other action taken.
- Authorization middleware (`server/middleware/auth.js`): `requireAuthenticatedUser` and `requireRole(role)` share one internal check (`resolveActiveUser`) so the disabled check can't be forgotten on one but not the other — 401 if not logged in, 403 if `disabled`, otherwise a rank comparison (`{user:0, researcher:1, admin:2}`, `>=`) so `requireRole('researcher')` also admits admins. Centralized here specifically so role-checking logic isn't scattered per-route. Named aliases `requireAdmin`/`requireResearcherOrAdmin` are exported alongside `requireRole` for auditability at call sites (see User roles & Researcher workflow section) — this rank map originally read `reviewer` instead of `researcher` (defined but never wired to any route); renamed when the Researcher role went live rather than leaving two near-synonym unused/used roles side by side.
- OAuth flow CSRF protection (`server/routes/auth.js`): a server-side `oauth_states` table (single-use, 10-minute expiry) alone is **not sufficient** — it stops open redirects and replay, but doesn't prove the browser presenting the callback is the one that started the flow (classic login-CSRF: an attacker completes their own flow, captures the valid callback URL, and hands it to a victim). Fixed by binding `state` to a short-lived `oauth_nonce` cookie set at flow start; the callback rejects outright (403, before any code exchange) if the query `state` doesn't match the cookie. The final redirect target is *never* taken from client input — always reconstructed server-side from the `state`→`return_session_id` mapping stored at flow start, re-validated against the live `sessions` table.
- Anonymous → authenticated migration runs inside the OAuth callback, in a `db.transaction()`: each of the departing anonymous identity's submissions is reassigned (`anonymous_id` → `user_id`) unless the authenticated account already has a submission for that same app, in which case the authenticated row stays canonical and the anonymous row is marked `state = 'superseded'` (never merged, never deleted, never a second row for that user+app — the partial unique index also structurally guarantees this).
- **Weighting/auto-accept (`tryAutoAccept()` in `community.js`)**: auto-accept requires unanimous agreement **and at least 2 distinct authenticated (`user_id`-bearing) contributors** — anonymous submissions can never trigger it alone, at any count. This is a hard structural gate, not a weighted-sum threshold, specifically because a cookie identity is trivially resettable (clearing cookies / switching browsers produces a new `anonymous_id`) and the per-IP rate limit doesn't stop one person manufacturing several across a day. Anonymous submissions still matter: they still count toward unanimity (their disagreement still blocks acceptance) and still surface apps into the admin queue sooner. A separate, purely informational weighted breakdown (anonymous=1, authenticated=3) is shown to admins on the two admin pages — it never auto-resolves anything, admins still decide disagreements manually.
- Every consensus-related query (`tryAutoAccept()`'s breakdown, `admin.js`'s `submissionsByNormalizedName()` and `COMMUNITY_FLAG_EXISTS`) filters to `state = 'active' AND (anonymous_id IS NOT NULL OR user_id IS NOT NULL)` — superseded and legacy identity-less rows are visible everywhere but never counted. Keep this predicate in sync across call sites if it ever changes.
- My Submissions (`public/my-submissions.html`, `GET /api/community/mine`): a private, non-gamified view of the caller's own submissions (works for anonymous cookie-holders too, not login-gated) — explicitly **no leaderboard, no public profile, no reputation score**. Only one state is actually stored (`community_submissions.state`, `'active'|'superseded'`); everything else displayed (Pending/Accepted/Disputed/Removed) is derived at query time by joining against current `apps`/`unknown_apps` state, not tracked as separate stored events — avoids an entire class of "forgot to update state when an admin acted elsewhere" bugs. Each non-Superseded row has an inline **Edit** control (status buttons + notes, Save/Cancel) backed by `POST /api/community/update` — deliberately narrower than `/submit`: it can only update a row that already exists for the caller's identity, never creates one, and skips re-validating against a live scan session (the original anti-abuse proof was already satisfied once, at the original `/submit` time). Superseded rows aren't editable — they belong to an identity a later authenticated submission has already superseded (see Anonymous → authenticated migration below), so editing one would just reactivate a stale claim.
- Admin/ADMIN_SECRET migration path: `ADMIN_SECRET_ENABLED` (default true) gates the shared-secret branch of `requireAdminAuthOrOAuth`; setting it to `'false'` in Railway retires shared-secret admin access, requiring OAuth+`role='admin'` only. A config change, not a code change — reversible in seconds, and like any `process.env` value takes effect on the next process restart/redeploy, not instantaneously. At no point during the migration is OAuth the *only* admin path unless this is deliberately flipped.
- **Bootstrap / manual role elevation — the permanent process, not a temporary workaround:** log in once via Google or GitHub (creates a `users` row at `role='user'` automatically), find it (`SELECT id, provider, provider_id, display_name FROM users;`), then elevate directly with SQL:
  ```sql
  UPDATE users SET role = 'admin' WHERE provider = 'google' AND provider_id = '1234567890';
  ```
  This remains the only way to create the *first* Admin (a chicken-and-egg bootstrap — `admin-users.html`'s role controls require an existing Admin session to reach at all). Once at least one Admin exists, further role changes (`user`/`researcher`/`admin`, any direction, including demotions) go through `admin-users.html` → `POST /api/admin/users/:id/role` instead — see User roles & Researcher workflow section. Disabling an account (`disabled = 1`) is unchanged: still direct SQL only, no admin UI for it exists or is planned right now — `admin-users.html` shows disabled state read-only.
- **Deploy-readiness safeguard:** `OAUTH_ENABLED` (default unset/false) gates whether the session middleware, Passport, and `/auth/*` routes register at all — if false, none of this code path runs, and the server behaves exactly as it did before this feature existed. If `OAUTH_ENABLED=true` and `SESSION_SECRET` is unset, the server refuses to start entirely rather than falling back to a guessable default (unlike `ADMIN_SECRET`'s `|| 'change-me'`, which is fine for a low-stakes shared secret but wrong for something that can forge login sessions). **Now set to `true` in Railway production** — flipped after the volume persistence pre-flight (below) and full local verification against real Google/GitHub providers; confirmed live via `GET /api/auth-status` returning `enabled:true` with both providers configured.
- **Railway Volume — provisioned and mounted at `/data`** (`ngpcx-volume`, attached to the `ngpcx` service, `DATA_DIR=/data` set in Railway). Originally confirmed via `railway volume list`/`railway logs` that no volume existed and the DB resolved to `/app/data/compatibility.db` (Nixpacks' default build WORKDIR — no Dockerfile/railway.toml/nixpacks.toml in this repo, so that was the default, not a deliberate choice); that's the gap this closes.
- `server/db.js` resolves the DB path via a `DATA_DIR` env var (`DATA_DIR/compatibility.db`, defaulting to the existing repo-relative `data/` if unset — local dev and any environment without `DATA_DIR` set are unaffected). This exists so the volume can be mounted at any path without depending on Railway's internal build directory (`/app`) staying stable.
- **Incident, don't reintroduce:** the first deploy to the empty volume hung indefinitely (Node event loop fully blocked — even unrelated requests like `/health` returned 502) partway through `seedFromCache()`'s original per-row auto-committing insert loop, seemingly a SQLite/volume-filesystem locking issue on the very first write to the freshly-mounted path. Fixed in `server/seed.js`: the entire seed runs inside one `db.transaction()`, so a crash/kill/hang partway through leaves **zero** committed rows (SQLite rolls back the whole incomplete transaction on next open) instead of a silent partial state — verified by force-killing the process mid-transaction and confirming a clean 0-row reopen and reseed.
- **`seedFromCache()` replays both bulk cache sources, not just winget.** A forensic pass (triggered by production's post-volume DB showing only 1,506 rows instead of the historical ~8,883) found the original fix only ever replayed `.cache/winget/` — `.cache/worksonwoa/projects.json` (8,772 entries, also committed to git, also a first-class `enabled: true` scraper in `server/scrapers/index.js`) was never wired into the bootstrap path at all. Not lost data — just an asymmetry in the reseed tooling. Fixed by extracting the exact same per-entry logic the live scrapers use into reusable functions — `saveToDb()` exported from `winget.js`, `buildAppEntry()` extracted and exported from `community/worksonwoa.js` — and calling them from `seed.js`, so the seed never has a second, divergent interpretation of either source's data. Confidence/conflict resolution across the two sources still goes through the normal `mergeApp()` semantics (confidence-gated, agreement bonus), same as a live scraper run would produce.
- Completion is tracked by `meta.seed_sources` (a comma-joined list of which sources completed, e.g. `winget,worksonwoa`) plus `meta.seeded_at`, not just row count or a bare boolean marker — specifically so that when a new source gets added to the expected set (as WorksOnWoA was here), an *older* marker written before that source existed correctly reads as incomplete and safely re-triggers a full reseed, rather than silently leaving the new source unseeded forever. Both markers are written **only if every expected source's cache was found and processed in the same run** — partial availability (e.g. one cache file missing) leaves whatever *did* merge committed, but withholds the marker so the next boot retries the whole seed instead of considering a partially-covered database done.
- Known, deliberately deferred (not restored by `seedFromCache()`, decide separately): ~22 winget-sourced rows present in historical production data but whose cache files no longer exist in the current `.cache/winget/` (real packages — `Microsoft.WSL`, `Microsoft.EdgeWebView2Runtime`, etc. — just cache drift across historical scrape runs); 3 `source='admin'` rows from manual curation, one of which (`admin_test_app_xyz`, "Test App XYZ") is leftover test data, not real.
- The volume-survives-a-redeploy pre-flight (write test data, redeploy, confirm it's still there) was completed before flipping `OAUTH_ENABLED=true` in Railway — see the seed-atomicity incident above, which doubled as that verification (the seeded catalog data demonstrably persisted across multiple redeploys). The app does not and should not try to auto-detect ephemeral-vs-persistent storage from inside the Node process; this stays a manual, one-time judgment call.
- Production also needs `app.set('trust proxy', 1)` (already wired in `server.js`, gated on `NODE_ENV === 'production'`) — Railway terminates TLS at its edge, so without this Express can't see the request as secure and `Secure`-flagged cookies silently misbehave.
- **Explicitly deferred, do not build without being asked:** leaderboards, public profiles, contributor reputation scores, any social/gamification feature. (An in-app role-management UI and a Researcher-invitation workflow — both listed here as deferred in an earlier phase — are now built; see User roles & Researcher workflow section below.) The `contact_opt_in`/`contact_email` columns on `users` exist for a future minimal opt-in "I'm open to helping" path (an admin manually queries `WHERE contact_opt_in = 1` and reaches out by hand) — not built, and deliberately not using OAuth profile email for this, to avoid requesting unnecessary scopes.

## User roles & Researcher workflow
Three authorization roles, unchanged from the Community trust layer section's `users.role`: `user` (default — scan, contribute, edit own submissions, volunteer when eligible), `researcher` (trusted volunteer, see below), `admin` (everything Researcher plus user/role management and full admin tooling). Admin stays small and manually granted — no automatic promotion exists anywhere in this codebase, by role, request count, or any other signal.

**Researcher request is workflow, not a role.** A request (`researcher_requests` table: `id, user_id, status, note, requested_at, reviewed_at, reviewed_by, decision_note`) only ever *causes* a role change when an Admin approves it — the table never itself grants access. `status`: `pending` | `approved` | `declined` | `withdrawn`. A partial unique index (`idx_researcher_requests_pending_user`, `WHERE status = 'pending'`) enforces "at most one pending request per user" at the DB level, not just in application logic — makes repeated "volunteer" clicks harmless even under a race. `decision_note` is Admin-internal only, never returned by any user-facing endpoint.

- **Eligibility** (`server/routes/researcher.js`, `GET /api/researcher/status`): the Researcher-volunteer invitation appears on `my-submissions.html` once a `user`-role account has **3 active community submissions** (`ELIGIBILITY_THRESHOLD`, easy to tune). Meeting it only shows the invitation — it never implies qualification or auto-approval. The endpoint returns a single computed `uiState` (`invite | pending | declined | welcome | none`) rather than raw fields, so the eligibility/cooldown branching logic lives in exactly one place, not duplicated client-side.
- **Declined requests get a cooldown, not a permanent block:** `DECLINE_COOLDOWN_MS` = 30 days from `reviewed_at`. Within the window, `my-submissions.html` shows the declined message; after it, `uiState` reverts to `invite`/`none` based on current eligibility, so re-requesting needs no separate "try again" UI — it's just the normal invitation reappearing.
- **Welcome message** is driven by `users.researcher_welcome_seen` (sticky per-account, not per-request or session-based) plus `role === 'researcher'` — shown once, dismissed via `POST /api/researcher/welcome-ack`. Deliberately not a general notification system; this is the one place a "seen" flag was needed and it's a single boolean column.
- **Approval** (`POST /api/admin/researcher-requests/:id/approve`, Admin-only): marks the request `approved` and sets `users.role = 'researcher'` in one `db.transaction()`. **Decline** leaves `role` untouched — a declined request never disables, punishes, or otherwise marks a normal user; they keep contributing exactly as before.

**Authorization helpers** (`server/middleware/auth.js`): `requireAdmin` and `requireResearcherOrAdmin` are named aliases over the existing rank-based `requireRole()` (`requireRole('researcher')` already admits Admins via the rank comparison — no new logic, just an auditable name at each call site). `server/routes/admin.js` applies these **per-route**, not via a blanket `router.use()` — `GET /unknown-apps`, `GET /stale-apps`, `POST /resolve-app` require Researcher-or-Admin; every user-management, role-change, and destructive-delete route requires Admin. This is a deliberate, minimal expansion of Researcher permissions into the *existing* catalog-review pages (least privilege, easy to audit at each route definition) rather than a new Research workspace or a broad admin-check replacement.

**Admin pages** (both Admin-only, dark-theme-consistent with the rest of the site):
- `admin-dashboard.html` — a work-queue view, not analytics: pending Researcher requests, unknown applications, community findings needing attention (disagreement-flagged), stale entries, authenticated users — each count links to where you'd act on it. No metric invented just to fill the page.
- `admin-users.html` — **Researcher Volunteers** (pending requests first, oldest first, with display name/provider/active+superseded submission counts/account age/request date/optional note/an expandable view of their own community submissions via `GET /api/admin/users/:id/submissions`, and Approve/Decline) then **Users** (default sort: active submissions desc, then account age asc, then `id` asc as a fully deterministic final tiebreaker — `created_at` only has second resolution). Role changes use an explicit select + Apply action (never auto-applies on change) and surface the server's `needsConfirmation` response as a native `confirm()` before resending with `confirmSelf: true`.

**Role-change safeguards** (`POST /api/admin/users/:id/role`, all server-enforced — client-side hiding is convenience only): rejects any role outside `user`/`researcher`/`admin`; rejects malformed/non-integer ids; requires `confirmSelf: true` to change your own role (returns `needsConfirmation: true` otherwise); blocks demoting the sole remaining **active** (non-disabled) Admin — a disabled Admin was never "active," so demoting one is unaffected. This last-Admin check is a plain `COUNT(*)` read immediately followed by the `UPDATE`, with no explicit transaction wrapper needed for correctness: `better-sqlite3` calls are synchronous and Node is single-threaded, so no other request's handler can interleave between the two statements within one process. Authorization always re-reads live `req.user` (role + disabled) on every request via the existing Passport `deserializeUser` — a demoted/disabled Admin or Researcher loses access on their very next request, no logout needed, consistent with the rest of this codebase's auth model.

**Privacy boundaries specific to this feature** (see also the Privacy & Data Governance Audit, `PRIVACY_BACKLOG.md`): contribution counts everywhere in Researcher/user-management UI come from `community_submissions` only — nothing here reads or exposes `sessions.raw_apps`/`sessions.results`, so an Admin reviewing a Researcher request can see *what* someone reported, never their full scan inventory. Provider IDs are never displayed (provider name only, e.g. "Google"). Volunteer notes are visible only on Admin-only endpoints. No email is collected or requested anywhere in this workflow (unchanged from the Community trust layer section's existing scope decision).

**Deferred, do not build without being asked:** a dedicated workspace for *direct catalog resolution* beyond reusing `admin-research.html`/`admin-revisit.html` — that narrower scope is still true today; Community Review (below) is a separate concern (reviewing community evidence and recommendations, not a replacement for direct resolution), and its own dedicated page was a deliberate later decision, not a reversal of this one. Also still deferred: account disable/enable via the admin UI (stays direct-SQL, unchanged from before this phase); reviewer-tier/senior-Researcher roles; any notification system beyond the in-app welcome/pending/declined states already described.

## Community Review (Phase 1)
A dedicated Researcher/Admin workspace (`admin-community-review.html`, backend in `server/routes/admin.js`) for reviewing community evidence and turning it into catalog changes — the gap left after community submissions, inline editing, and Compatibility Notes all existed but had no review workflow of their own. Preserves three deliberately separate concepts, carried through the whole design:
- **Community Findings** — firsthand `community_submissions` rows. Reviewable by a Researcher, never editable by one; only the original contributor edits their own via My Submissions (`POST /api/community/update`).
- **Researcher Recommendations** — a Researcher's evaluation of that evidence (`researcher_recommendations` table). Purely additive: proposing one never touches `community_submissions` or the catalog.
- **The Canonical Catalog** (`apps` table) — the only thing a *confirmed* recommendation updates, and only via the existing `resolveAppCatalog()` path (see below), never a second write path.

**Review queue** (`GET /api/admin/community-review/queue`, `buildCommunityReviewQueue()` in `admin.js`): any app with at least one active, identity-bearing `community_submissions` row qualifies. Deterministic priority sort — conflict (internal disagreement among submissions, or a submission disagreeing with the current catalog verdict) > still sitting in the `unknown_apps` queue with evidence > carries Compatibility Notes > everything else — ties broken by total submission count desc, then app name asc. `buildCommunityReviewQueue()` is the single source of truth for both the queue endpoint and the `dashboard-counts` summary card, so the two can never drift apart. **Don't reintroduce this bug:** "Unknown catalog entries with evidence" means literally still present in `unknown_apps`, not merely "no `apps` row yet" — those are different states (an app can lack a catalog row without ever having been queued as Unknown by a real scan).

**Evidence detail** (`GET /api/admin/community-review/app/:normalizedName`): catalog verdict/source/confidence/notes, the full findings list, and any recommendations. Findings are stripped to `arm_support`/`notes`/`created_at`/a derived `contributor` label (`"Authenticated Contributor"` or `"Anonymous Contributor"`) only — no `anonymous_id`, `user_id`, `session_id`, IP, or scan inventory, matching the Community trust layer's existing privacy boundary. Recommendations, by contrast, **are** attributed by display name (`created_by`/`reviewed_by` joined to `users.display_name`, falling back to a `*_label` like `'shared-admin-secret'` under the legacy secret path) — that's an internal accountability feature for Researchers/Admins reviewing each other's work, not a public-facing one, consistent with how Researcher requests already show display names to Admins elsewhere.

**Recommendation + confirmation workflow** (`researcher_recommendations`, `status`: `'pending' | 'confirmed' | 'rejected'`): a Researcher proposes (`POST .../app/:normalizedName/recommend` — proposed compatibility, optional Compatibility Notes, optional internal rationale). At most one pending recommendation per app at a time (partial unique index in `db.js`) — proposing a second while one is pending returns 409 rather than piling up. A second Researcher or Admin then acts:
- **Confirm** (`POST .../recommendations/:id/confirm`) — applies the recommendation exactly as proposed.
- **Revise** (`POST .../recommendations/:id/revise`) — the second reviewer adjusts the proposed values and applies them in one step. Deliberately a single-step "confirm with edits," not a propose-again/re-confirm cycle — a full revision-history workflow would be the "workflow engine" this phase was explicitly asked to avoid (see Deferred below).
- **Reject** (`POST .../recommendations/:id/reject`) — `status = 'rejected'`, no catalog change, no `community_submissions` change. Unblocks a fresh recommendation for the same app.
- **Leave Unresolved** (`POST .../recommendations/:id/unresolved`) — a genuine no-decision. `status` intentionally stays `'pending'`; `reviewed_by`/`reviewed_at`/`review_note` still update, doubling as a lightweight "last looked at by" marker without a separate touch-tracking column.

**Self-review is blocked, not just discouraged**: `assertNotSelfReview()` in `admin.js` rejects (403) any Confirm/Revise/Reject/Unresolved where the reviewer's `req.user.id` matches the recommendation's `created_by` — server-enforced, not just hidden client-side (the UI also greys these out and shows an explanatory note, using `id` now included in `GET /api/auth-status`'s `user` object specifically to support this). Only meaningful under OAuth; the shared `ADMIN_SECRET` path has no `req.user` and so no self to compare against — same documented limitation as the rest of that legacy path.

**Only a confirmed recommendation touches the catalog, and it reuses the existing resolve path.** `resolveAppCatalog()` in `admin.js` is the one function that ever writes a verdict to `apps` — both `POST /api/admin/resolve-app` (the pre-existing direct admin-research/admin-revisit flow) and Confirm/Revise now call it, so there is exactly one catalog-writing implementation, not two that could drift. Same authorization (`requireResearcherAuthOrOAuth`) governs every entry point into it.

**Stale-recommendation confirmation guard**: a recommendation's proposed verdict is a snapshot of the Researcher's judgment at propose-time — Confirm/Revise must not silently apply it if community evidence has materially changed since. `researcher_recommendations.evidence_snapshot` stores a small JSON fingerprint at proposal time (`computeEvidenceFingerprint()` in `admin.js`): the sorted set of distinct `arm_support` values among active submissions, whether the community agreed with the catalog verdict, a SHA-256 hash of sorted non-empty note text (detects addition/removal/edit without persisting the notes themselves), and a submission count (informational only). **Not a copy of the evidence** — no contributor identity, no raw note text, no scan data. At Confirm/Revise time, `checkEvidenceFreshness()` recomputes the fingerprint live and compares (`compareEvidenceFingerprints()`): material change is a verdict appearing/disappearing from the distinct-opinions set, catalog/community agreement flipping, or the notes hash changing — a plain increase in an *already-present* status's count (another agreeing submission) is deliberately not material and never blocks. A material change returns 409 with `evidenceChanged: true`, a concise `summary` string, and a `changes` breakdown (new/removed verdicts, whether catalog-agreement or notes changed, submission counts before/after) — the recommendation is left untouched (still `pending`, catalog untouched). The reviewer must resend with `acknowledgeEvidenceChange: true` to proceed; doing so prepends an auto-generated note to `review_note` recording that confirmation happened despite the change, for the same accountability reasons as the rest of this section. The self-review guard is checked first and independently — acknowledging an evidence change never bypasses it. `admin-community-review.html`'s `showEvidenceChangeBanner()` handles the 409 by refreshing the card's evidence in place (via the same `refreshCard()` used elsewhere) *before* offering the acknowledgment button, so the reviewer sees current findings, not stale ones, when deciding. **Known lightweight-by-design gap:** the acknowledgment isn't itself versioned against a specific evidence state — a second change arriving in the brief window between viewing the refreshed evidence and clicking acknowledge wouldn't be caught. Acceptable for a low-traffic internal tool; a stricter design would need to re-check freshness at the moment of acknowledgment too, which was judged unnecessary complexity for this phase.

**Research Logging** (`research_activity_log`, append-only, no UI reads it yet): every catalog-changing action (`direct_resolve`, `recommendation_confirmed`, `recommendation_revised`) and every recommendation-lifecycle event that doesn't change the catalog (`recommendation_created`, `recommendation_rejected`, `recommendation_left_unresolved`) writes one row — app name, actor (`actor_id` + `actor_label` fallback, same pattern as `researcher_recommendations`), action, previous/new `arm_support`, previous/new notes, timestamp. Captured now specifically so a future Research Activity page has real history to show rather than needing a backfill.

**Dashboard**: `admin-dashboard.html` gained an "Applications awaiting review" card (`communityReviewApps` from `dashboard-counts`, reusing `buildCommunityReviewQueue()`) linking to Community Review, with a small secondary "N conflicts" indicator (`communityReviewConflicts`) when nonzero.

**Explicitly deferred (do not build without being asked)** — acknowledged, not started: a dedicated Research Activity page; researcher activity history UI; a peer-review dashboard; recommendation revision-history UI; rollback support; evidence-derived confidence scoring; AI-assisted research or automatic recommendation generation; x64/x86 emulation simplification (still tracked as two separate statuses everywhere); a richer device-review workflow (Community Review is apps-only, matching `community_submissions`'s existing app-only scope); public contributor recognition; researcher metrics; notifications; messaging; approval automation.

## Product Polish milestone (coherence, trust, first impressions)
A deliberately non-scanner milestone, explicitly prioritizing coherence over new capability, ahead of limited external testing.

**Assessment Pipeline Staging** (`report.html`): the loading state now has two distinct, never-blended stages, matching the real underlying architecture (Scanner = observation, the assessment pipeline = reasoning, Results = recommendation):
- **"Scanner connected"** — shown only when a physical scanner genuinely produced this page load (a fresh scan from `index.html`). Default HTML state.
- **"Preparing Assessment"** — covers classification, evidence synthesis, confidence calculation, and recommendation generation. Shown from page load onward for any arrival that never involved a scanner (Recalculate, the Workspace "Results" link, or an imported session) via a new `?origin=assessment` URL flag set by `workspace.html`'s `resultsUrl()` and `index.html`'s import redirect — and shown as a brief transitional swap (before "Scanner connected") once a genuine scan poll detects `status === 'complete'`, so a fresh scan also passes through the same stage on its way to Results, not a jump straight from "scanner" language to a finished report.
- The transition includes a short, honest pause (150ms for assessment-only arrivals, 400ms for fresh scans) — real computation happens in that window (client-side classification/synthesis), the pause just ensures the stage is perceptible rather than flashing for one frame. Not an artificial "fake work" delay; a fast Recalculate still feels fast.
- `checkSession()` (the poll body) is called immediately for assessment-only arrivals rather than waiting out the first 1.5s interval tick, so Recalculate doesn't feel sluggish for results that are already sitting in the database.

**New page: `how-it-thinks.html`** ("How CheckMyARM Thinks") — a philosophy page, not a technical one. Organized around: starting with observation, gathering evidence not certainty (with plain examples — running apps, startup apps, recently-launched apps, attached devices — deliberately no mention of UserAssist/Prefetch/registry/thresholds anywhere), no single observation deciding anything, community knowledge, why the Workspace exists, Confidence as certainty-not-correctness, and privacy by design. Carries the mission statement (`Our goal is to reduce the stress of uncertainty.`) as both opening line and closing callback — a deliberate exact-repeat bookend, not an accidental duplicate. Linked from `index.html`/`report.html`/`workspace.html` nav (alongside the existing Why/About links) and from `about.html`/`why.html` in-body cross-links.

**Privacy page refresh** (`privacy.html`): new leading "Privacy by design, not by promise" section states the philosophy up front (we don't want/need privacy-invasive data; documents/passwords/browsing history/personal communications/file contents are never collected because none of it answers the compatibility question; we never retain your assessment; saving your work is your choice, not ours; minimizing collection/retention is the actual mechanism of privacy-by-design, not just protecting whatever's already been collected) — before the existing technical detail sections (What we collect / What we do NOT collect / Scan retention / Community contributions / Third-party services), which are otherwise unchanged. The old standalone "Our philosophy" closing section was trimmed to a short "In short" summary to avoid restating the new opening section almost verbatim — a real duplicate the terminology sweep below caught and fixed, not left as an intentional bookend the way `how-it-thinks.html`'s repeat is.

**Mission statement placement** — deliberately restrained, not site-wide: `how-it-thinks.html` (opening + closing), `about.html` (one line in the origin section, immediately after the existing "fast, objective, and a lot less uncertain" sentence it naturally extends). **Explicitly not added** to the homepage hero (already five paragraphs of copy — a sixth would be the "marketing wallpaper" this milestone was explicitly asked to avoid) or to any footer.

**Terminology/consistency sweep findings** (full sweep across all visitor-facing pages, not just this milestone's own new copy): confirmed clean — no stale "Quick/Standard/Full Scan" labels (all correctly say "Light/Standard/Advanced Assessment"), no edit-implying language on `report.html` (explicitly read-only throughout, confirmed via its own `// Results ... editing or researching it is not` code comment), no leftover "Check →" research links on Results (confirmed absent, correctly Workspace-only), no "Unclassified" anywhere (the "Unknown" revert from an earlier phase held). One real, pre-existing duplication found and fixed: `report.html`'s "Not Currently Prioritized" section had its own explanation of Light Assessment's prioritization logic that nearly restated the separate `quick-apps-note` shown higher on the same page — trimmed the section's own copy to a short, non-repeating action line ("These aren't currently shaping your Score or Confidence — promote any that matter to you from the Workspace") since the fuller mechanism explanation already exists once, higher up.

## Attention & Relevance milestone (post-deploy tester feedback)
The first limited external testing pass on the Product Polish build (above) surfaced real, concrete feedback — described by the person who commissioned it as "assessment/UI refinements rather than scanner problems," and every fix below stayed on that side of the line: `main.rs` is untouched, `scan.js`'s `classifyApps()`/`isSystemComponent()` are untouched. Three problems, all solved client-side in `assessment.js` plus nav markup in the five informational pages.

**1. Session-aware navigation.** Once an assessment session exists, it's now the effective "home" — before this, `about.html`/`why.html`/`privacy.html`/`how-it-thinks.html`/`my-submissions.html` always pointed their logo and back-link at the plain homepage regardless of an active session, stranding a user who clicked through from Results or Workspace.
- `assessment.js` gained `resolveActiveSession()` (reads `?session=`/`&level=` off the CURRENT page's own URL, validates via `GET /api/session/:id`, treats anything other than `status: 'complete'` — missing, expired, still mid-scan — as no session) and `applySessionAwareNav()` (called once on load by the five informational pages; rewrites `#nav-logo-link`/`#nav-back-link` to point at Results, reveals `#nav-workspace-link`, and propagates the session onto every `[data-info-link]` cross-link in the page body). A missing/invalid session is a silent no-op — the plain-homepage markup already in each page is the default, not a fallback branch that has to be triggered.
- `decorateInfoLinks(sessionId, level)` is the other half — called synchronously by `report.html`/`workspace.html` themselves (which already know their own session id at load, no async check needed) to append `?session=&level=` onto their own outgoing nav links to Why/How CheckMyARM Thinks/About/My Submissions, so the thread never drops on the way out either.
- Every page's logo is now wrapped in an `<a>` for the first time (previously a non-clickable `<div>` everywhere) — a small incidental improvement, not just a vehicle for the session-aware href.
- Verified directly (synthetic session, real browser navigation): a valid `status:'complete'` session correctly rewrites logo/back-link/Workspace-link and propagates through every in-body cross-link; a missing session, a bogus session id (404), and no `?session=` param at all all correctly fall back to the plain homepage default with no console errors.

**2. Attention eligibility — "does this deserve the user's attention."** The concrete complaint: Windows platform/shell AppX packages (`Microsoft.WindowsNotepad`, `Microsoft.VP9VideoExtensions`, `Microsoft.Winget.Source`, `Microsoft.XboxIdentityProvider`, `Microsoft.MicrosoftSolitaireCollection`, PowerToys context-menu shims) were dominating Needs Attention purely for having `is_running` or `has_start_menu_entry` — the two weakest, most passive signals in the whole Guidance Signal set, true of nearly any OS-shell process. Explicit instruction: a small number of durable architectural rules, not a name list. This is a genuine evolution of the standing Guidance Signal philosophy (CLAUDE.md, Observation Enrichment milestone) — **"Evidence should support importance. Evidence alone should not automatically create importance"** is the new governing sentence alongside the old one, not a replacement for it.
- **`isPlatformComponent(app)`** — structural, not a name list: an AppX (`discovery_source === 'appx'`) package-family-name matching `PLATFORM_COMPONENT_ID_PATTERN` — `^Microsoft\.(Windows|Xbox|Gaming|Microsoft)` (Microsoft's own first-party inbox/shell/gaming-integration naming conventions, including the redundant "Microsoft.Microsoft*" doubled prefix real Microsoft Studios inbox apps use — MicrosoftSolitaireCollection, MicrosoftStickyNotes) or a generic plumbing keyword (`Extension(s)`, `Provider`, `.Source`, `ContextMenu`, `Overlay`, `SparseApp`, `Service(s)`). Never applied to winget/ARP apps — the convention is Microsoft-AppX-specific.
- **`hasDeliberateUseEvidence(app)`** — `is_startup || is_pinned_taskbar || hasFrequentUserAssistLaunches || hasRecentUserAssistLaunch || hasMeaningfulDefaultHandler` — every signal that reflects an actual human choice, deliberately excluding `is_running`/`has_start_menu_entry`.
- **`qualifiesForAttention(app, decision)`** — the eligibility gate itself: `critical_to_me` always passes; deliberate-use evidence always passes; a platform component with only passive evidence fails; everything else (an ordinary winget/ARP/AppX app with `is_running` or `has_start_menu_entry`, still the majority case) passes exactly as before. This is a PRIOR question to `defaultImportanceForApp()`, not a replacement for it — passing items are still ranked/labeled by the existing single-reason-chip logic unchanged.
- **`classifyAssessmentItem()`** gained a 4th outcome, `lowSignal`, checked only for genuinely Unknown apps (`bucket !== null`) that fail the gate — settled/verified/known-bucket items are unaffected, and devices never go through this gate at all (unchanged: "a Light-scanned device list is already just 'currently connected'"). Callers (`report.html`'s `renderReductionSummary`, `workspace.html`'s `bucketItems()`) now pass `app` through.
- **New UI surface, both pages**: `report.html` gained a `renderLowSignal()` collapsed section (same `preview-list`/`<details>` pattern as Not Currently Prioritized, deliberately a separate section — orthogonal axis, an item can be in both/either/neither, same precedent as the existing Not-Currently-Prioritized-vs-Needs-Attention relationship). `workspace.html` gained a `lowSignal` group in `bucketItems()`/`renderGroups()`, same collapsed-by-default treatment as Already Considered/Set Aside. Low-signal items are still fully classified, scored, visible, and promotable ("This matters to me" always overrides) — this only changes where they're grouped for attention, never the compatibility bucket or the score.
- **Real fix found during validation, not anticipated up front:** `report.html`'s reduction-narrative loop (`renderReductionSummary`) never checked `isLightDeprioritized()` before this pass — a pre-existing gap (present before this milestone too) that let a Light-deprioritized app appear in the "Needs Attention" preview AND in the separate "Not Currently Prioritized" section simultaneously. Fixed by skipping Light-deprioritized apps at the top of the loop, matching what `workspace.html`'s `bucketItems()` already correctly did. Verified via a synthetic Light-mode scan: before the fix, a deprioritized app appeared in both places; after, it correctly appears only in Not Currently Prioritized.
- **Verified end-to-end against a synthetic Standard scan** (real HTTP session + `/api/scan` payload, not just code reading): `Microsoft.VP9VideoExtensions`, `Microsoft.Winget.Source`, `Microsoft.XboxIdentityProvider`, `Microsoft.PowerToys.FileLocksmithContextMenu`, and `Microsoft.MicrosoftSolitaireCollection` all correctly routed to Low-Signal Items instead of Needs Attention; Google Chrome, Microsoft Teams, Visual Studio Code, and WhatsApp (none AppX-platform-pattern matches, or not AppX at all) kept full normal evidence treatment and landed in Needs Attention/Already Considered exactly as before — confirming Teams/365/VS-style real applications are structurally exempt from this gate, not coincidentally spared.

**3. Browser-hosted PWA compatibility inheritance.** A Chrome/Edge/Firefox-installed PWA (`messages.google.com`, TripIt, and similar HKCU-ARP-discovered entries — see Scanner Enrichment milestone) has no independent ARM64 binary of its own; it runs entirely inside its host browser's rendering engine, so an independent "? Unknown" verdict for it is architecturally misleading, not merely unresolved.
- **`isBrowserHostedPWA(app)`** — `discovery_source === 'arp'` and `publisher` matches a known browser-vendor prefix (`Google\Chrome`, `Microsoft\Edge`, `Mozilla\Firefox`, `BraveSoftware\Brave-Browser`) — the exact per-user ARP Publisher string the scanner already reports for these entries (Scanner Enrichment milestone); no new scanner signal needed.
- **`findHostBrowserEntry(app, allApps)`** locates the host browser's own entry within the SAME scan population by normalized name, and **`effectiveBucketFor(app, bucket, decision, allApps)`** is the one shared priority function — personal verification first (an explicit human claim), then PWA inheritance for a still-Unknown app (an architectural fact), then the raw catalog bucket — used identically by `computeSynthesis()` (score/confidence), `classifyAssessmentItem()` call sites (attention grouping), and `pwaInheritanceNote()` (UI transparency), so scoring and grouping can never silently disagree about an app's effective bucket the way two independent inline copies of this logic previously risked.
- **Never a silent override**: the PWA's own row still renders in its raw "? Unknown" bucket/badge everywhere (`report.html`'s app table, `workspace.html`'s status badge) — same "row stays where it renders" precedent as the existing `personally_verified` badge — with an ADDITIVE note ("Runs inside Google Chrome — treated as Native ARM64 for your Score, since it has no separate ARM64 binary of its own") shown next to it, always visible, not gated behind Deeper Research Mode.
- Considered and deliberately NOT done: `Angry Birds Friends`, named alongside `messages.google.com`/TripIt in the original feedback as a "PWA-like" example, turned out on inspection to be a genuine Store/AppX game package (`discovery_source: 'appx'`, not an ARP entry with a browser-vendor Publisher) — structurally NOT a browser-hosted PWA by this milestone's own detection logic, so it correctly does NOT inherit anything and stays an independent Unknown verdict. Flagging this rather than forcing a special case for it, per the same "principled model over individual exceptions" instruction the rest of this milestone followed.
- Verified end-to-end against a synthetic scan: two ARP entries with `publisher: "Google\\Chrome"` alongside a real "Google Chrome" winget entry (native) both correctly inherited Native, contributed to score/confidence as Native, landed in Already Considered (not Needs Attention/Low-Signal), and rendered the inheritance note on both Results and Workspace.

**Known, not fixed — a pre-existing catalog-data artifact found during validation, unrelated to this milestone's own logic:** a synthetic "Notepad" app (`Microsoft.WindowsNotepad`) fuzzy-matched to an `emulated` catalog entry in local dev data rather than `native` — almost certainly stale/unrelated catalog data (or a coincidental fuzzy-name collision) rather than a bug in this milestone's code, since `isPlatformComponent()`/`qualifiesForAttention()` never touch classification, only attention grouping. Not investigated further or "fixed" by editing catalog data, since altering `apps` table contents wasn't part of this task and the actual production catalog may already have this correct.

## Background / Reference milestone (Report vs Workspace separation)
A direct follow-up to the Attention & Relevance milestone above, after the user's own real assessment still felt like "a filtered software inventory" rather than a recommendation. Core principle, stated explicitly and now load-bearing across this whole area: **the catalog supports the assessment — it does not become the assessment.** Everything the scanner/catalog find stays fully discoverable (Workspace, especially Advanced); not everything needs to be surfaced as if it were an equal decision driver. Still entirely client-side (`assessment.js` + `report.html`/`workspace.html`); `main.rs` and `scan.js` remain untouched.

**1. Unified background/reference classification** — the prior milestone's `isPlatformComponent()` (AppX package-ID keyword patterns) missed real examples from the user's own machine (`Microsoft.PowerAutomateDesktop`, `Microsoft.CommandPalette` — neither matches any plumbing keyword or the `Microsoft.Windows*`/`Xbox*`/`Gaming*`/`Microsoft.Microsoft*` prefixes). Replaced with a broader, simpler, more durable TYPE test:
- **`isMicrosoftAppxPackage(app)`** — `discovery_source === 'appx'` AND (publisher contains "microsoft" OR the package family name `id` starts with `Microsoft.`). Two signals, not one, found necessary during validation: `classifyApps()`'s fuzzy name-matching can pair an AppX package with an unrelated third-party catalog entry, and `mergeEntryWithApp()` lets that catalog row's publisher win over the scanner's own — a synthetic `Microsoft.WindowsNotepad` package matched an unrelated catalog app and inherited its publisher (`"Arsh Digital"`), which would have silently defeated publisher-only detection. `app.id` (the package family name) is scanner-reported and never overwritten by the catalog merge, so it survives this failure mode. Deliberately still publisher/id-based, never a package-ID keyword list — "is this Microsoft's own first-party AppX software," not "does this name look like plumbing."
- **`isBrowserHostedPWA(app)`** — unchanged from the prior milestone (ARP + browser-vendor Publisher).
- **`isBackgroundCandidate(app)`** = `isMicrosoftAppxPackage(app) || isBrowserHostedPWA(app)` — a structural TYPE verdict, independent of which compatibility bucket the app resolves to.
- **`hasBackgroundOverride(app, decision)`** — `critical_to_me`, `personally_verified`, or real deliberate-use evidence (`hasDeliberateUseEvidence`) — the one way a background-typed item earns full treatment. Passive evidence (`is_running`/`has_start_menu_entry`) is deliberately never an override.
- **`isBackgroundApp(app, decision)`** = `isBackgroundCandidate(app) && !hasBackgroundOverride(app, decision)` — the single verdict used identically by attention grouping (`classifyAssessmentItem`), score/confidence weighting (`computeSynthesis`), and Report framing, so they can never silently disagree. The prior milestone's `qualifiesForAttention()` helper became redundant under this design and was removed.
- `classifyAssessmentItem()`'s outcome was renamed `lowSignal` → **`background`**, and its check now runs BEFORE the resolved/unresolved check (previously only genuinely-Unknown apps could be low-signal) — a background-typed app stays background-weighted/grouped even when the catalog does have a real native/emulated/unsupported verdict for it, since the whole point is this kind of software rarely drives a purchase decision either way. Both pages' UI renamed "Low-Signal Items" → **"Background Items"**.

**2. Score/Confidence weighting** — new `BACKGROUND_WEIGHT = 0.25` constant (same order of magnitude as `DOESNT_MATTER_WEIGHT`, a distinct concept, retunable independently). Applied inside `computeSynthesis()`'s per-app loop identically to `doesnt_matter`/`critical_to_me` — a background app's Score/Confidence contribution is multiplied down, REGARDLESS of whether its bucket is resolved or still Unknown (an inherited-native browser-hosted PWA is still down-weighted; "rarely a valuable assessment driver" is a statement about influence, not merely about uncertainty). Background-typed unresolved apps are also excluded from `unresolvedCount` (the "N unresolved apps are holding back a higher confidence rating" sentence) — at 0.25 weight they barely move Confidence, so counting them there would overstate their real impact. `scoreReasons` (Primary Reasons) now excludes background apps outright, not just down-weights them — Primary Reasons is explicitly "why this recommendation," and background software shouldn't headline it even on a large raw point swing.

**3. Report vs Workspace: curate vs expose.** `computeSynthesis()` returns two new counts, `materialCount` (apps that are NOT background — what actually drove today's recommendation) and `backgroundCount`. `report.html`'s reduction-narrative headline was rewritten around them: *"We analyzed N applications. M materially influenced today's recommendation. K are worth a closer look — sharing what you know about them helps you and the next person. Everything else — including Microsoft platform software and browser-hosted apps — stays fully available in the Workspace to review or contribute to."* Replaces the old "We evaluated N applications and devices... X already provide enough information..." framing, which counted every resolved app as if it were an equal contributor. The four raw app-bucket tables (`buildTable()` — native/emulated/unsupported/unknown) are now collapsed-by-default `<details>` for every type, not just System Components (a small unconditional-`<details>` generalization, dropping the old type-specific branch) — the full inventory stays exactly as discoverable (one click), it's just not presented as if every row were an equal decision driver.

**4. Community contribution alignment.** The refresh banner's copy (`renderRefreshBanner()`) now names the actual small "worth a closer look" count (the same population `renderReductionSummary()` already computes, cached in `currentNeedsAttentionCount`) — *"3 applications worth your attention don't have a confirmed verdict yet..."* — instead of a generic "some apps don't have a confirmed verdict yet," which used to fire for any of dozens of Unknown/Emulated/Unsupported rows regardless of relevance. Falls back to a background-specific message when nothing material is unresolved, rather than staying silent or misleadingly generic.

**Verified end-to-end against a realistic synthetic Standard scan** (15 apps: Chrome/Teams/VS Code/WhatsApp as real user software; the user's own named examples — WindowsNotepad, VP9VideoExtensions, Winget.Source, XboxIdentityProvider, MicrosoftSolitaireCollection, PowerToys.FileLocksmithContextMenu, PowerAutomateDesktop, CommandPalette — as Microsoft AppX; two Chrome-hosted PWAs plus one Edge-hosted PWA where Edge itself isn't in the scanned population, to exercise the uninheritable case):
- Headline correctly read "We analyzed 15 applications. 4 materially influenced today's recommendation" (vs. a naive "15 analyzed, 7 already resolved" framing the old copy would have produced).
- All 8 named Microsoft-platform examples correctly routed to Background Items regardless of resolved bucket — including `Command Palette`, which fuzzy-matched a real catalog `native` entry and still correctly stayed background-weighted rather than becoming a Primary Reason.
- The Edge-hosted PWA with no Edge in the population correctly showed a plain "? Unknown" badge with no fabricated inheritance claim — the two Chrome-hosted PWAs correctly inherited Chrome's Native status with the existing transparency note.
- `critical_to_me` ("This matters to me") correctly overrides the background verdict, confirmed directly against `classifyAssessmentItem()`.
- Results and Workspace agreed exactly on both counts (4 material / 11 background) with no console errors.
- **Known test-environment artifact, not a product bug:** the animated score gauge (`animateScore()`, driven by `requestAnimationFrame`) rendered "0" in this headless browser-automation pane while the underlying `computeSynthesis().score` was correct and the readiness label/explanation text rendered correctly — `requestAnimationFrame` appears not to tick reliably in this specific automated context. `animateScore()` itself was not touched by this milestone.
- **Limitation, stated plainly:** this environment has no real Windows machine or physical scanner available, so "use a real assessment while refining the model" was necessarily approximated with a realistic synthetic population built from the user's own named real-world examples, not their actual machine's scan. The user should re-run a real Light/Standard assessment to confirm the before/after populations match this validation.

## Canonical Effective Status milestone (consistency correction)
A focused correction after real-machine testing of the Background/Reference milestone found several places still displaying or grouping by the RAW catalog bucket while the assessment had already computed a different EFFECTIVE status for the same item — e.g. a browser-hosted PWA whose badge said "? Unknown" right next to a note saying "treated as Native ARM64." Root cause: `effectiveBucketFor()`/`isBackgroundApp()` were being called independently at several UI call sites (Report tables, Report counts, Workspace badges, the reduction narrative) instead of from one shared source, so they could silently drift apart. Still entirely client-side (`assessment.js` + `report.html`/`workspace.html`); `main.rs` and `scan.js` remain untouched.

**1. `classifyPopulation(results, personalContext)`** (`assessment.js`) — the one canonical per-app classification: `{app, rawBucket, effectiveBucket, isBackground, decision}`. `computeSynthesis()` now builds its working set from this instead of re-deriving `effectiveBucketFor()`/`isBackgroundApp()` inline in three separate places (the main loop, `scoreReasons`, `confidenceReasons`) — a real bug this surfaced: `confidenceReasons` was checking the RAW bucket for "still unresolved," so a critical-to-me PWA that had successfully inherited Native from its browser could still wrongly show as "limiting confidence." `report.html`'s `renderReport()` now computes `classified` once and threads it through counts, tables, and `renderReductionSummary()`.

**2. Badges always show the EFFECTIVE status, never raw.** `buildTable()` (`report.html`) and `statusBadge()` (`workspace.html`) both read `effectiveBucket`, not the raw catalog bucket — a browser-hosted PWA that inherited Native from Chrome now shows "✓ Native ARM64 (inherited)" as its primary badge, not "? Unknown" with a separate explanatory note that used to read as contradictory. Raw catalog status, when it differs from effective, is now shown ONLY in the expandable detail row (Report) / Deeper Research Mode (Workspace), explicitly labeled "Raw catalog status:" — never blended with the primary badge. `checkLink()` also switched to effective bucket, so an inherited-Native PWA no longer offers a "Check →" research link for something already resolved.

**3. Background apps excluded from the primary Report tables entirely, not just re-labeled.** Previously a background-typed app (Microsoft AppX platform software, browser-hosted PWA) still appeared in the raw Native/Emulated/Unsupported/Unknown tables AND in the separate Background Items section — genuinely duplicated. `materialByBucket` (`report.html`) now excludes both `isBackground` AND deprioritized apps before populating the four primary tables and their stat-count cards, so those counts always equal `synthesis.materialCount` broken out by bucket — no more "Unknown (7)" in the table while the headline says "2 unresolved apps." System Components reuses the same `{app, rawBucket, effectiveBucket, decision}` shape (`rawBucket === effectiveBucket === 'system'`) for consistency, even though it has no real bucket concept.

**4. Background status is checked BEFORE deprioritization, not after — a real ordering bug.** `isDeprioritized()` (Light/Standard footprint gate) used to run first in `computeSynthesis()`, `bucketItems()` (`workspace.html`), and `renderReductionSummary()` (`report.html`), which meant a background-typed app with no footprint evidence (true of most platform AppX packages) got silently absorbed into "Not Currently Prioritized" instead of "Background Items" — and `backgroundCount` swung between assessment levels even though background-ness is a structural, level-independent fact. Fixed by checking `isBackground` first everywhere: a background app now always lands in Background Items and always counts toward `backgroundCount`, regardless of scan level; deprioritization only ever applies to genuinely non-background apps. Verified directly: `backgroundCount` is now identical (11) across a Light and a Standard scan of the same synthetic population, where before the fix it read 5 under Light and 11 under Standard for no principled reason.

**5. Standard's material population is no longer "everything not background."** `isDeprioritized()` gained a real Standard-tier gate — `isStandardPriorityApp(app)` (`has_start_menu_entry || isLightPriorityApp(app)`) — broader than Light's strict gate (which deliberately excludes `has_start_menu_entry`) but still a real bar, not "any catalog match counts." Previously *every* non-excluded, non-background app counted toward `materialCount` under Standard regardless of evidence, which is why a real Standard scan reported "92 materially influenced" — essentially the full non-background catalog population. Verified against a realistic synthetic 22-app population: Light `materialCount` 5, Standard `materialCount` 6 (one footprint-only app, `has_start_menu_entry` but otherwise untouched, correctly promotes from deprioritized under Light to material under Standard) — a small, real, credible difference, not "Standard = Light with a lower Confidence bar."

**6. Terminology: "Needs Attention" → "Deserves Attention"**, throughout `assessment.js` (`classifyAssessmentItem()`'s return value), `report.html`, and `workspace.html` — code identifiers (`groups.needsAttention` → `groups.deservesAttention`, `needsAttention` array/count variables) renamed alongside the display text, not just the label, so the two can't drift back apart later. `currentNeedsAttentionCount` → `currentDeservesAttentionCount`.

**Real bug found and fixed while doing this rename, unrelated to the rename itself:** `workspace.html`'s wiring loop (`['notPrioritized', 'needsAttention', 'alreadyConsidered', 'setAside'].forEach(...)`, which calls `wireItemCard()` to attach decision-button/checkbox listeners) never included `'background'` — added in the prior milestone but never wired into this loop, so every card in Background Items rendered with dead buttons. Fixed by adding `'background'` to the array.

**Verified end-to-end against the same realistic synthetic 22-app population used in the prior milestone**, extended with 5 zero-footprint "Ghost Utility" apps (no evidence of any kind — the exact shape of the real-world noise that produced "92 materially influenced") and one footprint-only app to exercise the Light/Standard boundary:
- Standard: "We analyzed 22 applications. 6 materially influenced today's recommendation" (down from what would have been ~17 under the pre-fix "everything not background" definition) — all counts reconcile exactly (4 Native + 0 Emulated + 0 Unsupported + 2 Unknown = 6 material; 6 + 11 background + 5 not-currently-prioritized = 22 total).
- Light: 5 materially influenced, 6 not currently prioritized, 11 background (same background count as Standard) — confirms Light and Standard now have meaningfully different material populations from the identical underlying scan, not just a Confidence-tier difference.
- Browser-hosted PWAs (Chrome-hosted, inheritable) showed "✓ Native ARM64 (inherited)" consistently on both Results and Workspace; an Edge-hosted PWA with no Edge in the population honestly showed "? Unknown" with no fabricated inheritance claim, on both pages, in both the primary badge and the Background Items list.
- No item appeared in two groups simultaneously (spot-checked "Not Currently Prioritized" vs. the primary Unknown table — zero overlap after the fix, five items of overlap before it).
- No console errors, no duplicate DOM ids, confirmed via direct DOM inspection across both pages and both assessment levels.
- **Known test-environment artifact, unrelated to this milestone:** the animated score gauge (`animateScore()`, `requestAnimationFrame`-driven) again read "0" in this headless browser-automation pane while `computeSynthesis().score` itself was verified correct via direct inspection — the same pre-existing environment limitation documented in the prior milestone; `animateScore()` was not touched.
- **Limitation, stated plainly:** no real Windows machine is available in this environment. This pass used the user's own real-machine feedback (the concrete "92 materially influenced" figure, the specific contradiction examples) to build a realistic synthetic reproduction and verify the fix against it — the user should re-run a real Standard assessment to confirm the corrected counts feel right in practice.

## Browser-Hosted PWA Compatibility milestone (real-machine follow-up)
A focused follow-up after real-machine testing of the Canonical Effective Status milestone found one remaining materially misleading case: browser-hosted PWAs (Chrome/Edge/Firefox/Brave-installed, ARP-discovered — see `isBrowserHostedPWA()`) still showed as `? Unknown`. The prior design ("inherit the classified bucket of whichever specific browser entry is present in this scan's own population," via `findHostBrowserEntry()`) had two real flaws the user's testing surfaced: it silently reverted to Unknown whenever that specific browser wasn't itself present in the scan population, and it implied — wrongly — that the future ARM PC must run that exact browser to get the benefit. Still entirely client-side (`assessment.js` + `report.html`/`workspace.html`); `main.rs` and `scan.js` remain untouched.

**The governing product rule, now load-bearing:** browser-hosted applications inherit compatibility from the browser platform and are presented as supported through a compatible ARM64 browser — not as Unknown merely because they have no independent ARM64 binary. This is a TYPE fact about the app (it has no ARM64 binary to classify, by architecture, not by data gap), never contingent on which specific browser happens to be in this scan's population — Chrome, Edge, Firefox, and Brave all ship real ARM64-native builds today, so the buying-decision conclusion holds regardless of which one the user currently uses.

**1. New `'supported'` effective bucket** — a bucket value distinct from native/emulated/unsupported/unknown, assigned unconditionally by `effectiveBucketFor()` whenever `bucket === 'unknown' && isBrowserHostedPWA(app)` (no host-browser lookup, no dependency on any specific browser's presence or bucket). `findHostBrowserEntry()`/`hostBrowserNameFor()`/`BROWSER_HOST_APP_NAMES` were deleted outright, not deprecated — the new design has no use for "which browser hosts this" at all, only "is this confidently a browser-hosted PWA." Badge label: "✓ Browser-Hosted (Supported)," a third distinct color (`.badge.supported`, teal `#2dd4bf` in `assessment.css`) — not native's green (it isn't a literal ARM64 binary verdict) and not check's blue (nothing here needs further research). `pwaInheritanceNote()` was renamed `pwaSupportedNote()` and rewritten to drop all browser-specific wording: *"This browser-hosted application does not require a separate ARM64 binary. It can be installed or used through a compatible ARM64 browser on your ARM PC."* No "(inherited)" badge suffix anymore — the `supported` bucket value is already self-explanatory, so the badge/inherited-tag machinery in both `buildTable()` (`report.html`) and `statusBadge()` (`workspace.html`) was removed, not adapted.

**2. Raw catalog status stays visible, never contradicts.** The existing "Raw catalog status: Unknown" line (Report's expandable detail row, Workspace's Deeper Research Mode) is unchanged — it still fires whenever `rawBucket !== effectiveBucket`. What changed is only the primary badge/effective bucket it's shown alongside.

**3. Scoring: `'supported'` contributes like a resolved positive fact, tracked separately from `nativeCount`.** `computeSynthesis()`'s points formula is now `(effectiveBucket === 'native' || effectiveBucket === 'supported') ? 100 : ...` — same 100 points as native, since it's a genuinely resolved, positive compatibility fact, not "not applicable." A new `supportedCount` variable (returned from `computeSynthesis()`, used in `scoreReasons` too) is tracked deliberately separate from `nativeCount` so the explanation sentence can never claim a browser-hosted app "runs natively" — it gets its own clause instead: *"N more are browser-hosted and supported through a compatible ARM64 browser."* This is the sentence-level enforcement of "don't imply the user must use the same browser" / "don't describe it as currently running inside an ARM64 browser," not just a badge-level one.

**4. Compatibility treatment and importance treatment remain fully independent — unchanged from the prior milestone, verified still true.** `isBrowserHostedPWA(app)` alone makes an app a background *candidate* (`isBackgroundCandidate()`), same as before; `hasBackgroundOverride()` (critical_to_me, personally_verified, or real deliberate-use evidence — running/pinned/frequent/recent-UserAssist/default-handler) is still the one escape hatch, untouched by this milestone. A pinned or actively-used PWA (e.g. LinkedIn) can still land in material apps and even headline a Primary Reason with its honest "Browser-Hosted (Supported)" badge; a passively-detected PWA with no override evidence still recedes into Background Items. `isBrowserHostedPWA()`, `isBackgroundCandidate()`, `hasBackgroundOverride()`, `isBackgroundApp()`, `classifyAssessmentItem()` were not modified by this milestone — only how a `'supported'`-effective-bucket PWA is *labeled and scored* changed, never whether it's material.

**5. Bug found and fixed while wiring this up:** `report.html`'s `materialByBucket` object (populates the four primary compatibility tables from the canonical `classified` population) was initialized as `{ native: [], emulated: [], unsupported: [], unknown: [] }` — no `supported` key. Since a background-overridden PWA (e.g. a pinned LinkedIn) now has `effectiveBucket === 'supported'` and correctly reaches this material path, pushing into `materialByBucket['supported']` would have thrown (`Cannot read properties of undefined`). Fixed by adding `supported: []` to the initializer and a new `#supported-section` table (same `buildTable()` call pattern as the other four, same collapsed-by-default treatment) between Native and Emulated in the Report's app-inventory list.

**6. System Components de-duplicated from Background Items on ordinary Reports.** Separate real-machine finding: Microsoft software was correctly landing in Background Items (client-side, AppX-based `isMicrosoftAppxPackage()`) but *also* still rendering in the server-side System Components section (`results.systemComponents`, an entirely independent mechanism — see System component classification section) on the same Light/Standard Report, reading as redundant even though the two mechanisms don't literally duplicate the same row (System Components apps are removed from the native/emulated/unsupported/unknown populations server-side before they'd ever reach `classifyPopulation()`). Fixed by gating both the `#system-section` table render and its explanatory note (`system-component-note`) to `results.scan_mode === 'full'` only — Light and Standard Reports no longer show System Components at all; Advanced (full) Report is unchanged, still the place "full categorization" lives. Workspace was never a consumer of System Components at all (confirmed via grep — no `systemComponents` reference anywhere in `workspace.html`), so this gating removes nothing from Workspace, consistent with "don't remove data from the Workspace."

**Verified**: `node -c` on both `assessment.js` and the extracted inline `<script>` blocks of `report.html`/`workspace.html` passed clean. Full grep sweep confirmed zero remaining references to `findHostBrowserEntry`, `hostBrowserNameFor`, `BROWSER_HOST_APP_NAMES`, or the old `pwaInheritanceNote` name anywhere in `public/`.

**Follow-up, same day, after real-machine confirmation:** the fix above was verified correct against a real Windows machine's actual scan (running the built scanner exe against production directly) — 10 genuine Chrome-hosted PWAs (Google Calendar, TripIt, Google Maps, ChatGPT, Wyze Web Live, three Google Gemini profile variants, and two more) were all correctly classified `'supported'` and rendered "✓ Browser-Hosted (Supported)". But the fix *looked* broken in practice: those PWAs only ever appeared inside the collapsed `#supported-section` `<details>` table under "Full application inventory" — the top-of-page summary row (`.summary` in `report.html`) only ever had four cards (Native/Emulated/Unsupported/Unknown), so a real fix with no visible surface at a glance read as "still doesn't work." Fixed by adding a 5th `#supported-summary-card` between Native and Emulated, matching `materialByBucket.supported.length` (the same material-only count the table itself uses) — hidden via `display:none` when zero (the common case: most scans have no browser-hosted PWA) rather than always showing a "0" card for a compatibility category that usually doesn't apply. New `.summary-card.supported`/`.supported .summary-count` CSS in `assessment.css`, same teal as `.badge.supported`. Verified against the real captured session data (replayed through a local server) — card shows/counts correctly when PWAs are present, stays hidden when absent, no duplicate DOM ids.

**Second follow-up, same day: the publicly-downloadable scanner.exe was stale.** Testing with the actual site download (not a locally-built exe) reproduced a much worse symptom — far fewer apps found, zero browser-hosted PWAs at all, no UserAssist/default-handler console lines. Root cause: `public/ngpcx-scanner.exe` (served by the `GET /ngpcx-scanner.exe` static-file route in `server.js`) is a committed binary, not built at deploy time — it was last updated 2026-07-15 (commit `3853901`), one commit before `922cc4f` (2026-07-16) added HKCU ARP coverage, UserAssist evidence, default-handler evidence, and AppX friendly names. Since Chrome/Edge-installed PWAs register *only* under HKCU (see Scanner Enrichment milestone), the stale exe could never see them, independent of anything fixed client-side this session. Fixed by rebuilding `cargo build --release` from current `scanner/src/main.rs` (verified via a real scan against both `--local` and production before publishing) and overwriting `public/ngpcx-scanner.exe`. **This is a deploy-hygiene gap, not a one-time fix**: nothing currently rebuilds/republishes this file when `scanner/src/main.rs` changes — a future scanner-side change needs the same manual `cargo build --release` + `cp` + commit step, or the public download will silently drift stale again. Not automated this pass (out of scope); worth revisiting before signing.

**Scanner versioning, added alongside the republish:** `Cargo.toml`'s `[package] version` (bumped `0.1.0` → `0.5.0`, since pre-1.0 development) is the one canonical source, already read via `env!("CARGO_PKG_VERSION")` into `ScanPayload.scanner_version` (existing field from the Scanner Stabilization milestone, previously unpopulated-looking since nothing printed it). Now also printed to the console (`Version: v{}`) immediately before "[1/4] Collecting system information..." — console and payload can't drift apart since both read the same constant. Verified end-to-end: console shows `Version: v0.5.0`, and the session stored server-side shows `scanner_version: "0.5.0"`. Deliberately no server/DB/Report/Workspace/export changes this pass — the field already passes through untouched and unused, exactly as before. Going forward, bump this version only when `scanner/src/main.rs` itself changes, not for server- or website-only releases.

**`scanner_version` full-pipeline verification (later pass, no code changes needed):** traced and then live-tested every hop the field passes through, prompted by a question of whether it actually survives the whole assessment lifecycle rather than just the initial scan. Confirmed all seven stages already worked correctly, with real requests against a running server, not just code reading: (1) scanner emits it via `env!("CARGO_PKG_VERSION")`; (2) `scan.js`'s `POST /api/scan` parses it as `req.body.scanner_version || null` and includes it in the stored `report` object; (3) session storage persists the whole `report` object (incl. `scanner_version`) as JSON in `sessions.results` — no separate column, no separate handling needed; (4) `server.js`'s `POST /session/:id/refresh` preserves it for free, since it spreads `...previous` before `...classified` and `classifyApps()`'s return value never contains this field, so it can't be clobbered; (5) `assessment.js`'s `buildObservationSnapshot()` (export) already included `scanner_version: results.scanner_version || null`; (6) `assessment.js`'s `sanitizeExportPayload()` (import) already parsed it back out as `scannerVersion` with a `typeof === 'string'` check, defaulting to `null` for anything malformed or absent; (7) `index.html`'s import handler already re-submits it as `scanner_version` to `POST /api/scan`, closing the loop. **Backward compatibility confirmed live, not just assumed**: submitted a scan payload with no `scanner_version` field at all (simulating a pre-versioning scanner exe) — server stored `null`, export carried `null`, re-import recovered `null`, and the assessment itself round-tripped correctly with zero errors at every stage. Conclusion: the field was already fully and correctly wired end-to-end from the moment it was introduced; this pass only confirmed it, no gaps were found and no code changed.

## Results Page Presentation milestone (calmer first impression)
A presentation-only pass on `report.html`, explicitly scoped to not touch the recommendation algorithm, confidence calculation, scoring, compatibility classification, or Workspace behavior — the underlying `computeSynthesis()` math and every stored value are byte-for-byte unchanged; only how the same numbers are laid out, colored, and worded changed. Motivated by real feedback: the Readiness gauge and red Confidence bar dominated the page, while the positive evidence (Native/Supported counts) didn't appear until well below the fold — a first-time, less-technical user could come away thinking the recommendation was poor even with substantial positive evidence already present.

- **Summary cards moved directly under the header**, ahead of System Info, the explanation sentence, and Primary Reasons — concrete counts (Native/Supported/Emulated/Unsupported/Unknown/Devices) are now the first thing visible after the score, not something reached after several screens of text.
- **Confidence bar recolored, not recalculated.** `synthesis.confidenceLabel`/tier/underlying math are untouched; a new `confidenceDisplayColor()` in `report.html` maps the label to a display-only palette — High stays green, Moderate stays amber, but Limited/Very Limited Data now render calm blue (`#93c5fd`) instead of red. Red stays reserved for the genuinely blocking Unsupported card. Bar also shrunk (120px→96px, 6px→4px) to read as secondary metadata next to the gauge, not a second verdict.
- **New reassurance line and "Next step" callout**, both hidden at High Confidence (nothing to improve). The callout deliberately doesn't state the "already understood" count as a number — a raw count paired with "review N more" reads as if the two together covered every detected app, which they don't (Standard's material population already excludes background/deprioritized items — see Canonical Effective Status milestone). Wording is qualitative instead: *"Most items shaping this recommendation are already understood. Review N more to strengthen confidence."* — the exact counts stay available in the sections below.
- **Terminology**: neither new string says "Workspace" — the reassurance line reads "Refining a few items..." and the callout never names a destination, matching this page's own visible button label ("→ Refine Assessment") rather than introducing a term the button itself doesn't use. (The many pre-existing "Workspace" references elsewhere on this page were untouched — out of scope for this pass.)
- **Intro simplified via progressive disclosure, nothing removed.** The six-line intro block (methodology paragraph, scan timestamp, scan-level explanation, printer note, mode-specific notes, Ko-fi line) is now collapsed inside an "About this assessment" `<details>`, replaced above the fold by one JS-generated line (`results-tagline`, e.g. "Standard Assessment complete — here's what we found."). All the original elements (`scan-time`, `scan-level-display`, `printer-note`, and the mode-specific notes still `insertAdjacentElement`'d after `printer-note`) keep their exact IDs and positions relative to each other, just wrapped — zero JS logic changed.
- **System Info relocated a second time**, into a collapsed "Technical Details" `<details>` placed after the device table, at the very end of the report content — it had already moved once (Browser-Hosted PWA milestone, above) from directly-under-the-header to just-before-devices, but even there it read as though it belonged to the Unknown/device sections rather than being genuinely separate reference material.
- **RAM display rounding**, presentation-only: `friendlyRamGb()` snaps the scanner-observed value to the nearest common marketed size (2/4/6/8/12/16/24/32/48/64/96/128 GB) when within 1.5 GB, e.g. a real ~31 GB reading (Windows reports usable, not physically-installed, memory) now shows "32 GB". Falls back to a plain rounded integer for genuinely unusual amounts rather than forcing a nearby common size. No scanner/server change — `s.ram_gb` itself is untouched, only the display in `report.html`.

**Verified**: `node -c` on the extracted inline script passed clean. Both the Limited Data case (real captured session replayed locally) and the High Confidence case (synthetic session) checked at desktop (1280×900) and laptop (1024×700) widths — summary cards visible without scrolling in both, confidence bar/label calm blue vs. green respectively, reassurance note and Next Step callout correctly shown/hidden per confidence tier, "About this assessment" and "Technical Details" both collapse/expand correctly with all original content intact, RAM rounds 31→32 GB. No console errors in any configuration.

## Application Shell milestone (navigation architecture redesign)
A full navigation/application-shell redesign, motivated by the product's own evolution: CheckMyARM stopped being "Scanner → Report" and became "Evidence Collection → Guided Assessment → Recommendation → Community Contribution," but the navigation — a hand-written `<nav>` block duplicated with real divergence across all 13 pages — still reflected the old shape. Explicitly an application-architecture exercise, not a cosmetic refresh: a full proposal (desktop/mobile layout, adaptive-navigation matrix, platform-detection evaluation) was reviewed and refined with the user across two rounds before any code was written.

**Governing design principle, load-bearing throughout:** *the application shell communicates context; the page communicates the assessment and analysis.* The header answers "where am I, what am I working on, what can I do here" — it never renders a score, confidence tier, or readiness label. Results/Workspace still own all of that. This is a hard boundary, not a style preference — see the shell/analysis separation below.

**Two new shared files, loaded by every page against a single `<div id="app-shell"></div>` placeholder** (previously ~13 divergent hand-written `<nav>` blocks, each with its own inline CSS and its own copy of session-resolution/auth-status logic):
- **`public/nav.js`** — a self-invoking IIFE (this codebase has no bundler/module system; every page is a plain `<script>` tag, so wrapping in an IIFE is the only way to avoid leaking dozens of globals). Owns identity (`/api/auth-status`), active-session resolution, coarse platform detection, menu building/rendering, and all shell interaction wiring (dropdowns, mobile hamburger/accordion, keyboard/click-outside dismissal). Exposes `window.NavShell = {auth, session, ready, triggerImport}` and dispatches a `navshell:ready` `CustomEvent` once initial render completes, so page-specific scripts that need the same auth/session data (My Submissions' researcher-status card, admin pages' Delete-button gating) can consume it instead of re-fetching.
- **`public/shell.css`** — shell chrome only (nav bar, dropdowns, mobile panel, CTAs, avatar/role badges). Page content styling stays wherever it already lived (`assessment.css` for Results/Workspace, each page's own inline `<style>` otherwise).

**Deliberately excludes scoring/recommendation logic — a hard, not soft, boundary.** `nav.js` never imports or calls into `assessment.js`'s `computeSynthesis()` or anything downstream of it, and never renders a score/confidence/readiness label. The one place this needed real care: the subtitle beneath the logo (see below) needed *some* evidence-derived text without crossing into interpretation — resolved by having it read raw app/device counts directly off the loaded session's `results` object, never a synthesized verdict.
- **Self-contained import validation, deliberately duplicated rather than shared.** `nav.js` carries its own copy of `sanitizeExportPayload()`/`sanitizeImportedEntry()`/the observation-app/device sanitizers, byte-for-byte matching `assessment.js`'s versions. This is intentional: "Import Assessment" must work from *any* page per the navigation matrix (including Learn pages and admin pages, which never load `assessment.js` at all), so `nav.js` can't depend on that file being present without coupling script load order across every page. This is pure validation logic (allowlists, type checks) — duplicating it doesn't cross the scoring boundary the way sharing `loadPersonalContext()`/carry-forward logic would have, so those stayed exclusively in `assessment.js`, untouched.
- **`decorateInfoLinks()`** was ported from `assessment.js` into `nav.js` and now runs automatically from `init()` — no page needs to call it. It applies the same session carry-forward to any in-body `[data-info-link]` cross-link (e.g. About's own prose linking to How CheckMyARM Thinks), the body-content equivalent of what `withSession()` (below) does for menu items `nav.js` builds itself.

**Five logical modes, not a flat link list — "what am I trying to do," not a page list:**
- **Assessment** — only exists as a menu once a session is loaded (`Results`, `Refine Assessment`, `Export`, `Print`, `Run Another Scan`, `Import Another Assessment`); before that, a plain non-clickable "Assessment" label sits in its place and the equivalent actions live in the header CTAs instead (see platform-aware Scan below).
- **Community** — Research Unknown Apps / Revisit Verdicts / Community Review. **Hidden entirely below researcher rank**, not shown-but-empty — a deliberate judgment call, confirmed with the user: ordinary users have no genuine standalone Community destination today (contribution happens contextually inside Results/Workspace), and "I would rather omit an empty navigation category than create a destination merely to justify the category" was the explicit reasoning. Kept the name **Community**, not renamed to **Research**, despite the current contents being researcher-only workflows — matches ~15+ existing identifiers across this codebase (`community_submissions`, "Community trust layer," "Community Review," etc.) and better supports the longer-term model where ordinary-user contribution is more visible.
- **Learn** — Why CheckMyARM / How CheckMyARM Thinks / About. Same three destinations previously scattered across each page's own nav.
- **My Account** — Sign In (anonymous) or an avatar+name+role-badge dropdown (signed in) with My Contributions + Log out. No "My Assessments" and no assessment-history feature of any kind — explicitly not built. The user was direct about this: "Please do not create: localStorage history, saved assessments, placeholder assessment history simply to populate the Account menu... Let's expose only capabilities that genuinely exist today."
- **Administration** — Dashboard / Manage Users, Admin role only, exactly as before.

**Adaptive navigation — the full matrix** (identity × assessment state; platform only matters pre-assessment, see below):

| Identity | No assessment loaded | Assessment loaded |
|---|---|---|
| Anonymous | Assessment (static), Learn, Sign In, header Scan/Import CTAs | Assessment menu, Learn, Sign In, subtitle shows counts |
| Signed in (user) | + avatar/My Contributions/Log out | + avatar/My Contributions/Log out |
| Researcher | + Community menu | + Community menu |
| Admin | + Community menu, + Administration menu | + Community menu, + Administration menu |

**Platform-aware Scan/Import emphasis — guidance only, never capability.** Coarse Windows/Non-Windows/Unknown detection (`navigator.userAgentData.platform` with a `navigator.platform`/`userAgent` regex fallback for non-Chromium browsers) changes which header CTA is primary, never what's available:
- No assessment loaded, Windows or Unknown → **Run New Scan** primary, **Import Assessment** secondary.
- No assessment loaded, Non-Windows → **Import Assessment** primary, **Scan a Windows PC** secondary (reworded, not hidden — a Mac/Linux/iPad user may legitimately be preparing an assessment for a different Windows machine).
- Assessment loaded, any platform → **neither CTA renders in the header at all.** Both actions move inside the Assessment menu ("Run Another Scan," "Import Another Assessment") — once evidence exists, the assessment becomes the center of the application, not the scanner.
- **Deliberately deferred**: ARM-vs-x86 browser detection (Chromium-only, async, no real payoff until ARM-specific verification/optimization workflows exist — see the parked "ARM Optimization Workspace" section). Explicitly agreed with the user to implement only the coarse three-state signal.

**Browser context vs. assessment context — a standing architectural rule, not just this milestone's implementation detail.** A loaded assessment always represents the computer being evaluated; the current browser only represents the device the user happens to be interacting from right now. These are independent — viewing a Windows assessment from an iPad, or yesterday's assessment from a different Windows PC, are both normal. Once a session is loaded, it *always* takes precedence over browser/platform detection for every navigation decision; platform detection is only ever consulted pre-assessment, to decide CTA emphasis.

**Subtitle beneath the logo** (`.shell-subtitle`) — subtle, never clickable, deliberately not another dashboard: *"Start or continue an assessment"* pre-assessment, *"Current assessment based on 148 apps · 12 devices"* once loaded, using raw counts read directly off `session.results` (native+emulated+unsupported+unknown+systemComponents for apps, `devices.length` for devices) — never a score, confidence tier, or synthesized verdict. Reinforces "this is built from evidence" without leaking any interpretation into the shell.

**Unsaved-work safeguard for Import/Run New Scan when an assessment is loaded** (new, added at the user's explicit request during the final review round). `confirmReplaceIfNeeded()` checks `sessionStorage['ngpcx_session_context_' + sessionId]` — the exact key `assessment.js`'s `writeSessionContext()`/`sessionContextKey()` already write to, so it reflects real, current Personal Context state, not a separate tracking mechanism — and shows a native `confirm()` (matching this codebase's existing precedent, e.g. the Role-change safeguards' `needsConfirmation` pattern) only when there's actually something to lose. Menu wording is explicit about the transition ("Import **Another** Assessment," "Run **Another** Scan") rather than an ambiguous bare "Import."

**Admin pages: nav-link mutation replaced, content gate untouched.** Every admin page previously ran its own `loadWorkspaceNav()` (three divergent copies) that both fetched `/api/auth-status` a second time *and* manually injected extra `<a>` tags into a `#nav-links` container for Researcher/Admin-only links. `nav.js`'s own Community/Administration menus now do that entirely — `loadWorkspaceNav()` on `admin-research.html`/`admin-revisit.html` was reduced to just awaiting `navshell:ready` and reading `window.NavShell.auth.user.role` to set the pre-existing `isAdmin` flag that gates the Delete button (sequencing preserved: `loadWorkspaceNav().then(loadApps)`, so the flag is known before first render, exactly as before). `admin-community-review.html`'s equivalent now reads `window.NavShell.auth.user.id` the same way, for its self-review-block UI. **The shared-secret content-gating mechanism (`adminFetch()`/`showSecretForm()`/`localStorage['ngpcx_admin_secret']`) is completely untouched** — out of scope per CLAUDE.md's existing note that this is a deliberate, long-running migration window not to be changed without being asked.
- Admin pages keep a distinct wordmark split (`CheckMyARM` + `Admin`, vs. `CheckMy` + `ARM` everywhere else) — `nav.js`'s `logoMarkup()` branches on `window.location.pathname.startsWith('/admin-')`, preserving a pre-existing branding distinction rather than accidentally flattening it.
- The `™` mark (deliberately sparse per the Branding section — only the homepage nav logo and About's opening line) is preserved the same way: `logoMarkup()` only appends it when `pathname === '/'`.

**Real bug found and fixed during verification, not anticipated in the proposal:** `.shell-dropdown-panel` set `display: flex` unconditionally with no `[hidden]` exclusion. Since a class selector and the browser's default `[hidden] { display: none }` UA rule have identical specificity, source order decided the tie — `shell.css` loads after the UA stylesheet, so it silently won, and every dropdown rendered permanently open regardless of its `hidden` attribute (confirmed via direct DOM inspection: `panel.hidden === true` while the panel was visibly on-screen). Fixed with an explicit `.shell-dropdown-panel[hidden] { display: none; }` override — the same pattern `.shell-mobile-panel`/`.shell-accordion-body` already used correctly, just missed on this one selector.

**Differed from the reviewed proposal, in two small ways, both flagged rather than silently shipped:**
- **Export/Print delegate to already-loaded page globals rather than acting instantly from any page.** Building an export payload needs `computeSynthesis()`, which `nav.js` is forbidden from touching — so `triggerExport()`/`triggerPrint()` check for `window.exportAssessment`/a `#report` or `#workspace` root element and call it directly when present (Results/Workspace), falling back to navigating to Results first otherwise. A pragmatic simplification, not the instant one-click-from-anywhere the proposal implied, chosen over either an over-engineered cross-page action-triggering protocol or duplicating scoring logic into the shell.
- **`withSession(path, state)` helper**, not explicitly named in the reviewed proposal — added after noticing the Learn menu and "My Contributions" link needed the same session-carry-forward behavior the old `decorateInfoLinks()` provided, generalized into one small function used by every menu builder that needs it.

**Verified**: `node -c` (via extracted inline `<script>` blocks) passed clean on all 13 pages after integration. Live-tested against a running local server with a real synthetic session: dropdown open/mutual-exclusivity/click-outside/Escape dismissal, mobile hamburger + accordion expand/collapse, subtitle counts, Assessment-menu contents and hrefs (including `Export`/`Print` delegation targets existing), session propagation through `withSession()` on both the Assessment→Learn path and Learn pages' own in-body `data-info-link`s, active-page indication (`shell-mode-active`) on Results/Workspace (`assessment`) and About (`learn`), the unsaved-work confirm-and-block flow (fires when Personal Context exists, correctly absent otherwise), homepage-only `™`, admin-only `CheckMyARM`+`Admin` wordmark, and all 5 admin pages loading with zero console errors and the Community/Administration menus correctly absent for an anonymous visitor. Not live-tested: the full Researcher/Admin identity rows of the matrix (no OAuth-authenticated researcher/admin account was available in this environment) — verified instead by direct code review of `roleRank()`/`buildCommunityMenu()`/`buildAdminMenu()`, which are simple, already-precedented role-string comparisons.

## Observed Environment milestone (scanner environmental fields)
A small, carefully-scoped scanner addition following the Distribution section's "feature-complete pending signing" freeze — not a reopening of general scanner scope, a single bounded pass adding durable facts about the machine itself (not just its apps/devices) that a future recommendation layer might use. Still entirely scanner-side (`main.rs`); no scoring, UI, or recommendation-engine change of any kind.

**Governing principle, explicit and load-bearing:** the scanner observes; the assessment engine interprets. Every field added here is a raw, durable environmental fact — never a derived conclusion. Concretely rejected as out-of-scope for this milestone: no `isLaptop`, no `isGamingPC`, no `isBusinessComputer`, no "replacement likelihood" of any kind, and no vendor/model-to-purpose heuristics (a `ThinkPad`/`Legion`/`ROG`/`Latitude` name-pattern table was explicitly considered and rejected) — that kind of labeling is exactly the interpretation this project's Guidance Signal philosophy (see Observation Enrichment milestone) already draws the line at leaving to a future assessment layer, now extended from per-app signals to whole-machine ones.

**New `SystemInfo` fields, all additive:**
- `battery_present: bool` — `Win32_Battery` existence check. The single strongest available signal that a machine is portable, without labeling it "a laptop."
- `cpu_vendor: Option<String>` — raw `Win32_Processor.Manufacturer` (`"GenuineIntel"`/`"AuthenticAMD"`/etc.), a genuinely separate WMI property already available on the same object queried for the CPU name — not parsed or normalized out of the CPU name string, which would itself have been exactly the kind of derived-conclusion logic this milestone excludes.
- `discrete_gpu_present: bool` and `gpu_vendors: Vec<String>` — see "Discrete GPU: a deliberate scope deviation" below.
- `domain_joined: bool` — `Win32_ComputerSystem.PartOfDomain` only. No domain name, organization, or tenant is ever captured — a boolean is the entire signal.
- `windows_edition: Option<String>` — registry `EditionID` (`HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion`), not `Win32_OperatingSystem.Caption`, for a shorter, cleaner "Home"/"Pro"/"Enterprise"/"Education"-style value.
- `system_manufacturer`/`system_model: Option<String>` — raw `Win32_ComputerSystem.Manufacturer`/`.Model`.
- `motherboard_manufacturer`/`motherboard_product: Option<String>` — raw `Win32_BaseBoard.Manufacturer`/`.Product`, a genuinely separate WMI class query from the rest of this group.

All seven are plain observations reported into `SystemInfo`, exactly like `os_version`/`cpu`/`ram_gb`/`architecture` already were — untouched by, and untouchable by, `classifyApps()`, which has never taken `system` as an input.

**Discrete GPU: a deliberate scope deviation, documented rather than silently implemented.** Reliably attributing which specific `Win32_VideoController` entry is "the discrete one" without a vendor/product-name pattern table is genuinely hard — e.g. "AMD Radeon Graphics" (integrated APU graphics) and "AMD Radeon RX ####" (a real discrete card) share a vendor and a naming family, so a vendor-based rule can't distinguish them, and a product-name rule is exactly the per-model heuristic table this milestone was asked to avoid. Resolved as two vendor-agnostic fields instead of the single "discrete GPU vendor" originally asked for: `discrete_gpu_present` is derived purely from a COUNT of real video controllers exceeding 1 (Microsoft's own placeholder drivers — `Microsoft Basic Display Adapter`/`Microsoft Basic Render Driver` — are filtered first, the same "is this a real physical device, not a placeholder" filtering already applied to virtual `SW\`-prefix devices elsewhere in this file); `gpu_vendors` reports the full distinct set of observed `AdapterCompatibility` strings across all real controllers, plural, rather than attempting to single out one as "the discrete card's vendor." A single-GPU machine still gets a populated `gpu_vendors` (its one vendor) with `discrete_gpu_present: false` — the two fields answer different questions and are read together, not collapsed into one.

**Consolidated collection.** `get_system_info()` was refactored from two separate old-style `Get-WmiObject` calls (CPU name, RAM) plus this milestone's new fields bolted on as more separate calls, into ONE `Get-CimInstance`-based PowerShell script run through the existing `run_powershell_json()`/`json_as_items()` helper pattern (Scanner Stabilization milestone) — lower per-scan process-spawn cost and consistent with how every other collector in this file already works, not a special case. `gpu_vendors` is written with PowerShell's `@()` array-subexpression operator specifically so a single-vendor machine still serializes as a JSON array (not a bare string) through `ConvertTo-Json`; the Rust parser handles both shapes defensively anyway, matching `json_as_items()`'s existing tolerance for PowerShell's single-item output quirks.

**`payload_version` stays at 1** — explicit decision, not an oversight. Every addition is inside the existing `SystemInfo` struct; `ScanPayload`'s own top-level shape is unchanged, so this is exactly as additive as prior Guidance Signal field additions to `AppEntry`/`DeviceEntry`, none of which bumped it either.

**`scanner_version` bumped `0.5.0` → `0.6.0`** (`Cargo.toml`), per the standing convention from the Browser-Hosted PWA Compatibility milestone ("bump this version only when `scanner/src/main.rs` itself changes") — this is a real `main.rs` change, so the version moves.

**Backward-compatibility verified concretely, not assumed.** Rebuilt and ran a real scan against this machine (a real AMD desktop, not synthetic data) via the local dev server: `battery_present: false`, `discrete_gpu_present: false` (correct — the Ryzen 5 5600G's Radeon graphics is integrated APU graphics, one real controller after placeholder filtering), `gpu_vendors: ["Advanced Micro Devices, Inc."]`, `cpu_vendor: "AuthenticAMD"`, `windows_edition: "Professional"`, `domain_joined: false`, `system_manufacturer: "To Be Filled By O.E.M."` / `system_model: "A520M Pro4"` (a real, well-known artifact of motherboard-based desktop builds with no OEM chassis — not a bug), `motherboard_manufacturer: "ASRock"` / `motherboard_product: "A520M Pro4"` — all sane, all correctly passed through `scan.js` unmodified into the stored session. Confirmed `classifyApps()` output is unaffected: a pre-existing real session on this same machine (score 69, native 34 / x64-emulated 18 / unsupported 0 / unknown 96 / systemComponents 23) and the freshly re-scanned session produced identical bucket counts and score; additionally, calling `POST /api/session/:id/refresh` on the pre-existing session — re-running `classifyApps()` directly, independent of anything scanner-side — reproduced the exact same numbers a third time. `server/routes/scan.js` and `report.html`'s Technical Details section were both confirmed (by reading, not assumption) to pass through/ignore unrecognized `system` fields gracefully, so no server or UI change was needed or made.

**Not wired into scoring, UI, or recommendation logic anywhere.** This milestone is observation-only, matching the unscored-signal precedent every prior Guidance Signal addition established — these fields exist for a future assessment layer (see `WORKLOAD_ASSESSMENT_INVESTIGATION.md`) to potentially interpret, not for this pass to interpret itself.

## Adaptive Assessment Surface milestone (feature/adaptive-assessment-surface, POC — not merged)
A proof-of-concept implementation of the product's next evolution, built on a short-lived feature branch per explicit direction, not yet merged to `main`. Reframes CheckMyARM from "a scanner that produces a report" to "an advisor that happens to have a scanner" — the scan becomes one evidence source among others, not the product's front door. Still entirely additive on top of the existing scanner/catalog/`computeSynthesis()` foundation; no scanner change, no second scoring engine, no duplicated report logic.

**`public/index.html` is now the persistent Adaptive Assessment Surface**, not a scan-only landing page — the same URL (`/`) is the "front door" both before and after evidence exists, satisfying "the user should not feel like they're leaving one tool and entering another." The page never navigates away from itself for the scan-trigger flow (session creation, exe download, localhost:7878/poll handshake — all byte-for-byte the same mechanism index.html always used) — only the completion redirect target changed, from straight to `report.html` to back to `/` itself with `?session=&level=` now present, which re-triggers the same load path a direct visit with those params would.

**Two equally prominent entry paths** — "Start with a Scan" (the pre-existing scan-level radios + Run Scan button, unchanged) and "Start with a Few Simple Questions" (new: reveals a pill-based panel in place, no navigation). Both converge on the same client-side state; neither is gated behind the other. Once any evidence exists (a session, or an answered question), `.entry-paths` collapses into a compact two-button row (`.compact` CSS class) so it stops competing with the recommendation panel for attention — same actions, quieter frame, not a different flow.

**Evidence model, mapped onto the existing architecture rather than reinvented** (per explicit instruction to inspect before changing):
- **Observation** — unchanged: the scanner's raw `results.apps`/`devices`/`system`.
- **Evidence** — Guidance Signals (unchanged) plus a new sibling: **Adaptive Context**, the user's own answers to the two question groups (`intended_use[]`, `relationship`). Stored in `assessment.js`, following the *exact* existing Personal Context pattern (`loadAdaptiveContext()`/`saveAdaptiveContext()`, a pending session-less stash migrated into a session-scoped one once a real session id exists — same mechanism as `loadPersonalContext()`'s carry-forward, not a new persistence idea). Nothing here is ever sent to the server.
- **Assessment** — unchanged: `computeSynthesis()`. Adaptive Context never touches `effectiveBucketFor()` or the score formula — it is a second, independent input the recommendation layer reads *alongside* the assessment, never inside it.
- **Recommendation** — new, thin layer in `assessment.js`: `recommendationFor(synthesis)` (a 5-value taxonomy — Too Early to Tell / Poor Fit / Proceed with Caution / Probably a Good Fit / Good Fit — mapped from the existing 3-tier readiness label plus the existing confidence tier, not a new scoring model) and `nextActionFor({hasSession, adaptiveContext, synthesis})` (a modest, explicit ruleset, not a generalized next-best-action engine).

**Deliberate, load-bearing decision: the Recommendation label never leaves "Too Early to Tell" without a completed scan, no matter how many questions are answered.** Adaptive Context alone shapes the *next-action copy* ("you likely have enough for an early read") but never the recommendation badge itself — fabricating a compatibility verdict from workload category guesses alone would contradict this product's entire premise (catalog-verified evidence, not category-based guessing). This was a real design tension surfaced by the spec's own examples (a Microsoft-365-only user "may already have enough evidence for a useful early recommendation") and resolved conservatively: the *feeling* of "you may have enough" is delivered through next-action framing, the recommendation number stays honest.

**Next-action ruleset** (`nextActionFor()`), matching the four spec'd cases exactly: no evidence → offer both paths; scan-only → recommend questions (a scan cannot know intent); questions-only → branches on answers (specialized workload → recommend a scan; secondary/travel + no specialized workload → scan is optional; lightweight-only (web/365) → likely enough without scanning; otherwise → optional); both present → refine (if confidence isn't already High) or view the report.

**"Infer aggressively, commit conservatively," implemented as a strict read/write separation, not a slogan:**
- `inferWorkloadHints(results)` — a small, named keyword list matching app names already in the classified population (steam/discord → gaming; visual studio/git/docker → development; photoshop/premiere → creative; Microsoft 365/Outlook/Teams → productivity). Same "principled short list, never a sprawling per-vendor table" precedent as `isSystemComponent()`/`isMicrosoftAppxPackage()`.
- `inferRelationshipHint(results)` — `system.battery_present === false` suggests `secondary_travel`, directly exercising the Observed Environment milestone's own "reserved for a future assessment layer" framing.
- These hints are **only ever consulted for what a pill looks pre-activated as** (`effectiveIntendedUse()`/`effectiveRelationship()` in index.html, used for rendering only). `nextActionFor()`/`recommendationFor()` always read the raw, explicitly-*saved* Adaptive Context (`adaptiveContext.updated_at` gates this) — an unconfirmed hint can never silently drive the recommendation or next action. The first pill click of any kind captures the then-displayed state (hints included) as the user's real, explicit answer; before that click, nothing is persisted.

**Persistent recommendation summary and Advanced disclosure** live directly on the page, always present, evolving in place — never a separate destination. The Advanced `<details>` shows real classified counts (`materialCount`/`backgroundCount`, from the unchanged `computeSynthesis()`) and a short "Deserves Attention" preview (reusing `classifyAssessmentItem()` unmodified), plus links into the existing Workspace and Detailed Report. No app-level editing UI was rebuilt here — that stays Workspace's job, reached via a real link, not reimplemented inline.

**The detailed report is preserved, not replaced — its role changed, not its existence.** `report.html`/`workspace.html` are functionally untouched (verified via live regression: a real session's Confidence/readiness/tables all render identically to before). Two small, additive integration points:
- `report.html` gained one explicit "← Back to Assessment" link (hidden until a session id is known, then pointed at `/?session=&level=`) — a discoverability aid on top of a return path that already existed implicitly (see below).
- `public/nav.js`'s shared shell — the single place `logoHref()`/`buildAssessmentMenu()` are defined — now points the session-aware logo and a new "Assessment" menu item at the new surface (`assessHref()`), while the pre-existing "Results" menu item is relabeled "Detailed Report" and still points at `report.html` unchanged. This means the return path from *any* page in the shell (not just Results) already worked correctly before the explicit link was even added, for free, via the one shared shell file — consistent with "don't duplicate report logic."
- `nav.js`'s `triggerImport()` redirect target changed from straight into `report.html` to the new surface (`/?session=...&carryForward=1`), so imports converge on the same persistent surface exactly like a fresh scan does.

**Verified live against a real scan** (not synthetic data): ran the actual rebuilt scanner exe against the local dev server, landed on `/?session=...` exactly as designed, confirmed real recommendation output ("Proceed with Caution," "Limited Data," matching `report.html`'s own "Workable with Caveats" 3-tier label for the same session — a deliberate, expected difference in vocabulary between the two destinations, not a bug), confirmed the Advanced panel's real counts (62 material / 54 background / 10 devices) and a real "Deserves Attention" preview list, confirmed "View Detailed Report" and the new back-link round-trip correctly with session state intact, confirmed `workspace.html` is unaffected. Question-pill toggling verified interactively: selecting "Development" correctly triggered the "specialized workload, scan recommended" rule; clearing it while `secondary_travel` remained correctly fell through to the "travel companion, scan optional" rule — matching the spec's own worked examples exactly.

**Explicitly a proof of concept, not a finished feature — known limitations, stated plainly:**
- No automated test suite exists anywhere in this codebase (confirmed in the preceding investigation task) — this milestone adds real logic (`recommendationFor`/`nextActionFor`/hint inference) with zero regression protection beyond the manual verification above.
- `inferWorkloadHints()`'s keyword list is small and illustrative, not tuned against a broad real-world sample the way e.g. `isSystemComponent()`'s pattern list was refined over several milestones.
- The Advanced section's "Deserves Attention" preview is a plain top-5 slice, not weighted/sorted by the same importance-evidence ranking `report.html`'s own Primary Reasons uses — a reasonable POC simplification, not a design decision to diverge permanently.
- No new automated coverage was added for `recommendationFor()`/`nextActionFor()`'s branch logic; every case in this section was verified by hand, once, in this session.
- This branch has not been merged to `main` and nothing was deployed — per explicit instruction, this stays local until reviewed.

### Candidate Suitability addendum (same branch, follow-up pass)
A focused follow-up after brief real-browser testing of the milestone above surfaced two concrete issues: the empty-state next action silently favored the questions path over the scan path, and — more seriously — a real dead end: telling a user "you may have enough for an early read" while displaying no read at all. Still entirely within `assessment.js`/`index.html`/`report.html`; no scanner change, no second assessment engine.

**Three-state model, made explicit in code** (`assessment.js`): `determineAssessmentState({hasSession, adaptiveContext})` returns one of `ASSESSMENT_STATE_NO_READ` / `ASSESSMENT_STATE_CONTEXTUAL` / `ASSESSMENT_STATE_SCAN_BACKED`. This is the code-level expression of a permanent conceptual split this product must never collapse:
- **Candidate suitability** — a directional read about whether Windows on ARM broadly suits a described *workload*. Based on intended use, relationship to the current machine, and stated priorities. Never a claim about any specific application or device. Produced by `contextualReadFor(adaptiveContext)` alone, without touching `computeSynthesis()`, `effectiveBucketFor()`, or any compatibility bucket.
- **Compatibility assessment** — the existing, evidence-backed verdict (readiness score, recommendation label, confidence). Based on observed applications, devices, architecture, and catalog research. Only `computeSynthesis()`/`recommendationFor()` ever produce this, unchanged by this addendum.

`contextualReadFor()` is deliberately the **one** rule evaluation behind both the contextual-read panel's copy and its next-action — `nextActionFor()`'s questions-only branch now delegates to it rather than re-deriving a parallel copy of the same branching, closing off the "second disconnected recommendation engine" risk named explicitly in the request. It never produces a numeric score — `verdict` is qualitative only (`positive`/`uncertain`) and `label` is a directional phrase ("Likely a Good Candidate," "Depends on Your Setup"), never a percentage or a point on the Recommendation taxonomy's 5-value scale.

**UI never lets the two vocabularies collide.** The recommendation panel (`index.html`) gained a `.rec-eyebrow` label that always names which state is showing ("Status" / "Early Read · Based on Your Answers, Not a Scan" / "Compatibility Assessment"), and the contextual badge uses a third, distinct color (teal, `.rec-badge.contextual`) — never the gray "Too Early to Tell" or the green/amber/red compatibility palette. A contextual read shows its explanation directly (no numeric score anywhere), plus two concrete next actions ("Run a Scan →" / "Review or Add More Details →") so "you may have enough for an early read" always resolves to an actual, visible read — the dead end this addendum exists to close.

**Empty-state language fixed**: the no-evidence next action (`nextActionFor()`'s `!hasSession && !hasQuestions` branch) is now `kind: 'start-empty'` (was `'start'`, shared with the "some context but not enough" case) specifically so the UI can hide the single-CTA button entirely when no evidence of any kind exists — "Start a new assessment by choosing a starting point below," with both entry cards left equally prominent, no single button nudging toward either.

**Canonical score/label/confidence consistency, fixed at the source.** The investigation that preceded this branch found `report.html`'s animated gauge read the server's raw, unweighted `results.score` while the readiness label beside it read the correctly-weighted `computeSynthesis()` value — two different numbers rendered as if they were one. Fixed by computing `synthesis` once, immediately after `classified`, near the top of `renderReport()`, and reusing that single value for both the gauge (`animateScore(synthesis.score)`) and every downstream consumer (readiness label, confidence bar, explanation) — removing the second, later `computeSynthesis()` call that used to exist purely for those other consumers. `report.html`, `workspace.html`, and the Adaptive Assessment Surface (`index.html`) all now derive score/label/confidence exclusively from `computeSynthesis()`, called once per page render; no page holds a second, independently-computed copy of any of these values. Verified against the same real session used throughout this branch's testing: `synthesis.score` (76) now drives the gauge instead of the stale `results.score` (69); `report.html`, `workspace.html`, and the Adaptive Assessment Surface agree exactly (Confidence: "Limited Data" on all three; "Workable with Caveats"/"Proceed with Caution" — the expected 3-tier/5-tier vocabulary difference between Results and the Surface, not a bug).

**Known, accepted test-environment artifact, not a regression**: the animated gauge (`animateScore()`, `requestAnimationFrame`-driven) still renders "0" in this specific headless browser-automation pane, exactly as documented in the Results Page Presentation milestone above — confirmed unrelated to this fix by calling `animateScore(76)` manually and observing the same non-tick. The underlying `synthesis.score` value driving it is correct and was verified directly.

**Verified live**: fresh visit (neutral empty state, both cards prominent, no CTA), low-information questions (stays "Too Early to Tell," honest, offers "Answer a few questions →"), strong early-read scenario (Productivity + Web/Email + travel companion → "Likely a Good Candidate," explanation shown, two real actions, no score, no "View Detailed Report" link since no scan exists), specialized workload (Development → "Depends on Your Setup," scan recommended but not framed as mandatory), scan-only (unchanged "Compatibility Assessment" eyebrow, next action correctly asks for context), scan+questions (compatibility state takes precedence, unaffected by contextual answers, Advanced counts real), full report round-trip (Adaptive Context survives navigation via `sessionStorage`, back-link returns to the same session/level, score/label/confidence agree across all three surfaces), and a mobile-width (375px) pass confirming the two entry cards stack vertically and the empty state remains calm.

**Still a POC, scope held deliberately narrow per explicit instruction**: no generalized information-gain engine, no new confidence mathematics, no broader workload-inference tuning, no new questionnaire content, no Detailed Report redesign, no Advanced-mode rebuild. `contextualReadFor()`'s ruleset is a small, explicit, hand-verified branch set over the existing 5-option vocabulary — not a scoring model.

### Assessment Dashboard milestone (same branch, UX/interaction redesign)
A full interaction-model redesign of `public/index.html`, explicitly framed as evolving the Adaptive Assessment Surface rather than replacing it — the assessment engine (`computeSynthesis()`, `classifyPopulation()`, `contextualReadFor()`, `recommendationFor()`, `nextActionFor()`, `determineAssessmentState()`) is completely unchanged; this pass is a UX/interaction layer on top of it, not a second one. Motivated by the surface still reading as "a report followed by a workspace" rather than a living dashboard.

**Three persistent layers, each with a distinct job:**
1. **Assessment Dashboard** (`#dashboard`) — fixed shape, top of page. A Recommendation gauge and a Confidence gauge (both compact 64px circular SVGs, reusing the `stroke-dasharray`/`stroke-dashoffset` mechanic `report.html`'s own gauge already established, not a new visual language) plus a Next Step pill on one row; a row of three always-visible toggle buttons — Why / Unknowns / More Info — below it. The dashboard's *shape* never changes as evidence accumulates, only the values, colors, and helper-panel content inside it — this is the literal implementation of "gradually become more informative rather than structurally changing."
2. **Evidence Navigation** (`#evidence-nav`) — a persistent tab bar driven entirely by one array, `EVIDENCE_CATEGORIES` (`{id, label, render}`), currently Current Computer / Intended Use / Findings / Profile / Community. Adding, removing, or renaming a category is a one-entry change to that array — no markup duplicated per tab, no second registration point.
3. **Details Workspace** (`#details-workspace`) — the only region that substantially changes per selected category, rendered by calling the active category's own `render()` function. The dashboard and nav bar are never touched by a tab switch.

**Recommendation gauge is a full-fill colored ring (a status dial), not a proportional one** — a deliberate choice: the 5-value taxonomy (plus contextual-read labels) is categorical, not a continuous 0–100 scale, so a proportional fill would imply false precision. The Confidence gauge, by contrast, IS proportional (`synthesis.confidencePct`), since confidence genuinely has a percentage. Confidence renders as a muted gray, empty ring with the label "No Scan Yet" for both the no-read and contextual-read states — "avoid displaying a numeric readiness/confidence figure for a context-only result" is enforced structurally (the gauge is empty, not just visually de-emphasized) rather than by convention.

**Next Step pill is deliberately terse** — a short imperative label (`NEXT_STEP_PILL_LABELS`, e.g. "Run a Scan," "Add Intended Use," "Review Findings") rather than the previous milestone's full sentence-length next-action text. The full reasoning moved into the "Why" helper panel, reached on demand — this is the dashboard's own compactness goal ("consume little vertical space... most interaction should occur in the Details workspace") applied literally. Clicking the pill switches the active Evidence Navigation tab via `NEXT_STEP_TAB_TARGET` (a `next.kind` → tab-id map) rather than scrolling to a card, since cards no longer exist as separate page regions — `'report'` is the one exception that navigates away entirely (to `report.html`), and `'start-empty'` (the true no-evidence case) renders the pill in an inert, unstyled-as-actionable state with no click handler, preserving the prior milestone's "don't nudge toward one entry path before the user has chosen either" guarantee, now expressed as a CSS class (`.inert`) instead of hiding the control outright — the pill's fixed slot in the dashboard is never empty.

**Why / Unknowns / More Info are always visible and enabled, never hidden or disabled — content adapts instead.** This was an explicit requirement, not a nice-to-have: "if expanded before enough evidence exists, explain why rather than hiding the feature." Concretely, the no-read state's Why panel reads "There's not enough evidence yet to explain a recommendation..." rather than the button being grayed out or absent. All three panels are mutually exclusive (opening one closes the others) to keep the helper row compact — a small accordion, not three independently-growing sections.

**Current Computer became the reference interaction model**, per explicit instruction — its "compact action when there's nothing yet, richer detail once evidence exists" shape (scan-level radios + Run Scan button pre-evidence; system summary + Scan Again post-evidence) is the same shape Intended Use (pill editor, always visible once the tab is selected — no more "reveal on click" indirection, since navigating to the tab already is the reveal) and Findings (stats + attention list, present but honestly empty pre-scan) now follow.

**Scan-trigger logic re-wired, not rewritten.** The exact same session-creation/exe-download/`localhost:7878`-poll handshake from the prior milestone is preserved verbatim inside `wireScanButton()` — the only change is that it's now called every time the Current Computer tab renders (since that tab's DOM, including `#run-scan-btn`, is rebuilt via `innerHTML` on each tab switch) rather than bound once at page load to a fixed element. This is the one place tab-based rendering required a real structural change to existing logic, not just relocation.

**Profile and Community are deliberately modest**, per this codebase's standing rule against fabricating history or state that doesn't exist yet (see Application Shell milestone: "Please do not create... placeholder assessment history simply to populate the Account menu"). Profile shows real session/scan metadata and real sign-in state (via `window.NavShell.auth`, refreshed on a `navshell:ready` listener since nav.js resolves auth asynchronously and may not have finished by the time this page's own initial render runs). Community explains, honestly, that contribution today happens per-app inside the Workspace ("Share your Findings"), rather than duplicating that feature on this surface — both are real, working destinations for their stated purpose, not stubs pretending to be more.

**A light, concrete implementation of "living recommendation," per the spec's own example.** `isLikelyNonWindows()` (a small, self-contained `navigator.platform`/`navigator.userAgent` check — deliberately not shared with `nav.js`'s own `detectPlatform()`, matching that file's own documented "self-contained duplication over cross-file coupling" precedent) surfaces an inline note in the Current Computer tab when scanning genuinely doesn't apply to the device viewing the page — the tab itself is never hidden or disabled, only informative, consistent with the Why/Unknowns/More Info "explain, don't hide" principle applied to a whole evidence category rather than just a helper panel.

**Initial tab choice is computed once, from `nextActionFor()`'s kind, and never re-evaluated afterward** (`chooseInitialTab()`, called only from `loadSessionIfPresent()`'s three exit paths). This means a fresh load with an existing scan-backed session correctly opens on Intended Use (the next-recommended action), while a user's own subsequent tab clicks are always the final word — the dashboard reacting to evidence changes (gauges, pill, helper panels) never causes an unrequested tab switch, which would have violated "minimal page jumping."

**Explicitly deferred, per instruction — architecture leaves room, nothing implemented:** a "Guide me" single directed action across categories; richer dashboard instrumentation; startup/dashboard animation; additional evidence categories beyond the five (though `EVIDENCE_CATEGORIES`'s one-entry-per-category shape is exactly the hook for this); assessment export from this surface (still Results-only); expanded dashboard explanations beyond the current three helper panels.

**Verified live**, real scan + real synthetic questions data: fresh visit (dashboard shows "Too Early to Tell"/"No Scan Yet," inert pill, Current Computer tab active by default, all three helper panels open with honest empty-state copy, mutually exclusive); tab switch to Intended Use with pill selections correctly updated the dashboard in place *without* changing the active tab (gauge → "Likely a Good Candidate"/"Early Read," pill → "Scan Optional — Add Detail" — confirming reactivity doesn't cause page jumping); real scan-backed session correctly defaulted its initial tab to Intended Use (matching the computed next action) and showed "Proceed with Caution"/"Compatibility Assessment"/"Limited Data," matching the exact values verified against `report.html` and `workspace.html` for the same session in the prior milestone; Findings tab showed real counts (62/54/10) and a real, correctly-truncated attention list; Current Computer (post-scan) showed real system info and a working "Scan Again" action; the Why/Unknowns panels on a real session showed the actual `synthesis.explanation` sentence and the actual deserves-attention list respectively, with a working "See all in Findings →" cross-link; report round-trip preserved session state and produced identical values on return; Profile and Community rendered real, non-fabricated content; a 375px mobile pass showed no horizontal overflow; zero console errors across the entire pass.

### Dashboard Instruments addendum (same branch, focused refinement pass)
A follow-up pass after brief real-browser use of the milestone above, refining the dashboard's visual/interaction model without touching the three-layer structure, the assessment engine, or `computeSynthesis()`. Four changes: gauges redrawn as fixed automotive-style instruments, two label renames, a new Dashboard Confidence model, and removal of the Profile tab.

**Gauges are now a fixed ~240° sweep, not a full-circle progress ring.** `gaugeSvg()` (`index.html`) draws two `<polyline>`s (track + fill) along an explicit, hand-computed set of points from `GAUGE_START_ANGLE = 150°` through a `GAUGE_SWEEP = 240°` arc (ending at 390°/30°) — a classic dashboard-gauge shape opening at the bottom, low/resting position at the lower-left, filling clockwise through the top to the lower-right. Deliberately built from point-by-point polylines rather than SVG's `<path>` arc command, to avoid that command's large-arc/sweep-flag ambiguity for a >180° sweep — more verbose, but unambiguous and easy to verify by inspection (confirmed by direct pixel-dimension checks against a running page, not just code review). `GAUGE_R`/`GAUGE_STROKE`/`GAUGE_PAD` are fixed constants never derived from the value being displayed, so the SVG always renders at exactly 78×61px regardless of the reading — the housing genuinely never resizes.

**Fixed-height text areas prevent the dashboard from growing.** `.gauge-block` is now a fixed 108×128px box; `.gauge-label-area` reserves 2.5rem regardless of whether the label is one line ("Too Early to Tell") or wraps to two ("Likely a Good Candidate"); `.gauge-label-sub` reserves its own 0.85rem line even when empty, so its later appearance (the contextual-read qualifier, see below) never shifts anything. `.next-step-pill` is a fixed 210×128px box with a `min-height` on the button itself. Verified directly: dashboard height measured identically (222.375px) across the empty state, a contextual-read state with a longer wrapped label, and a real scan-backed state — confirmed via `getBoundingClientRect()`, not visual inspection alone.

**The Recommendation gauge's instrument title is now fixed ("ARM Suitability"), matching how the Confidence gauge's title ("Confidence") already behaved — it never changed per state even before this pass.** Previously the eyebrow text switched between "Status"/"Compatibility Assessment"/"Early Read · Based on Your Answers, Not a Scan" depending on assessment state; per this pass's explicit instruction ("dashboard labels should describe the assessment state, not the mechanism used to gather evidence"), that dynamic eyebrow was replaced with a permanent title, consistent with a real dashboard instrument always being labeled "Speed" or "RPM" regardless of the current reading. **The contextual-vs-compatibility distinction this eyebrow used to carry didn't disappear — it moved to `.gauge-label-sub`**, a small fixed-height line beneath the main label that reads "Early Read" (teal) only in the contextual state, empty otherwise. This preserves the safety property established two milestones ago (a context-only read must never be mistaken for a scan-backed compatibility verdict) without reintroducing a state-dependent instrument title. Confidence gauge's initial-state label changed from "No Scan Yet" to "No Data Yet," for the same reason — it no longer names the mechanism (a scan), since Confidence can now be non-zero without one.

**Dashboard Confidence is a new, deliberately different metric from `synthesis.confidencePct`** (`dashboardConfidencePct()`, `assessment.js`) — the latter is unchanged and still drives Results/Workspace exactly as before. The new function blends evidence from *any* source:
```
DASHBOARD_CONFIDENCE_SCAN_WEIGHT = 70          // max points from a completed scan, scaled by synthesis.confidencePct
DASHBOARD_CONFIDENCE_INTENDED_USE_WEIGHT = 20  // flat credit once intended_use.length > 0
DASHBOARD_CONFIDENCE_RELATIONSHIP_WEIGHT = 10  // flat credit once relationship is set
```
A scan alone can reach at most 70; Intended Use alone (workload + relationship) can reach at most 30; both together can reach 100. This is an explicit, first-pass model — not claimed to be statistically principled, only monotonic (independent evidence never lowers the number) and honest (a scan stays the largest single contributor, matching how much more it actually reveals about real compatibility than a stated intent can). A 5-tier label (`dashboardConfidenceLabel()`: No Data Yet / Limited Evidence / Building Evidence / Good Evidence / Strong Evidence) uses deliberately different wording from Results/Workspace's "Limited Data/Moderate Confidence/High Confidence" tiers, as a second, redundant signal that these are different metrics, should the two ever be read side by side. **Suitability and Confidence are computed independently** — `recommendationFor()`/`contextualReadFor()` never feed into `dashboardConfidencePct()`, and vice versa — so toggling an Intended Use pill can raise Confidence while ARM Suitability stays whatever it already was (or changes independently, e.g. a specialized workload pushing Suitability toward "Depends on Your Setup" while still raising Confidence, since answering *is* evidence regardless of what it reveals).

**Verified directly, in a fresh browser tab** (to rule out sessionStorage carryover from earlier testing sessions, which briefly produced confusing results during this pass — see below): a real scan-backed session with no Intended Use answered showed Dashboard Confidence at 34% ("Building Evidence") — `synthesis.confidencePct` was 48 for this session, and 0.48×70=33.6→34, confirming the scan-only ceiling and scaling both work as designed. Adding two Intended Use answers (a workload pill + a relationship pill) raised it to 64% ("Good Evidence") — 34+20+10=64 — confirming Intended Use answers move Confidence independently of the scan, and that the total correctly approaches (but, with only these three evidence sources, does not yet reach) 100.

**Real behavior discovered during this verification, not a bug — worth recording so it isn't rediscovered as one:** clicking a pill that's already showing as hint-*suggested* (see the Adaptive Assessment Surface milestone's "infer aggressively, commit conservatively") toggles it **off**, not on — because the click handler reads the currently-*effective* (hint-merged) list and removes the clicked value if it's already present in that effective list, exactly matching ordinary toggle-button semantics from the user's point of view (a pill that looks active, when clicked, becomes inactive). A test in this pass briefly misread this as a bug before tracing it — the hint/toggle design from the earlier milestone was already correct.

**Profile tab removed.** It had been built as logged-in-identity/account information ("Who this assessment belongs to..."), which was not its intended meaning (a usage/context profile) and substantially overlapped with Intended Use once correctly scoped. Removed from `EVIDENCE_CATEGORIES` (now `Current Computer` / `Intended Use` / `Findings` / `Community`), `renderProfileTab()` deleted outright rather than stubbed, and the `navshell:ready` listener that existed solely to refresh a stale sign-in state on that tab removed as dead code. No replacement filler content was added, per explicit instruction — the `EVIDENCE_CATEGORIES` array shape is unchanged and remains the hook for a future, separately-earned Profile/context category if one is designed later.

**Known limitation, stated plainly:** the confidence weights (70/20/10) are a sensible first model, explicitly not tuned against real usage — CLAUDE.md and the code comments both flag this so the numbers aren't mistaken for a considered final answer. A scan that itself found very little (`synthesis.confidencePct` near 0) still contributes almost nothing to Dashboard Confidence even though a scan happened at all — this is a deliberate choice (an empty scan is not real evidence) but means a very sparse scan and no scan at all can look similar on this gauge; revisit if that reads as confusing in practice.

### Evidence Model addendum (same branch, architectural seam pass)
A pass focused on making Dashboard Confidence easier to evolve, without building a generalized reasoning engine and without changing its effective numbers. Same weights as before (scan up to 70, Intended Use workload +20, relationship +10) — the difference is architectural, not numerical.

**Confidence is now computed from an explicit evidence-source array, not ad-hoc arithmetic.** `evidenceSourcesFor({hasSession, synthesis, adaptiveContext, scanUnavailable, findingsAttentionCount})` (`assessment.js`) returns one object per evidence source — `id`, `label`, `availability` (`'available'|'unavailable'`), `presence` (`'absent'|'partial'|'present'`), `contribution`, `maxContribution`, `quality` (0–1 or `null`), `explanation`. `dashboardConfidencePct()` is now a thin reducer summing `contribution` across this array — the numeric behavior is unchanged from the prior pass, only its expression. This is the seam: a future source (or a reweighted existing one) is a change to `evidenceSourcesFor()` alone, never to the summation or to any UI call site.

**Sufficiency, not completeness — the load-bearing principle this pass exists to protect:** Confidence represents whether the available evidence is sufficient to support the current recommendation. It does not imply that no additional evidence could improve the assessment. Reaching 100% means every source this model currently accounts for is fully present — a statement about the sources this version knows how to weigh, not a claim of exhaustiveness. This is why `evidenceSourcesFor()` deliberately still returns entries for Community and Findings even though both currently contribute zero weight — a real, visibly-absent-or-present source that simply isn't counted yet, kept visible specifically so 100% Confidence can never read as "nothing more could be learned." Documented in the function's own doc comment in `assessment.js`, not just here, so it can't be silently dropped in a future edit.

**Evidence Summary — a new, compact UI surface answering "why is Confidence at this level," reusing the same array the gauge sums** (`evidenceSummaryHtml()`, inside the "More Info" helper panel). Four rows — Current Computer / Intended Use / Community / Findings — each a short qualitative phrase (`Not examined`, `Scanned`, `Scanned — limited result`, `Not described`, `Partially described`, `Described`, `No relevant evidence yet`, `None`, `Some require attention`) derived from `presence`/`quality`, never a raw point value. Intended Use's two internal sources (workload, relationship) are merged into one display row (`0/1/2 answered` → `Not described`/`Partially described`/`Described`) since the task's own worked example treats them as one row; the underlying array still tracks them separately for weighting.

**Scan Confidence Semantics — investigated and settled, no code correction needed.** `synthesis.confidencePct` (the *existing*, unchanged, scan-only metric that still drives Results/Workspace) was inspected directly against real code, not assumed: it equals the weighted fraction of the material app population whose `effectiveBucket` is anything other than `'unknown'` — i.e., **classification/resolution coverage**, never compatibility outcome. Proven concretely, not just read from the formula: a synthetic population of 10 apps all classified `native` and a second population of 10 apps all classified `unsupported` produced **identical** `confidencePct` (100) and `confidenceLabel` ("High Confidence") despite opposite `score` (100 vs -20) and opposite `readiness.label` ("Good Fit" vs "Not Recommended Yet"). A completed scan that produces an unfavorable result is therefore already, correctly, treated as strong evidence by the existing metric — using it to scale the scan evidence-source's `quality`/`contribution` in the new model does not import a compatibility-outcome bias. **No rewrite, and no smallest-safe-correction, was needed** — the metric was already the right shape for this purpose; this section exists purely so the investigation and its proof don't need to be redone.

**Findings tab — assessed, not changed.** Traced `renderFindingsTab()`'s data dependencies directly: every number and list item it shows (`materialCount`, `backgroundCount`, device total, the deserves-attention list) comes from `classifyPopulation()`/`computeSynthesis()` applied to `currentResults` — the exact same scan data Current Computer's own tab already reports on. Findings collects nothing independently; it is a derived **view** over Current Computer's evidence (a triage/interpretation lens), not a second evidence source. The evidence model reflects this directly — `evidenceSourcesFor()`'s `'findings'` entry has `maxContribution: 0` and is explicitly commented as non-contributing to avoid double-counting evidence already reflected in the scan entry. **Recommendation: leave it as a peer tab for now, unchanged.** It serves a genuinely distinct *user-facing* purpose (triage — "what should I look at next" — versus Current Computer's "what do we know about this machine"), which is real UX value independent of whether it collects evidence, and demoting it to a subview or visually de-emphasizing it would be a structural change this pass was explicitly scoped not to make ("do not redesign the architecture"). Worth revisiting once/if the Evidence Navigation bar grows enough that the evidence-source/derived-view distinction needs to be visible in the UI itself, not just in code comments.

**Visual review performed against the fixed dashboard from the prior pass** (no evidence, one Intended Use selection, both selections, scan only, scan + Intended Use, 375px mobile, long recommendation text, long next-step text) — no defects found beyond what the prior pass already fixed; dashboard height, gauge position, and label wrapping all held steady with the new (longer) Evidence Summary content added to the More Info panel. No architecture changes were made as a result — this review confirmed the existing fixed-dimension design absorbs the new content correctly rather than requiring new fixes.

**Verified live, fresh browser session:** Intended Use answers moved Dashboard Confidence independently of ARM Suitability (confirmed via direct console inspection of `evidenceSourcesFor()`'s output, not just the rendered label); a scan-only session and a scan-plus-Intended-Use session combined contributions additively and matched the Evidence Summary's own qualitative rows exactly (e.g., Confidence "Building Evidence" paired with Current Computer "Scanned — limited result" / Intended Use "Not described"); dashboard dimensions and gauge geometry unchanged from the prior pass's fixed-size verification; no tab jumped during any reactive update; the existing scan flow and report round-trip were re-exercised and produced identical results to prior passes; mobile (375px) showed no overflow; zero console errors throughout.

**Explicitly not attempted, per scope:** no generalized rules/inference engine: `evidenceSourcesFor()` is a fixed, hand-written function enumerating exactly the sources this pass knows about, not a registry or plugin system. No reweighting of the 70/20/10 model. No new evidence sources actually collecting data (Community still has no collection mechanism; this pass only makes its *absence* visible). No architectural change to Findings, per the recommendation above.

### Instrument Panel addendum (same branch, presentation/interaction refinement pass)
A UX-refinement pass leaning into the dashboard's identity as an instrument panel — a calm, information-rich control panel answering "where do I stand / what is happening / what should I do next." Explicitly not a redesign: `evidenceSourcesFor()`, `computeSynthesis()`, `recommendationFor()`, `nextActionFor()`, `contextualReadFor()`, `determineAssessmentState()`, and the nav architecture are all byte-for-byte unchanged. Everything below is presentation and interaction only, in `public/index.html`.

**1. Gauge visual hierarchy.** Instrument title now sits above the gauge (`.gauge-eyebrow` moved before `.gauge-svg-wrap` in the DOM, margin flipped to `margin-bottom`), matching a real gauge's engraved name; the reading stays below, unchanged in position. Confidence gets a numeric readout alongside its label (`"Good Evidence — 64%"`, via a new `.gauge-pct` span) — ARM Suitability stays label-only, deliberately: it's a 5-value categorical taxonomy, not a 0-100 scale, and a number there would imply false precision. `gaugeSvg()` gained tick marks (`gaugeTicksSvg()` — 5 short radial hairlines at 0/25/50/75/100% of the 240° sweep, purely decorative, giving the "instrument, not donut" read) and a needle (`gaugeNeedleSvg()` — a center hub + radial pointer). The function signature is now `gaugeSvg(fillPercent, color, needlePercent)`: `fillPercent` still drives the colored arc exactly as before (a full 100 for the categorical Suitability gauge, a genuine 0-100 for Confidence); `needlePercent` is a new, independent third parameter, since a categorical gauge's "how full is the ring" and "where does the pointer sit" are different questions. ARM Suitability's needle sits at one of the same 5 tick positions the ticks themselves mark (`RECOMMENDATION_TIER_ORDER`, index/4×100) — for the contextual (context-only, no scan) state, which carries only two verdicts (`positive`/`uncertain`, see `contextualReadFor()`), the needle is placed at the same tier position a scan-backed read of matching sentiment would use (`Probably a Good Fit` / `Proceed with Caution`), never derived from a lookup against `recLabel` directly — an earlier draft did that and it broke, mapping every contextual label to needle position 0 since contextual labels ("Likely a Good Candidate," "Depends on Your Setup") aren't members of the 5-tier vocabulary at all. Confidence's needle is a genuine, continuous 0-100% position, identical to its fill percent.

**2. What Next? panel.** Replaces the old compact Next Step pill. Still reuses `nextActionFor()`'s existing `{kind, text}` output verbatim — this file only turns it into buttons, it never re-derives the recommendation. Structure: a fixed "WHAT NEXT?" label, the blurb (`next.text`, previously computed but never actually shown to the user — only a short pill label was), and 1-2 action buttons. `WHAT_NEXT_PRIMARY_LABEL`/`WHAT_NEXT_PRIMARY_TARGET`/`WHAT_NEXT_SECONDARY` map each `kind` to real, already-existing destinations only (an Evidence Navigation tab, or the Report/Workspace links) — no new page or feature was invented to populate a second button; some kinds render with only one. The true empty state (`'start-empty'`, no evidence of any kind yet) is handled separately: two equal-weight buttons ("Start Scan" / "Answer Questions"), matching this surface's original "two equally prominent entry paths" design rather than a primary/secondary pair — there is deliberately no single recommended action when no evidence exists at all. `chooseInitialTab()` (computed once on load, never re-evaluated — unchanged precedent) now reads its kind→tab mapping from `WHAT_NEXT_PRIMARY_TARGET` instead of the deleted `NEXT_STEP_TAB_TARGET`, preserving the exact prior fallback behavior (`'report'` has no tab entry, so it still falls back to `'current-computer'`).

**3. Scanner Activity strip — prototype-level, not a final implementation.** A slim, always-present instrument row beneath the What Next panel reporting what the localhost:7878 handshake is doing: Ready → Downloading scanner → Waiting for scanner → Connected → Receiving results → Assessment updated (`SCANNER_STATE_META`, `setScannerActivity()`). The real difficulty this section exists to solve: a real scan spans a full page navigation (this page creates a session, triggers a download, polls 7878, then on success redirects to itself with `?session=&level=`, which resets all JS state) — without help, "Connected"/"Receiving Results" would never be visible, since the page reloads out from under them the instant they'd fire. Fixed with a one-shot `sessionStorage` bridge flag (`SCANNER_ACTIVITY_BRIDGE_KEY`, same pattern `CARRY_FORWARD_KEY` already uses elsewhere in this codebase): set right before the redirect, consumed at the very top of the next `loadSessionIfPresent()` call, gating whether "Receiving Results → Assessment Updated" plays on the fresh load (an ordinary revisit or import never sets this flag, so it never fires for stale, non-fresh results). "Assessment Updated" auto-settles back to "Ready" after 3 seconds. **Explicitly not attempted**: no attempt to slow down or artificially stage the real handshake to make each state linger longer for visual effect — the states show what's actually happening, for exactly as long as it's actually happening, even if that's briefly.

**4. Compact evidence-status meters.** The existing Evidence Summary (inside the More Info panel) gained a small horizontal meter per row, built strictly from the same `evidenceSourcesFor()` fields the qualitative phrase and the Confidence gauge already read (`contribution`/`maxContribution`/`presence`) — `evidenceMeterPct()` computes `contribution/maxContribution×100` for sources with real scoring weight (Current Computer; Intended Use as the combined workload+relationship pair), and falls back to a plain presence-based binary (0% or 100%) for sources with no scoring weight yet (Community, Findings) rather than implying a percentage that doesn't exist. This is presentation over existing data, not a new evidence dimension — no `evidenceSourcesFor()` change was needed or made. A "Manual" source (mentioned as a future example in the request) was not added: no such evidence source exists anywhere in this codebase today, and adding an indicator for it would be exactly the kind of fabricated-placeholder content this project's standing convention already rules out (see Application Shell milestone: "expose only capabilities that genuinely exist today").

**Two small defects found and fixed during visual review, not anticipated in the design:**
- The What Next buttons are `<a>` elements (so a primary "View Detailed Report" button can be a plain link rather than needing a JS click handler) but had no `text-decoration: none`, so they rendered with a default browser underline. Fixed in `.what-next-btn`.
- The recommendation gauge's label area could overflow by ~1px in the real (not just synthetic-stress-test) contextual-read state, where the label wraps to 2 lines ("Likely a Good / Candidate") AND the "Early Read" qualifier renders beneath it — the fixed-height box sized for the *general* stress-tested case didn't quite cover this specific real combination. Fixed by bumping `.gauge-label-area` from `2.5rem` to `2.75rem` and `.gauge-block` from `132px` to `136px` to match; reverified with `scrollHeight === clientHeight` (no overflow) directly.

**Verified live**: no-evidence state (both dashboard buttons equal weight, "Ready" scanner strip, all evidence meters empty); one and both Intended Use selections (needle/color/sub-label update correctly for the contextual "Likely a Good Candidate" state, Confidence meter and gauge move together, no gauge-label overflow after the fix); a real scan-backed session (needle correctly at the "Proceed with Caution" tier position, Confidence "Building Evidence — 34%" matching the exact value from the prior pass's own validation, What Next panel showing "Add Intended Use" / "View Detailed Report"); the six Scanner Activity states directly exercised via `setScannerActivity()` (each renders the correct label/color/pulse — see the known limitation below for why this, not a live click, was the verification method); mobile (375px, no horizontal overflow, buttons and tabs wrap cleanly); a synthetic long-text stress test on the What Next blurb (grows past its `min-height` cleanly, no clipping); the report round-trip (Results loads with zero console errors, "← Back to Assessment" returns to the same dashboard state); zero console errors in every state tested.

**Known limitation, stated plainly:** this browser-automation environment's click delivery proved unreliable for the "Run Scan" button specifically during this pass (two attempts landed on the wrong element at stale coordinates rather than the intended button — confirmed via network-request inspection showing no `POST /api/session` fired). Rather than fight the tooling further, the Scanner Activity *rendering* was verified directly (calling `setScannerActivity()` for all six states and confirming correct DOM output), and the *wiring* (that `wireScanButton()`/`loadSessionIfPresent()` call the right state at the right point in the real flow) was verified by direct code reading rather than a live click-through. The real end-to-end sequence — click Run Scan, watch Downloading → Waiting → (real scanner.exe runs) → Connected → Receiving → Updated — was not observed firsthand in this pass and should be re-checked with a real scan on Windows before relying on it.

### Coherence & Trust addendum (same branch, "earn the recommendation" pass)
A refinement pass explicitly about making the dashboard *feel* coherent and trustworthy rather than adding capability. `evidenceSourcesFor()`, `computeSynthesis()`, `recommendationFor()`, `nextActionFor()`, `contextualReadFor()`, `determineAssessmentState()`, and the nav architecture remain byte-for-byte unchanged — every change below is presentation logic in `public/index.html`, confirmed by re-reading the diff scope before committing.

**1. Recommendation Eligible gate — the advisor now earns the right to recommend.** `isSuitabilityEligible(state, evidenceSources)` decides whether the ARM Suitability gauge may show a real reading at all. Deliberately reuses thresholds this codebase already holds evidence to elsewhere, rather than inventing a new one:
- Scan-backed: `scan.quality >= SCAN_QUALITY_SUFFICIENT_THRESHOLD` (0.5) — the *exact* split `evidenceSummaryHtml()` already used to choose between "Scanned" and "Scanned — limited result." One named constant now backs both call sites (previously the same `0.5` literal lived twice).
- Contextual (no scan): both Intended Use questions answered — the exact condition that already produces "Described" rather than "Partially described" in the same Evidence Summary.
- Ineligible states render a "?" glyph in place of the needle (`gaugeSvg()` gained an optional fourth `glyph` param — when set, fill is forced to 0 and a centered SVG `<text>` replaces `gaugeNeedleSvg()`) and one of two honest labels: "Too Early to Tell" (zero evidence, `ASSESSMENT_STATE_NO_READ`) or "Need More Evidence" (real evidence exists, just not enough yet).
- **Real-data validation, not just synthetic:** the local test DB's own real captured session (`f808d771-…`, `synthesis.confidencePct: 48`) sat *just under* the 0.5 gate — confirmed live that a real, decent scan (`score: 76`) still correctly shows "Need More Evidence" rather than a premature "Proceed with Caution," while Confidence still honestly reflects the 48%-classified scan plus any Intended Use answers. This is the concrete case the gate exists for.

**2. Gauges: tighter spacing, a real value line, continuous movement once eligible.** `.gauge-label-area` height trimmed (2.75rem → 2.2rem) and a new `.gauge-value-area` line added beneath it, reserved even when empty so ineligible/no-number states never shift anything. Confidence's numeric readout moved off the inline `<span>` it briefly had in the prior pass onto this dedicated centered line. ARM Suitability gains a real number too, but *only* once eligible and *only* for the scan-backed case: `synthesis.score` (the same score Results/Workspace already show, clamped only for display — the underlying value is untouched) drives both the value line and, replacing the discrete 5-tier needle stop, a continuous needle position. The label itself stays categorical (5 tiers) — "gradual movement as evidence improves" is expressed by the needle and number moving continuously *beneath* a stable categorical label, not by inventing a second scoring model. The contextual (no-scan) eligible case deliberately shows *no* number — there is no real continuous score behind a context-only read, and fabricating one would be exactly the "pretend to know more than it does" this pass's design philosophy rules out; the qualifier tag ("Early Read") is the only annotation there, unchanged.

**3. Evidence Summary: blockier bars, illustrative markers.** `.evidence-meter-track` grew from a 4px hairline to a 9px blocky level-meter (`border-radius: 3px`, matching a meter more than a progress pill), with two subtle reference lines per row (`EVIDENCE_MARKER_MIN_USEFUL_PCT = 30`, `EVIDENCE_MARKER_STRONG_PCT = 70`) — explicitly illustrative, not literal per-source thresholds, and deliberately *not* the same numbers as the aggregate Confidence tier boundaries (which operate on the combined dashboard total, a different scale). Fill width still comes only from `evidenceMeterPct()`, unchanged from the prior pass. Added `transition: width 0.4s ease` on the fill so a value change animates rather than snapping — cheap, real feedback.

**4. Scanner becomes a permanent instrument, sharing What Next's visual language.** The old single-line "scanner-activity" strip is gone; a new `.info-display` card class is shared by both What Next and a new Scanner card, laid out side by side in `.dashboard-displays` (stacked on mobile). `SCANNER_STATE_META` split each state's text into a short `value` (the big centered readout — "Ready," "Downloading," "Waiting," "Connected," "Receiving," "Updated") and a smaller `caption` (the fuller sentence), matching the value+caption shape already established for gauges this pass. Still explicitly prototype-level, per the prior pass's own caveat — unchanged.

**5. What Next: boxed like an instrument, monospace, one sentence.** Reuses `.info-display` card chrome instead of a bare `border-top` divider. The blurb now renders in a monospace font stack (`ui-monospace, SF Mono, Menlo, Consolas, monospace`) — a small, deliberate choice to read as a live generated readout rather than ordinary page prose. `firstSentence(text)` trims `nextActionFor()`'s existing `next.text` to its first sentence for display — a presentation-only trim (the underlying string in `assessment.js` is untouched); only the two-clause kinds (`optional-scan`, `none-needed`) actually lose anything, and what they lose ("...or review your answers below") is already redundant with the action buttons rendered directly beneath.

**6. Feedback loop — investigated, and partly a real bug fix.** The reported symptom ("changing Intended Use has little effect on Suitability") is **expected behavior on a scan-backed session** — `recommendationFor()` has always been, deliberately, scan-only (documented since the Adaptive Assessment Surface milestone: "fabricating a compatibility verdict from workload guesses alone would contradict the entire premise of this product"). That boundary is correct and was not changed. What *was* missing was visible confirmation that the interaction still did something: `lastConfPct` (module-level, reset only across renders) now drives a brief `.gauge-block.pulse` highlight on the Confidence gauge whenever its value genuinely changes, skipped on the very first render so page load never pulses. Combined with item 1's gate, there's also a genuine *new* case where Intended Use answers now visibly move Suitability even on a contextual-only session: going from one answered question to both flips the gate from "Need More Evidence" to a real reading — confirmed live in this pass (see item 1).

**7. Why? affordance — reuses the existing Why panel and How CheckMyARM Thinks, duplicates nothing.** `eligibilityGateExplanation(state, evidenceSources, synthesis)` is a small, template-filled (never free-form) explanation of exactly why the gate hasn't opened — reused in two places: the gauge's own "Why?" link (rendered in the same fixed `.gauge-label-sub` slot the "Early Read" qualifier tag uses when eligible — the two are mutually exclusive, so one reserved line covers both) programmatically clicks the existing "Why" helper-toggle button rather than duplicating its open/close logic; and `renderHelperPanels()`'s Why panel content itself, which now branches on the *same* `eligible` flag the gauge uses and shows the gate explanation (plus a link to `/how-it-thinks.html`) instead of the real `synthesis.explanation`/`contextualReadFor().explanation` whenever the gate hasn't opened. **A real bug this surfaced and fixed, not anticipated in the brief:** before this pass, the Why panel always showed the *real* underlying explanation regardless of whether the gauge was displaying it — meaning a user could open "Why" on a "Need More Evidence" gauge and read the exact compatibility verdict the gauge was supposed to be withholding. Gating both surfaces on the identical `eligible` value closes that gap structurally, not by convention.

**One real defect found during mobile visual review, not a synthetic stress case:** `.info-display` used `flex: 1 1 0` (a flex-basis-0 equal-division trick, correct for the desktop row layout) — but in the mobile stacked (column-direction) layout, that same flex-basis interacted with the container's own content-driven height to clamp each card at its `min-height` (128px) even when wrapped text and two stacked buttons needed ~152px, silently overflowing the button row outside the card's visible border. Confirmed via `getBoundingClientRect()` before and after: fixed by overriding `.info-display { flex: 1 1 auto; }` inside the existing `@media (max-width: 640px)` block, letting each card's height follow its own content while `flex-grow: 1` still fills any remaining space.

**Verified live** (fresh browser session, desktop 1280px and mobile 375px): no-evidence state ("?" + "Too Early to Tell", both empty-state buttons, empty Scanner card); one Intended Use answer (confidence moved to 20%, gauge correctly gated to "Need More Evidence" + working "Why?" link opening the correct gate explanation, verified the underlying real explanation is *not* leaked); both Intended Use answers (gate opens, "Likely a Good Candidate" / "Early Read" appears, confidence continues to climb to 30%); a real scan-backed session sitting just under the gate (confirmed "Need More Evidence" despite a real `score: 76`, matching the gate's own documented purpose); the eligible scan-backed path exercised via a stubbed `computeCurrentSynthesis()` (since no local session currently clears 0.5) — confirmed "Good Fit" label with a real "76" value line and correctly colored needle; evidence meter rows confirmed rendering correct fill percentages and marker positions (30%/70%) via direct DOM inspection; mobile layout confirmed overflow-free after the fix, with both cards visually boxed and non-overlapping; report round-trip (Results loads with zero console errors, "← Back to Assessment" link intact); zero console errors throughout.

**Explicitly rejected, with reasons:**
- A brand-new numeric eligibility threshold (e.g. "Confidence must reach 40%") — rejected per the brief's own instruction not to hardcode an arbitrary percentage when a better architectural solution exists; the reused scan-quality/both-answered signals were that better solution.
- A continuous number for the contextual (no-scan) Suitability reading — rejected as fabricating precision that doesn't exist; the design philosophy this pass names explicitly ("never pretend to know more than it does") argues directly against it.
- Editing `nextActionFor()`'s underlying strings to be shorter — rejected in favor of a presentation-only `firstSentence()` trim, preserving the full text for any future consumer and keeping `assessment.js` untouched.
- A shared literal CSS class (rather than matching chrome values) between What Next and Scanner — considered, but the two cards need different internal alignment (top-aligned label+blurb+buttons vs. vertically centered status+caption), so `.info-display` covers shared chrome only, with each card free to lay out its own content.

**What surprised us, worth recording:** the real captured session's `confidencePct: 48` landing *just* under the 0.5 gate wasn't planned — it was a coincidence of already-existing test data — but it turned out to be the single most convincing piece of validation in this pass: a real, decent-scoring scan (`score: 76`) correctly held back from a premature recommendation, which is exactly the scenario the entire gate exists to prevent, observed on real data rather than a constructed edge case.

**Does the dashboard feel like a coherent instrument panel now, rather than a traditional report?** Closer than at any prior point in this branch. The three questions this pass was framed around — where do I stand, what evidence do we have, what should I do next — now each have one dedicated, honestly-gated instrument: Suitability (which can now say "I don't know yet" instead of always answering), Confidence (a value that visibly moves and pulses when it does), and What Next (a boxed, monospace, machine-feeling readout sitting beside a matching Scanner status card). The remaining gap toward "fully coherent": the What Next and Scanner cards' *internal* content shapes still differ (one holds a sentence + buttons, the other a value + caption) even though their outer chrome now matches — a deliberate choice this pass (see "explicitly rejected" above), but worth watching if a future pass wants them to feel even more like one family.

### Composition Correction addendum (same branch, "meters exist but aren't visible" fix)
A focused correction, explicitly not a redesign, after real use found the previous pass's evidence meters genuinely invisible on the rendered page. The evidence model, gate logic, and all `assessment.js` functions from the prior two passes are untouched.

**Root cause, confirmed by reading the code, not guessed at:** `evidenceSummaryHtml()`/`evidenceMeterRow()` were never broken — they always produced correct markup with correct fill percentages. The bug was placement: the only call site that ever inserted that markup into the DOM was inside `renderHelperPanels()`'s `moreInfoBody`, which populates the "More Info" `helper-panel` — an element with `hidden` set by default, only shown after a user clicks the "More Info" toggle. The meters were real, functional, and 100% hidden behind a dropdown from first paint. Confirmed via `git grep`/direct reading before writing a single line of fix — no time spent guessing.

**The fix: a three-column instrument row replacing the previous two-row layout.** `.dashboard-panels` (a single flex row) now holds:
- **Left — `.panel-conclusions`:** the two gauges, unchanged internally, now side by side inside one panel instead of their own full-width row (this was already their layout; only the wrapping div changed).
- **Middle — `.panel-evidence`:** a new permanent `#evidence-meters` container, populated by `renderDashboard()` on every render via `document.getElementById('evidence-meters').innerHTML = evidenceSummaryHtml(evidenceSources)` — the exact same function call the More Info panel used to make, just with a permanent home instead of a hidden one. The More Info panel's duplicate call was removed outright (not left as a redundant second copy) — More Info now only carries the app/device counts and links that don't fit in the compact meter view. Scanner moved into this same panel, directly beneath the meters (`.scanner-inline`), sharing the panel's border rather than owning a separate bordered card.
- **Right — `.panel-whatnext`:** the What Next blurb + buttons, tightened (smaller font, less padding, `min-height` trimmed from 2.3rem to 1.4rem) to match the new row's compactness.

**Scanner is no longer a standalone card.** The prior pass's `.info-display`/`.dashboard-displays` card system (which gave Scanner its own ~130px bordered box beside What Next) is gone. `SCANNER_STATE_META` dropped its `caption` field entirely — not just visually hidden — leaving a genuine two-line compact readout ("SCANNER" eyebrow, then a bold status word: "READY"/"RECEIVING"/etc.), matching the user's own worked example exactly. `setScannerActivity()` updated to match (no longer reads/writes a caption element).

**A real bug caught mid-fix, not anticipated:** the persistent `#evidence-meters` container was initially marked up as `<ul class="evidence-summary-list" id="evidence-meters">`, but `evidenceSummaryHtml()` already returns its own `<ul class="evidence-summary-list">...</ul>` wrapper — inserting that string as `innerHTML` of another `<ul>` would have produced invalid nested-list markup (`<ul><ul>...`). Caught by re-reading `evidenceSummaryHtml()`'s return statement before wiring the call site, not discovered live; fixed by changing the container to a plain `<div>`.

**Compactness measures, concrete and verified live, not just described:**
- `.dashboard` outer padding: `1.1rem 1.5rem` → `0.9rem 1.1rem`.
- `.evidence-meter-track` height: 9px → 6px (still clearly visible as a meter, per real DOM measurement below).
- `.evidence-summary-row` padding: 0.35rem → 0.22rem; `.evidence-summary-list` font-size: 0.82rem → 0.74rem.
- `.what-next-blurb`: font-size 0.82rem → 0.78rem, `min-height` 2.3rem → 1.4rem (most `next.text` values, already trimmed to one sentence by the prior pass's `firstSentence()`, fit on one line at this column width).
- `.what-next-btn`: padding and font-size both reduced (~15% smaller).

**Measured result, real browser, not estimated:** the empty (no-evidence) dashboard measures **320px total height** (`#dashboard.getBoundingClientRect().height`) — this is WITH the four evidence meters and Scanner now genuinely visible, where the prior pass's equivalent state measured roughly 280–300px WITHOUT them ever being shown (gauges row ~148px + a separate What Next/Scanner row ~130–155px, evidence meters contributing zero visible height since they were hidden). A real, data-rich scan-backed session (`f808d771-…`, real Standard scan + partial Intended Use context) measured **336px** — confirming the layout stays compact under real data, not just the synthetic empty case.

**Verified live, fresh browser session, exactly against the six required checks:**
1. Evidence meters visibly present on load, no interaction needed — confirmed via direct DOM read (`#evidence-meters` non-empty, `display !== 'none'`) immediately after page load, before any click.
2. Toggling an Intended Use pill (Office/Productivity) moved the Intended Use meter fill from 0% → 67% and the Confidence gauge from "No Data Yet — 0%" to "Limited Evidence — 20%" in the same render — confirmed via direct before/after DOM read.
3. Scanner's `getBoundingClientRect()` confirmed sitting directly beneath the meters, inside `.panel-evidence`, not a sibling card.
4. Scanner no longer has its own `.info-display`/bordered-card wrapper — confirmed by inspecting its actual parent element (`.panel-evidence`, shared with the meters) and the CSS rule that applies to it (`.scanner-inline`, a border-top divider, not a full card).
5. Desktop height dropped from an estimated ~280–300px (evidence-blind) to a measured 320–336px (evidence-visible) — net compaction relative to any layout that would have shown the same information the old way (two stacked rows).
6. Mobile (375px): confirmed zero horizontal overflow (`body.scrollWidth === innerWidth`), zero panel-content overflow (checked each `.panel`'s last child bottom against the panel's own bottom, all three panels clean), evidence meters and Scanner both visible and readable stacked full-width, report round-trip clean with zero console errors.

**Explicitly not attempted, per the correction's own scope:** no changes to `evidenceSourcesFor()`, `computeSynthesis()`, the Recommendation Eligible gate, or any other logic from the prior two passes — confirmed by re-reading the diff before committing. This was a placement and compactness fix only.

### Advisor Refinement addendum (same branch, "calmer instrument panel" pass)
A refinement-only pass, explicitly not a redesign — the three-panel dashboard architecture (Conclusions / Evidence / What Next), the Recommendation Eligible gate, and every function in `assessment.js` are untouched. All changes are presentation/interaction in `public/index.html`.

**1. Evidence compacted further.** `.evidence-summary-list` font-size 0.74rem → 0.66rem, `.evidence-summary-row` padding 0.22rem → 0.14rem, `.evidence-summary-toprow` margin 0.18rem → 0.12rem, `.evidence-meter-track` height 6px → 4px. Purely typographic/spacing — `evidenceSourcesFor()`/`evidenceMeterPct()` untouched, meters still read contribution-toward-Confidence, not workflow completion.

**2. Scanner became a genuine one-line indicator**, matching the brief's own example (`SCANNER  ● READY`): `.scanner-inline` changed from a centered two-line block to a left-aligned flex row (fixed "SCANNER" label, fixed-position dot, `white-space: nowrap` status text). The six-state color model was replaced with the brief's exact six states — neutral/dim/amber/blue/green/red — which required two real additions, not just a repaint:
- **`unavailable`** (neutral) — wired at the one place a genuine "scanning can't be used here" fact already existed: `isLikelyNonWindows()`, the same check `evidenceSourcesFor()`'s `scanUnavailable` param already used. `loadSessionIfPresent()` now calls `setScannerActivity('unavailable')` once at load (skipped when the scan-just-happened bridge flag is set, so it can never race a real in-progress scan) — a static fact about the viewing device, checked once, never re-evaluated.
- **`error`** (red) — `wireScanButton()`'s existing `POST /api/session` failure branch previously called `setScannerActivity('ready')` after showing "Error — try again" on the button itself, silently NOT reflecting the failure in the indicator. Now calls `setScannerActivity('error')` — a real, already-detected failure now honestly shown, not a new failure mode invented for this pass.
- `updated`'s label changed from "Updated" to "Completed" to match the brief's vocabulary exactly.

**3/4. What Next rebuilt as a ranked recommendation QUEUE**, not a paragraph with buttons. `#what-next-blurb`/`#what-next-actions` (a sentence + a horizontally-wrapping button row) were removed outright and replaced with `#whatnext-queue`, a vertical stack of fixed-height `.whatnext-slot` rows built by `whatNextSlotEl()`. Still reuses `nextActionFor()`'s existing `{kind, text}` output verbatim as the only source of what's recommended — `WHAT_NEXT_PRIMARY_LABEL`/`TARGET`/`WHAT_NEXT_SECONDARY` are the same maps as before (secondary entries gained a `title` field), this pass only changes how they're rendered. Three fixed slots, always: primary recommendation, secondary recommendation, and a third slot that today is always an empty dash placeholder (`—`) for every real (non-empty) state — no third genuinely worthwhile recommendation exists yet beyond the pair `WHAT_NEXT_SECONDARY` already curates, and inventing one to fill the slot was explicitly rejected per the brief's own "avoid inventing busywork" instruction. The true-empty (`start-empty`) state is the one exception: it renders three REAL slots — Start Scan, Answer Questions (unranked, equal weight, matching the surface's original "two equally prominent entry paths" precedent), and a genuinely new third recommendation, **Load Assessment** (`window.NavShell.triggerImport()`), which is exactly the moment importing a prior assessment is most valuable — there's nothing else to lose. Monospace type + a leading "›" marker (dash for empty slots) make the queue read as a generated readout, not prose.
- **Real defect found and fixed while wiring Load Assessment:** `window.NavShell` (nav.js) resolves asynchronously behind its own `/api/auth-status` fetch, so it was reliably still `undefined` at `loadSessionIfPresent()`'s first render — the empty-state queue's third slot silently stayed a dash forever, never upgrading to Load Assessment even once nav.js finished loading moments later. Fixed with a one-shot `navshell:ready` listener (`document.addEventListener('navshell:ready', () => renderDashboard(), { once: true })`) — a no-op once real evidence already exists by the time it fires, but the only way the true-empty state's third slot ever appears in practice.

**6. Hover text replaces dashboard prose.** Every real slot carries a `title=""` tooltip; the visible queue itself is label-only. The primary slot's tooltip is `nextActionFor()`'s own full, untrimmed `next.text` — the exact explanatory sentence the old blurb used to show inline now lives entirely on hover, never duplicated on the page. Secondary slots carry short, hand-written micro-copy (`WHAT_NEXT_SECONDARY[...].title`) describing that specific action, not a restatement of the primary recommendation. `firstSentence()` (the presentation-only trim the prior pass added for the now-deleted blurb) was removed as dead code rather than left unused.

**7. Investigated: why does Intended Use move Confidence but not Suitability?** Read `recommendationFor(synthesis)` and `computeSynthesis(results, personalContext)` directly — neither takes `adaptiveContext` as a parameter, anywhere, at any layer. This is the same scan-only boundary the Coherence & Trust addendum already investigated and reaffirmed once; this pass reaffirms it again, live, against real production data rather than by re-reading the code alone. **Conclusion: the separation is still correct and should not change.** Four reasons, unchanged from the prior investigation and reconfirmed here:
- Suitability is a compatibility verdict — it must be earned by catalog-verified evidence (the scan), never inferred from a stated workload category. Feeding Intended Use into `recommendationFor()` would mean two workload-identical users with different actual installed software could receive the same Suitability reading, which contradicts the entire product premise.
- Intended-Use-only users already have their own, separate, non-numeric answer: `contextualReadFor()`'s directional read ("Likely a Good Candidate" / "Depends on Your Setup"), rendered with its own "Early Read" qualifier tag precisely so it's never mistaken for a scan-backed verdict.
- Confidence and Suitability are deliberately different-scope metrics (Dashboard Instruments addendum, above) — Confidence answers "how much evidence do we have," Suitability answers "what does the evidence say." Intended Use is real evidence for the first question and irrelevant to the second.
- The Recommendation Eligible gate is orthogonal to this question and was correctly left untouched — confirmed live, see below.
- **Live confirmation, real data, not constructed:** loaded the real captured session (`f808d771-…`, `score: 76`, `confidencePct: 48`) and added two Intended Use answers on top of it. Confidence correctly rose to 64% ("Good Evidence") — Intended Use evidence counted. Suitability stayed exactly "Need More Evidence" — the scan's own 48% classification coverage is still under the gate's 0.5 threshold, and no amount of Intended Use evidence changes that number, because it structurally cannot reach `recommendationFor()`. The "Why?" panel correctly kept showing the scan-quality gate explanation, never leaking the underlying verdict — this is the exact real-world case the Recommendation Eligible gate (Coherence & Trust addendum) was built to prevent, now observed working end-to-end on real production data rather than synthetic test data.
- **No code changed for this item** — investigation-only, per the brief's own "do not force a change if it conflicts with the existing recommendation model" instruction.

**Verified live, fresh browser session, desktop (1280px) and mobile (375px):** empty state (three-slot queue: Start Scan / Answer Questions / Load Assessment, all three tooltips correct, Scanner one-line "SCANNER ● READY"); all eight Scanner color states exercised directly via `setScannerActivity()` (unavailable/ready/downloading/waiting/connected/receiving/updated/error all render the correct label and dot color); Intended Use pills toggled via direct `.click()` calls (this pane's `computer`-tool click delivery proved unreliable again for pill buttons specifically, consistent with the known limitation already documented in the Coherence & Trust addendum — verified instead by dispatching real DOM `click()` events, which is a strictly stronger test of the underlying app logic since it exercises the exact same listener) — Confidence and the queue both reacted correctly, Suitability did not (see item 7); dashboard height held exactly steady (254.4px) across the empty state and the contextual-read state, confirming the ranked-queue's fixed vertical shape; real scan-backed session (`f808d771-…`) round-tripped to `report.html` and back with zero console errors; mobile stacked cleanly with zero horizontal overflow (`body.scrollWidth === innerWidth === 375`).

**Does the dashboard feel more like an advisor than a dashboard?** Closer. The queue framing is the biggest qualitative shift this pass made — "here are your options, ranked, click one" reads as active guidance in a way the old "here's a sentence, here are some buttons below it" never quite did, and the empty third slot (rather than a forced third button) is what makes the ranking legible: a two-item queue with a visible gap communicates "these are genuinely the only two things worth doing right now," not "we ran out of ideas." The Scanner indicator finally reads as an instrument rather than a status paragraph. What's still not fully "advisor-like": the queue is still a fixed two-real-slot shape today (primary + secondary) for every scan/contextual state — the brief's own richer example list (Save Progress, Sign In) has no wired destination yet in this codebase, so a genuinely deeper queue (3+ real, differently-sourced recommendations) is gated on those capabilities existing, not on this pass's rendering logic, which is already built to show more slots the moment more real recommendations exist.

**Explicitly not attempted, per the brief's own scope:** no changes to the three-panel architecture, `computeSynthesis()`, `recommendationFor()`, `contextualReadFor()`, `nextActionFor()`'s branch logic, or the Recommendation Eligible gate. No fabricated "Save Progress" or "Sign In" queue slots — neither has a real, already-wired trigger reachable from this page today, and per the brief's own instruction ("avoid inventing busywork"), an unwired placeholder button was judged worse than an honest empty slot.

### Investigation Console addendum (same branch, visual-cohesion pass)
A visual-cohesion refinement pass, explicitly framed as "do not revisit recommendation logic, scoring, confidence calculations, or evidence organization" — none of `assessment.js` was touched. All changes are CSS/HTML/presentation-JS in `public/index.html`.

**1. Dashboard/console width increased 720px → 1040px.** `.dashboard`'s `max-width` was a leftover from the original single-summary-card layout, predating the three-panel instrument row — at 720px the row was visibly cramped. `.hero-compact` (the headline) stays at 720px deliberately, for text readability; only the two "product" elements (`.dashboard` and the new `.console`, below) widen, and they widen to the exact same value so the two layers align edge-to-edge rather than reading as two differently-sized blocks stacked on top of each other.

**2/3. What Next grew to four fixed slots; Scanner prototyped as the permanent fourth.** `whatNextSlotEl()`'s empty-slot branch now renders the same leading "›" marker every real slot uses, followed by a dashed placeholder line (`'-'.repeat(20)`) instead of a centered "—" — the intent, per the brief, is a slot that reads as intentionally available, not disabled or unfinished. `renderWhatNextPanel()` now always appends a fourth slot built by a new `whatNextScannerSlotEl()`, which is deliberately NOT built from the recommendation-slot shape (`{label, tone, onClick, href}`) — it's non-interactive, non-anchor, styled with a muted background and a thin top divider separating it from the real recommendations above, so it reads as a persistent utility living alongside the queue rather than a fourth ranked choice. Scanner's markup (and the `scanner-activity-dot`/`scanner-activity-value` ids `setScannerActivity()` already targets) moved out of the Evidence panel entirely — `.panel-evidence` now only holds the eyebrow and `#evidence-meters`.
- **Real defect found and fixed, not anticipated up front:** moving Scanner into the queue means its DOM nodes get destroyed and recreated on every `renderWhatNextPanel()` call (`queue.innerHTML = ''`), which happens on every dashboard re-render (e.g. an Intended Use pill toggle) — a state set mid-scan (`downloading`, `waiting`, ...) would have silently reset to the slot's hardcoded default ("Ready") the next time anything else on the page changed. Fixed with a new `currentScannerState` module-level variable that `setScannerActivity()` always updates first (even if the DOM nodes don't exist yet), and `renderWhatNextPanel()` calls `setScannerActivity(currentScannerState)` again immediately after appending the freshly-built scanner slot, re-syncing the new nodes to whatever the logical state actually is. This also correctly handles the pre-existing case where `loadSessionIfPresent()` calls `setScannerActivity('unavailable'|'receiving')` before the first render ever happens (the DOM lookup silently no-ops, but `currentScannerState` is already set correctly for the render that follows). Verified directly: forced `downloading`, called `renderDashboard()`, confirmed the rebuilt slot still read "Downloading" with the correct amber dot color.

**4. Evidence Nav + Details Workspace wrapped in one Investigation Console frame**, replacing the old "tabs protrude above a bordered box" pattern (`.evidence-tab` used to carry `border-bottom:none`/`top:1px`/`border-radius:10px 10px 0 0` to visually poke above `.details-workspace`, which had its own separate border/background/max-width). Both now live inside a new `.console` div sharing `.dashboard`'s exact card language (background/border/radius, same 1040px width) — `Instrument Panel` → `Investigation Console`, one aligned unit, not `Dashboard` → `tabbed web page`. `.evidence-tab` was restyled to match the What Next button family (rounded rect, blue-filled when active, muted/outlined when not) without literally becoming a `.whatnext-slot` — it selects a MODE, not a ranked recommendation, so no "›" marker and no monospace, per the brief's own "same family, not identical" instruction. `.details-workspace` dropped its own border/background/max-width/bottom-corner-radius and became an inset sub-panel instead — same treatment `.panel` already uses inside the dashboard (`#0f172a` fill, `#334155` border, 10px radius) plus a subtle `inset` box-shadow — so the console reads as one instrument with a display inside it, not a monitor nested inside another monitor. Both `#evidence-nav` and `#details-workspace` kept their existing element ids; no JS changed for this item.

**5. Restraint preserved.** No new explanatory paragraphs were added anywhere in this pass — the width change, the fourth slot, and the console frame are all structural/visual; the queue's existing hover-tooltip mechanism (Advisor Refinement addendum, above) is unchanged and still carries all the "why" text off the visible page.

**Verified live, fresh browser session, desktop (1280px) and mobile (375px):** `.dashboard` and `.console` both measured exactly 1040px wide and visually aligned; the four-slot queue confirmed via DOM read for both the empty state (Start Scan / Answer Questions / Load Assessment / Scanner) and a real scan-backed state (Review Findings / View Detailed Report / dashed placeholder / Scanner); all eight Scanner color states re-verified correct in the new location; the mid-render state-persistence fix confirmed directly (`downloading` survived a `renderDashboard()` call); Evidence Navigation tabs confirmed rendering as filled/outlined segment buttons inside the console frame, `Intended Use` tab switch confirmed correctly swapping `#details-workspace` content while the frame itself stayed put; mobile (375px) confirmed zero horizontal overflow, tabs wrapping cleanly to two rows, console's inset display panel reading as one continuous block beneath the mode selectors; real captured session (`f808d771-…`) round-tripped to `report.html` and back with zero console errors in both directions.

**Comments on the three specific questions asked:**
- **Does the wider layout improve the instrument panel?** Yes, clearly — at 720px the three-panel row felt like it was fighting its own container; at 1040px the Evidence meters and What Next queue both have real room, and nothing about the wider dashboard reads as empty or over-spaced. It stops well short of full-bleed, which was the right call — a 1280px-viewport screenshot still shows real margin on both sides.
- **Does relocating Scanner feel more or less natural?** More natural, and more so than expected going in. Grouped with the recommendation queue, "is the scanner connected" reads as one more piece of actionable assessment state sitting next to "what should I do next" — which is arguably a more honest categorization than its old home in the Evidence panel, where it sat awkwardly next to passive meters (Current Computer, Intended Use, Community, Findings) despite not being an evidence *source* itself. The muted/divided treatment successfully keeps it from being mistaken for a fourth recommendation. Nothing here argues for reverting it.
- **Does the investigation console now feel like part of the same product, not a separate web page?** Substantially yes. The shared card chrome and matched width mean the eye reads dashboard-then-console as one continuous instrument rather than a page section boundary; the mode selectors no longer look like HTML `<table>`-era browser tabs. The remaining gap: the console's *internal* content (Current Computer's scan-level radios, Intended Use's pill grid, Findings' stat trio) still uses ordinary page typography/spacing rather than an instrument-panel visual language of its own — that content was explicitly out of scope this pass ("do not revisit... evidence organization") and would be the natural next target if this direction continues.

### CheckMyARM UX Evolution Prototype (same branch, information-architecture pass)
A full navigation/organization redesign of the Investigation Console, explicitly framed as a UX/IA prototype using placeholder data where needed — not a scanner, recommendation-engine, or backend change. `main.rs`, `scan.js`, and `assessment.js` are all untouched; every new function in `public/index.html` either builds new presentation on top of existing real data or consumes existing assessment.js functions read-only (`isBackgroundApp()`, `defaultImportanceForApp()`, `defaultImportanceForDevice()`, `classifyPopulation()`, `decisionKey()`/`deviceDecisionKey()`, `personalContextToEntries()`/`writeSessionContext()`).

**Governing reframe:** the Assessment Workspace isn't displaying scan results — it's building an increasingly accurate model of the user's computing environment, with three equally-valid entry points (scan, describe Use Case, or build manually) rather than a scan-first funnel.

**1. Five workspaces replace the old four flat tabs**, organized into a 3-zone grid mirroring the brief's own diagram — `EVIDENCE_CATEGORIES` entries now carry `zoneCol`/`zoneRow`/`zoneRowSpan` fields, and `renderEvidenceNav()` places each tab via inline `grid-column`/`grid-row` styles against a `1fr auto 1fr auto 1fr` track (the `auto` columns hold two `.zone-divider` hairlines — thin vertical rules, not text labels, per "the layout itself should communicate this progression"):
- **Observed** (left, 2 rows) — Detected Apps / Detected Devices.
- **Intent** (center, spans both rows, deliberately the default/home tab — `activeTabId = 'use-case'`) — Use Case (the renamed Intended Use tab, content unchanged).
- **Assessment** (right, 2 rows) — Applications / Devices.
- "Current Computer" (the old scan-trigger tab) and "Findings" are retired as standalone tabs — scanning is now reached contextually from Detected Apps/Devices' empty state (`scanCtaHtml()`, a shared builder reused by both, wrapping the exact same `wireScanButton()`/`showWaitingMessage()` handshake this surface has always used), and Findings' stat trio/attention list moved into Applications. "Community" is gone from this console too — still reachable via the top nav's own Community menu (`nav.js`, untouched), not duplicated here.
- Mobile (≤640px) collapses the grid to a single stacked column via `grid-column: auto !important; grid-row: auto !important` on `.evidence-tab` — the explicit desktop placement only makes sense against the 5-track grid, so it's force-overridden back to natural document order rather than fighting a narrower grid.

**2. Detected Apps / Detected Devices — "what did we find, what do we currently think matters."** Every item renders in one of three tiers — Likely Important / Possible Importance / Likely System-Background — computed by `appImportanceTier()`/`deviceImportanceTier()`, which read real assessment.js evidence (`isBackgroundApp()`, `defaultImportanceForApp().level`, `defaultImportanceForDevice().level`) rather than inventing a parallel classification. Nothing is ever hidden — every detected item appears in exactly one tier section, always. A three-way override (Important / Auto / Background) per item writes to the *real* Personal Context via `applyImportanceOverride()` — reusing `decisionKey()`/`deviceDecisionKey()`/`personalContextToEntries()`/`writeSessionContext()`, the identical mechanism workspace.html's own decision buttons use (`critical_to_me: true` for Important, `decision: 'doesnt_matter'` for Background) — so this genuinely affects Confidence/Suitability downstream, not just this tab's own display.
- **No real "is this a system device" signal exists anywhere in this codebase** (see the Device/driver detection section above) — `deviceImportanceTier()`'s "background" tier is an explicit, commented placeholder mapping of `defaultImportanceForDevice()`'s existing recency-based 'low' level, not a claim that a real background-device signal exists.
- **Real bug found and fixed during verification, not anticipated up front:** the override buttons correctly wrote to Personal Context (confirmed directly via `sessionStorage` inspection) but the item never visibly moved tiers, because `appImportanceTier()`/`deviceImportanceTier()` never consulted the override — only `isBackgroundApp()`'s own internal override-awareness did, and only in the background-suppression direction. Fixed by checking `decision.critical_to_me`/`decision.decision === 'doesnt_matter'` first, before falling through to the computed read — "the user is refining our interpretation" now means the item genuinely moves, not just that a button lights up while the row stays put. Both call sites of `deviceImportanceTier()` (`renderDetectedDevicesTab`, `combinedDeviceItems()`) were updated to pass the looked-up `decision`, not just the device.

**3. Applications / Devices — "what should influence this recommendation," the app's evolving assessment model.** Each page visibly combines four sources — Detected (real, via `classifyPopulation()`/scan devices), Suggested (placeholder, `SUGGESTED_APPS_BY_USE_CASE`/`SUGGESTED_DEVICES_BY_USE_CASE`/`SUGGESTED_DEVICES_BY_RELATIONSHIP`, keyed off the *real* `INTENDED_USE_OPTIONS`/`RELATIONSHIP_OPTIONS` values so "the model gets richer as Use Case fills in" is an observable behavior, not just a claim), Manual (real, user-typed, in-memory for this page load), and Planned/future (real, in-memory, same mechanism) — organized by category (`categorizeAppName()`/`categorizeDeviceName()`, small principled keyword-list functions, same "short list over sprawling table" precedent as `isSystemComponent()`) rather than alphabetically. `manualApps`/`plannedApps`/`manualDevices`/`plannedDevices` are deliberately NOT persisted (no localStorage, no server write) — real, functional add/search/filter behavior, but throwaway storage, consistent with this being a UX prototype rather than a new feature. Search filtering only replaces a `#app-results`/`#device-results` sub-container (not the whole tab), specifically so typing in the search box never steals its own focus.

**4. Two layout improvements, applied alongside the IA work:**
- `.how-it-works`/`.what-we-check`/`.score-legend` widened 800px → 1040px (matching `.dashboard`/`.console`) — their existing `grid-template-columns: repeat(auto-fit, minmax(...px, 1fr))` CSS already supported more columns given more width, so this was a pure width change, no grid-rule rewrite. Verified directly: all 4 "What We Check" cards now share one row at 1280px viewport width.
- The three-zone grid (item 1, above) is the "visually reinforce the console organization" ask — same mechanism, not a separate change.

**5. Real defect found and fixed during mobile verification, not anticipated up front:** `.item-row` (name + source tag + tier badge + 3-button override group, used by all four Detected/Assessment workspaces) overflowed horizontally below ~640px — confirmed via direct measurement (`details-workspace.scrollWidth` 498px against a 345px `clientWidth`). Fixed with a mobile-only rule wrapping the row to two lines (name on its own line, tags/overrides flowing below) rather than shrinking text until unreadable — reverified at 0px overflow after the fix.

**Verified live, fresh browser session, desktop (1280px) and mobile (375px), against a real synthetic scan** (created via direct `POST /api/scan` since no captured real-hardware session survived this session's 24h expiry — a known, already-documented limitation, not new): the 5-tab zone grid renders with correct placement and dividers; Use Case is the default tab on first load; empty-state Detected Apps/Devices correctly show the scan CTA and wire the real scan handshake; a real synthetic population (VS Code running, Chrome pinned, Teams/Notepad++/Steam/an AppX Notepad package, a present webcam, a stale USB hub) classified correctly into all three tiers on both Detected tabs; the Important override on Notepad++ moved it from Possible to Likely Important on BOTH Detected Apps and Applications (confirming the shared `appImportanceTier()` fix propagates everywhere it's used) and persisted correctly in `sessionStorage`; Use Case pill selections correctly populated Suggested apps/devices on the Assessment-zone pages; manual add + search both worked without losing input focus; the real stat trio (Materially Influencing/Background/Suggested-Manual-Planned) matched `computeSynthesis()` output; mobile stacked the zone grid to one column and item rows to two lines with zero overflow after the fix; report round-trip (`?origin=assessment`) loaded with zero console errors in both directions.

**Explicitly not attempted, per the brief's own scope:** no scanner/recommendation/confidence/backend/API/community-workflow changes of any kind (confirmed via diff review before committing — only `public/index.html` changed). No real device-background signal was invented to fill the placeholder gap named in item 2. No persistence added for Manual/Planned entries. The console's zone dividers are hairlines, not colored zone-coding — considered and rejected in favor of relying on grid position + spacing alone, to avoid a new color language competing with the existing semantic palette (green=native/good, red=unsupported/error, blue=primary/active).

**Observations, as requested:**
- **What feels significantly improved:** the 3-zone layout makes the "observe → decide intent → assess" story legible at a glance without a single word of explanation — the dividers plus the 2-1-2 shape alone do it. Detected Apps/Devices' tri-tier classification is the single biggest qualitative change: "here's every app, here's what we think, correct us" reads as fundamentally more trustworthy than a flat list, and the fact that the override is real (not cosmetic) means poking at it during review isn't just a UI exercise. Removing the standalone "Current Computer" scan tab in favor of contextual scan CTAs genuinely reinforces "a scan is an accelerator, not a prerequisite" — Use Case as the default tab makes that concrete on first load, not just in copy.
- **What still feels awkward:** the Applications/Devices pages' category buckets can feel thin with only a handful of items (a Development section with 3 apps looks similar in visual weight to Utilities with 1) — real usage with a full scan population would likely need collapsed/expandable categories the way Results already does for large buckets. The "Suggested" mechanism is entirely Use-Case-driven right now; there's no equivalent "suggested because you're missing an obvious category" signal, which the spec's own framing ("increasingly accurate model") gestures at but this pass didn't build. Devices staying score-inert (per existing product design) means the Detected Devices override, while real and saved, has no visible payoff today — worth a line of copy clarifying that explicitly if this ships.
- **What I'd change with another iteration:** make the override control double as the "why" affordance — right now Important/Auto/Background are three buttons with only a title="" tooltip; a small inline reason chip (reusing `defaultImportanceForApp()`'s own `reason` string, already computed and already discarded by this pass) would answer "why did you classify it that way" without a click. I'd also unify Applications/Devices' search+add row visually with the What Next queue's monospace/instrument language from the prior passes — right now the Investigation Console's *outer* frame matches the dashboard's card language (per the earlier Investigation Console addendum) but its *inner* content (search boxes, add rows, category headings) still reads as ordinary form UI, which is the same gap the prior addendum's own "known limitation" already named.
- **Simplifications/opportunities this surfaced:** Findings' stat trio and attention list turned out to fold into Applications with almost no friction — strong evidence that "Findings" was never really a fifth kind of thing, just a derived view over the same population Applications already needed to show, exactly as the Evidence Model addendum's "Findings collects nothing independently" finding (documented earlier in this file) predicted. The override-writes-to-real-Personal-Context design also suggests the "Detected" tri-tier read could plausibly *become* the primary Light-Assessment importance UI (replacing or feeding `isLightPriorityApp()`'s current binary priority/deprioritized split) rather than staying a parallel prototype surface — worth a deliberate design conversation, not a decision to make inside a UX-only pass.

### Question-Driven Refinement addendum (same branch, scan lifecycle + Use Case + Applications rebuild)
A direct follow-up to the UX Evolution Prototype above, closing the gaps real review of that prototype surfaced: the scan lifecycle could show a stale "Run Scan" button during an active scan, Use Case was still the old flat 5+3-option vocabulary rather than the richer relationship/workload/sub-workload shape actually wanted, and Applications' per-item rows didn't yet carry compatibility or a real "please verify" mechanism. Framed explicitly around five questions the whole surface answers — What exists? (Scanner) / What are you trying to accomplish? (Use Case) / Which of these actually matter? (Applications/Devices) / What doesn't quite add up? (Please verify) / How ready are you now? (Recommendation) — though that framing informed the work rather than changing any code structure itself. `main.rs`, `scan.js`'s `classifyApps()`, and `computeSynthesis()`/`recommendationFor()` are all untouched — everything below is `public/index.html` + `public/assessment.js` taxonomy/copy only, confirmed via diff review before committing.

**1. Use Case taxonomy expanded, not reinvented.** `INTENDED_USE_OPTIONS` grew from the old flat 5 options to a 6-category vocabulary (`office`/`development`/`gaming`/`media`/`creative`/`utilities`, matching `APP_CATEGORIES`'s existing Title-Case category names exactly — see item 3 below for why that match matters) and `RELATIONSHIP_OPTIONS` grew from 3 to 6 (`primary`/`replacement`/`travel`/`secondary`/`shared`/`other`). A new `sub_workloads` field (`{ [workload]: [subKey, ...] }`, e.g. `{office: ['productivity', 'collaboration']}`) was added to `adaptiveContext`, sanitized against a `WORKLOAD_SUBCATEGORIES` map (4 sub-keys per top-level workload, 22 total) the same way every other adaptive-context field already is. `contextualReadFor()`'s branch conditions were updated to the new key names (`specialized`/`travelCompanion`/`onlyLightweight` checks) with identical logical shape — this is a vocabulary change, not a scoring change; `computeSynthesis()`/`recommendationFor()` still never take `adaptiveContext` as input, and still never will (see the repeatedly-reaffirmed scan-only boundary, most recently the Advisor Refinement addendum's item 7 investigation).

**2. Use Case UI: relationship radio + expandable workload/sub-workload checkboxes, nothing below the top section fixed.** `renderRadioGroup()`/`renderCheckboxGroup()` (new, small, reusable) replaced the deleted `renderPillGroup()`. Selecting a top-level workload checkbox reveals a `.subworkload-section` beneath it with that workload's own sub-category checkboxes — genuinely dynamic, not a fixed layout with hidden/shown blocks: unchecking the parent removes its sub-section from the DOM entirely (verified directly: `.subworkload-section` count tracks checked-workload count exactly, in both directions). Hint pre-activation (`effectiveIntendedUse()`/`effectiveRelationship()`) and the "infer aggressively, commit conservatively" write boundary (Dashboard Instruments addendum) are unchanged — only the option vocabulary and the rendering shape changed.

**3. Scan lifecycle: the center panel owns scan state while a scan is active, "Run Scan" never appears next to a running scan.** `isScanActive()` checks `currentScannerState` against a small `SCAN_ACTIVE_STATES` set (`downloading`/`waiting`/`connected`/`receiving`). `renderDetectedAppsTab()`/`renderDetectedDevicesTab()` both gate on `!currentResults && isScanActive()` before falling through to their existing scan-CTA branch — when true, they render a live `scanProgressHtml()` panel (heading, subtext, the same compact Scanner-instrument value the What Next queue's Scanner slot already shows, plus the existing "run ngpcx-scanner.exe" waiting copy when in the `waiting` state) instead of the Run Scan button. `wireScanButton()`'s click handler calls `setScannerActivity(state); refreshScanProgressDisplay();` in lockstep at every transition (downloading → waiting → connected/error), and `refreshScanProgressDisplay()` re-renders whichever Observed-zone tab is currently active — a no-op if the user has switched to Use Case/Applications/Devices, which pick up the current state honestly the next time the user returns to a Detected tab (`isScanActive()` is read fresh on every render, not cached at tab-switch time). Verified directly: switching away from Detected Apps mid-scan and back still shows the live progress panel with no stale/duplicate Run Scan button; the panel correctly reverts to the ordinary Run Scan CTA once `setScannerActivity('ready')` fires (matching the existing post-completion redirect, which navigates away from this page entirely before that would even need to happen in practice — this reversion path mainly protects an error/reset case).

**4. Applications rebuilt around per-item checkbox + compatibility + "please verify," matching the exact worked examples from the brief.** `combinedApplicationItems()` now attaches each item's real `effectiveBucket` (from `classifyPopulation()`, Detected items only) via `COMPAT_BUCKET_LABELS` (`✓ Native ARM64` / `⚡ Emulated` / `✗ Unsupported` / `? Unknown` / `✓ Browser-Hosted (Supported)`, reusing `report.html`'s own convention verbatim) and a `needsVerification` flag: `appNeedsVerification()` is true only for a Detected item whose category doesn't match any currently-selected top-level workload — this is exactly why item 1 unified the two taxonomies (`INTENDED_USE_LABELS[workload] === categorizeAppName()`'s output), letting the comparison be a plain string check with no translation layer. `computeIncluded()` derives the checkbox's checked state from real Personal Context first (`critical_to_me`/`doesnt_matter` override, checked over unchecked) and falls back to `!needsVerification` otherwise — a Detected item outside the current Use Case starts unchecked with an inline note ("Please verify — detected, but Gaming isn't currently part of your selected workloads."), a Suggested/Manual/Planned item renders "Not yet assessed" rather than fabricating a compatibility verdict (no per-app knowledge exists for anything not in the real classified population — inventing one would violate this project's standing no-fabrication rule). **The checkbox is a genuine write, not cosmetic**: toggling it calls the exact same `applyImportanceOverride()` Detected Apps' Important/Auto/Background buttons already use, so checking a "please verify" item clears its flag AND promotes it on Detected Apps in the same render pass — confirmed live: checking Steam's box in Applications immediately cleared its verify-note there and moved it from "Possible Importance" to "Likely Important" on Detected Apps, matching the single-source-of-truth precedent every override mechanism in this surface already follows. One accepted trade-off: the checkbox's own `change` listener deliberately does NOT also call the tab's local `refreshResults()` — `applyImportanceOverride()` already triggers a full `renderDetailsWorkspace()` re-render, and calling both would be redundant; the cost is that a checkbox click resets any in-progress text in the Applications search box, judged acceptable to avoid a second, parallel re-render path.

**5. Instrument Panel copy realigned to the new vocabulary.** The More Info evidence summary's four rows read `Scanner` / `Use Case` / `Community` / `Please Verify` (previously `Current Computer` / `Intended Use` / `Community` / `Findings` — `Findings` as a tab no longer exists post-UX-Evolution-Prototype, so its evidence-row label was stale). The Why-panel gate explanation and the no-evidence fallback copy both say "Scanner or Use Case" instead of "Current Computer or Intended Use." `WHAT_NEXT_PRIMARY_LABEL['questions']` reads "Add Use Case." "How It Works" step 2 heading reads "Describe Your Use Case." No `evidenceSourcesFor()` logic changed — this item is display-string alignment only, confirmed by diff review.

**Verified live, fresh browser session, desktop (1280px) and mobile (375px):** the Use Case tab's 6 relationship radios and 6 workload checkboxes render with correct labels; checking Development then Office each correctly revealed their own 4-item `.subworkload-section` (Web Development/Mobile Development/DevOps/Data Science; Productivity/Collaboration/Accounting/Project Management), and unchecking Development removed only its own section while Office's stayed, with `sub_workloads` persisting correctly in the sessionStorage-backed adaptive-context stash (`{office: ['productivity']}` after clicking the Productivity sub-checkbox); Confidence moved from "No Data Yet" to "Limited Evidence" reactively as workloads were selected, matching the existing Dashboard Confidence model unchanged; a real synthetic Standard-mode scan (6 apps spanning Office/Development/Gaming/Creative/Utilities categories, 2 devices) loaded cleanly and Detected Apps correctly tiered all 6 into Likely Important (3) / Possible Importance (3); the scan-progress panel, exercised via `setScannerActivity()`+`refreshScanProgressDisplay()` in lockstep (matching the real `wireScanButton()` code path, since real click-driven scans remain unreliable in this browser-automation pane per this session's own established precedent), correctly replaced the Run Scan CTA with a live "Scan In Progress" panel showing the Waiting state and its fallback download link, persisted correctly across a tab-away-and-back cycle, and correctly reverted to the Run Scan CTA on `setScannerActivity('ready')`; Applications correctly showed real compatibility badges for all 6 detected apps, "Not yet assessed" for 4 Suggested items (Microsoft 365, Notion, Git, Docker Desktop), and exactly 3 correct "Please verify" notes (Photoshop/Creative, Steam/Gaming, Chrome/Utilities — none of those three categories were in the selected Development+Office workload set) with correctly unchecked boxes; checking Steam's box wrote `critical_to_me: true` to the real session-scoped Personal Context (`sessionStorage['ngpcx_session_context_<id>']`) and correctly promoted it on both Applications (note cleared) and Detected Apps (moved to Likely Important) in the same interaction; the renamed More Info evidence rows (Scanner/Use Case/Community/Please Verify) rendered correctly; mobile (375px) showed zero horizontal overflow on both the Use Case tab and Applications; the report round-trip (`?origin=assessment`, Detailed Report link → `report.html` → back) loaded cleanly at every step. Zero console errors throughout the entire pass — no defects were found requiring a fix.

**Explicitly not attempted, per the brief's own exclusion list:** no AI suggestions, no fuzzy inference beyond the pre-existing hint mechanism, no complicated tagging, no automatic workload detection beyond the pre-existing `inferWorkloadHints()`/`inferRelationshipHint()`, and no deeper scoring changes — `computeSynthesis()`, `recommendationFor()`, `contextualReadFor()`'s core shape, and the Recommendation Eligible gate are all byte-for-byte unchanged from before this pass.

### Dashboard Instrument Cluster prototype (same branch, architectural UX evolution)
A full reorganization of the dashboard's layout and instrument behaviors, explicitly framed as validating a new interaction model around the EXISTING recommendation/confidence math, not redesigning it — `computeSynthesis()`, `recommendationFor()`, `contextualReadFor()`, `evidenceSourcesFor()`, and `dashboardConfidenceLabel()` are all byte-for-byte unchanged (confirmed: `public/assessment.js` has zero diff for this pass). Every change below is presentation/interaction logic in `public/index.html`.

**Layout: three columns, reordered.** `.dashboard-panels` is now What Next (left) → Instrument Cluster (center) → Suitability (right), matching the desired reading flow "what should I do → what effect did that have → what recommendation have we earned." This is a real DOM reorder, not a CSS `order` trick, so visual order, tab order, and reading order all agree. The center Instrument Cluster panel (`.panel-instrument-cluster`, new) holds the Confidence gauge and the Evidence meters side by side in one bordered box with a slightly brighter border — the dashboard's visual focus, per the brief.

**Suitability replaced entirely — round dial → vertical LED instrument.** The old `gaugeSvg()`-based recommendation dial is gone; `.panel-suitability` now houses a 12-segment vertical LED column (`#suitability-led-column`, 3 segments × 4 tiers, `flex-direction: column-reverse` so segments illuminate bottom-to-top) beside a static tier-label gutter (Poor Fit / Proceed with Caution / Probably a Good Fit / Good Fit). **Deliberately kept the real 4 labels `recommendationFor()` already returns** rather than inventing the brief's own example names ("Workable"/"Excellent Fit") — a second, renamed vocabulary for the same underlying value would drift from Results/Workspace's own wording over time; this was a judgment call, flagged rather than silently substituted.

**Three visual states per segment**, implementing the brief's unlock behavior exactly:
- **Locked** (default) — a barely-visible dashed outline. The instrument's resting "asleep" state when zero evidence exists (verified: a fresh page load renders all 12 segments in this state, no `unlocked`/`lit` class anywhere).
- **Unlocked** (dim) — `suitabilityUnlockedCount(confPct)`, driven by the exact same `confPct` the Confidence gauge itself displays (computed once, shared, so the two instruments can never silently disagree about how much evidence exists). Zero evidence → zero unlocked; the first sliver of real evidence always reveals at least one dim segment (`Math.max(1, ...)`), matching "reveal the lowest dim segments."
- **Lit** (a real tier color) — `suitabilityIlluminatedCount(eligible, tierIndex, unlockedCount)`, zero unless the pre-existing Recommendation Eligible gate (`isSuitabilityEligible()`, untouched) has opened, and **always capped at `unlockedCount`** even when the earned tier would imply more — the literal implementation of "we only display recommendation ranges we have earned enough evidence to evaluate." Verified directly: a contextual-read session with `confPct: 30` (unlocking 4 of 12 segments) and a "Probably a Good Fit"-tier verdict (which would want 9 segments fully illuminated) correctly capped at 4 lit, 0 dim — exactly the "confidence unlocks six, recommendation illuminates four" example from the brief, scaled to this session's real numbers.

**Motion**: newly-unlocked segments play a one-shot "power-on" brighten-then-settle keyframe (`.power-on`); newly-illuminated segments play a sharper "flash-on" brighten (`.flash-on`, shared with the evidence LEDs below); both skip via `@media (prefers-reduced-motion: reduce)`. Both are gated by comparing this render's unlocked/illuminated counts against the previous render's (`lastSuitabilityUnlocked`/`lastSuitabilityIlluminated`) — only the newly-crossed segments animate, not the whole column on every render. The whole `.panel-suitability` box also gets a brief pulse (reusing the existing `gauge-pulse` keyframe) whenever the shown recommendation LABEL changes, mirroring Confidence's existing pulse-on-change precedent.

**Confidence gauge: smooth needle rotation, a real (not cosmetic) refactor.** The prior implementation rebuilt the entire gauge SVG via `innerHTML` on every render, which meant the needle element was destroyed and recreated each time — a CSS `transition` on a freshly-inserted element has no prior state to animate from, so the needle used to jump instantly despite `transition` being present in spirit. Fixed by building the static SVG (track, ticks, a needle `<g>` with `transform-origin`/`transition` already set) exactly once per page load (`gaugeStaticSvg()`/`ensureGaugeBuilt()`), and having every subsequent render only update the fill arc (`updateGaugeFill()` — redrawn instantly, a variable-length-polyline transition was judged out of scope for this pass) and the needle group's `transform: rotate()` (`updateGaugeNeedle()` — this DOES animate, since the exact element already existed a moment ago at a different angle). Verified directly: the needle element carries `transform-origin: 39px 39px; transition: transform 0.7s cubic-bezier(...)` and its `rotate()` value updates correctly across renders.

**Evidence meters: continuous bar → segmented LEDs, color now means completeness only.** `.evidence-meter-track`/`-fill`/`-marker` are gone, replaced by `evidenceLedSegments()` (8 fixed segments per row, always rendered, unlit ones a dim outline). **Real behavior change, called out explicitly rather than silently shipped**: color used to be fixed per source (Scanner/Use Case always blue, Community always gray, Please Verify amber-when-needing-attention/green-otherwise); now `ledColorForPct()` derives ONE color per row from that row's own completeness percentage (red <25%, orange <50%, yellow <75%, green ≥75%) — implementing the brief's explicit "the colors indicate completeness, they do not indicate whether the result is good or bad" as a structural rule, not a per-source convention. Please Verify's old amber "needs attention" signal isn't lost — its status TEXT still reads "Some require attention" — it's just no longer double-encoded as color, since color now carries one consistent meaning across all four rows. Newly-lit segments flash via the same `.flash-on` keyframe as Suitability, tracked per row by a stable key (`lastEvidenceLit`) rather than a fresh comparison against zero on every render.

**Verified live, fresh browser session, desktop (1280px) and mobile (375px), against both a no-evidence load and two real sessions** (a contextual Use-Case-only session and a synthetic scan-backed session, both created via direct API calls): panel order and DOM order confirmed What Next → Instrument Cluster → Suitability; empty-state Suitability confirmed fully "asleep" (12/12 segments in the plain locked state); adding Use Case answers correctly unlocked 4 segments and illuminated exactly 4 (capped, not the tier's full 9) with the `flash-on` class present on first light; a scan-backed session at 100% confidence correctly showed all 12 segments lit and the Confidence needle at `rotate(240deg)` (full sweep); evidence LED rows showed correct fill counts and green coloring at high completeness, correctly empty (0 lit, no color) for Community with no data; mobile (375px) showed zero horizontal overflow, panels stacking to a single column, and the Instrument Cluster's internal Confidence-gauge-plus-Evidence-meters row staying side-by-side without breaking (no extra media query needed — it fit); the report round-trip (`report.html`) loaded with zero console errors, confirming `assessment.js` and `report.html` are unaffected. Zero console errors across the entire pass.

**Known, accepted compromise, not a bug:** on a first page load that already has evidence (e.g. arriving at an existing `?session=` URL), the Suitability segments that are already unlocked/illuminated play their power-on/flash animation on that very first paint, unlike the Confidence gauge's pulse (which explicitly skips its first render via a `null`-initialized tracking variable). Left as-is — reads reasonably as an instrument "self-testing" on power-up, which is also exactly the direction the brief's own deferred Startup Sweep concept points — but flagged here rather than silently decided.

**Explicitly deferred, per the brief's own instruction:** the automotive-style startup self-test sweep itself — not implemented this pass, but the segment-count-driven render functions (`renderSuitabilityLeds(unlockedCount, illuminatedCount)`, `evidenceLedSegments(key, pct)`) are already pure functions of two numbers each, which is the natural hook a future sweep would drive (temporarily call them with `unlockedCount = TOTAL`/`illuminatedCount = TOTAL` on load, then settle to the real values) without needing any further restructuring.

**Not attempted, per scope:** no changes to `computeSynthesis()`, `recommendationFor()`, `contextualReadFor()`, `evidenceSourcesFor()`, `isSuitabilityEligible()`, or any other scoring/confidence math (confirmed via `git diff --stat public/assessment.js` showing zero changes). No smooth animation on the confidence arc's fill growth (only the needle rotates smoothly — animating a variable-length SVG polyline smoothly is a materially harder problem than rotating a fixed needle and was judged out of scope for this pass, noted as an opportunity for the next iteration).

### Illuminated-LED visual refinement (same branch, following the Dashboard Instrument Cluster prototype)
A visual-only pass on top of the prototype above, driven by a user-supplied mockup image and explicitly scoped to preserve layout, interaction model, and logic exactly as they were. `public/assessment.js` has zero diff for this pass (confirmed via `git diff --stat`) — every change is CSS/markup/presentation-JS in `public/index.html`.

**LED color model inverted: fixed color, variable brightness — the core visual change.** Both the evidence meters and the Suitability instrument previously chose a single color for a whole row/segment based on some computed value (row completeness for evidence meters; the segment's tier for Suitability) and represented "off" as gray. Per the mockup, this flipped: **every LED segment always carries its assigned color** — the color itself never changes — and "on" vs. "off" is expressed purely as brightness (CSS `opacity`), never as gray.
- **Evidence meters**: each of the 8 segments now has a FIXED color by POSITION, identical on every row (`:nth-child` rules on `.evidence-led`: 1–2 red `#ef4444`, 3–4 orange `#f97316`, 5–6 yellow `#eab308`, 7–8 green `#10b981`) — completeness is now expressed by how many of those fixed-position LEDs are lit, not by which color was picked. `ledColorForPct()` (the old row-color-by-percent function) was deleted outright, not deprecated. Dim (unlit) segments sit at `opacity: 0.32` — chosen by iterating in the browser until the colors read as "unmistakably red/orange/yellow/green, clearly powered down" rather than gray or invisible, not a value handed down in the brief.
- **Suitability**: each tier group sets one CSS custom property (`--tier-color`, via `group.style.setProperty()` in `renderSuitabilityLeds()`) and every segment inside just reads `background: var(--tier-color)` — locked/unlocked/lit are now purely opacity states (`0.26` / `0.52` / `1`) of that same color, replacing the old locked-transparent-dashed-outline and unlocked-generic-gray treatment. The `power-on` keyframe was rewritten to animate `opacity` (0.9 → 0.52) instead of a hardcoded `rgba(148,163,184,...)` gray, since the segment's background is now always its tier color. **First attempt landed too dim** (`0.16`/`0.45`) — confirmed via a real screenshot that locked segments read as nearly invisible against the dark panel — corrected upward to `0.26`/`0.52` and reverified visually before settling. This dim/unlocked/lit opacity split is a judgment call extending the mockup's language (explicitly shown for Suitability in the reference image, though the user's own text called out evidence meters as "the biggest change") to the second instrument for visual consistency between the two LED families — flagged here in case it wasn't intended to extend that far.
- `.evidence-led`/`.suitability-segment` no longer carry inline `background`/`box-shadow` styles written per-render from JS — colors are now assigned once via CSS (`:nth-child` for evidence, one `--tier-color` custom property per Suitability tier group), which also simplified `evidenceLedSegments()`/`renderSuitabilityLeds()` (less per-segment JS work, same lit/unlit/flash logic, unchanged).

**Proportional adjustments, all pure CSS/dimension changes, no logic touched:**
- `.panel-whatnext` narrowed (`flex: 1 1 0` → `0.82 1 0`); the recovered share went to `.panel-instrument-cluster` (`flex: 1.7 1 0` → `1.9 1 0`).
- Confidence gauge enlarged: `GAUGE_R`/`GAUGE_STROKE`/`GAUGE_PAD` (34/8/5 → 40/9/6), yielding a 92×72 SVG (was 78×61). `.gauge-svg-wrap`/`.gauge-block` CSS dimensions were updated by hand to match — there's no shared source between the JS constants and the CSS, so these have to be kept in sync manually; noted directly in both places as a comment for the next person who changes one without the other.
- The numeric score beneath the Suitability recommendation (`#gauge-rec-value`, e.g. "87") was removed — both the DOM element and the `recValue` computation in `renderDashboard()`. `synthesis.score` itself is completely untouched and still drives the illuminated-segment count and everything else downstream (Results/Workspace) — this only stopped ALSO printing it as a second text line here.
- Dashboard height trimmed where practical: `.dashboard`/`.panel` padding tightened slightly (0.9rem/0.55rem → 0.8rem/0.5rem-ish), and the Suitability LED column/tier-label height reduced 176px → 158px (segment COUNT is unchanged — still 12 segments / 3 per tier, a pure box-height reduction, not a logic change). Net effect: removing the score line plus the shorter LED column meaningfully shrank the tallest panel (Suitability, which drives the row's `align-items: stretch` height), while the Confidence gauge grew — a real net height reduction despite one component getting bigger.

**Verified live, fresh browser session** (cleared `sessionStorage`/the pending-adaptive-context `localStorage` key first, to see the genuine zero-evidence state rather than carried-over test data): the true empty state showed all evidence LEDs and all 12 Suitability segments in their dim-but-clearly-colored resting state (a screenshot before the opacity correction showed the Suitability locked state reading as nearly invisible — the specific defect the `0.16`→`0.26` correction above fixed, confirmed fixed via a second screenshot); adding Use Case answers (Office + Primary computer) correctly lit the Use Case evidence row's fixed-position LEDs and moved Confidence to 30%, correctly unlocking and partially illuminating the Suitability instrument (capped at the unlocked range, matching the prior pass's already-verified cap behavior — unaffected by this pass); the numeric Suitability score is confirmed absent in all states; the Confidence gauge's enlarged SVG (92×72) and the narrower What Next / wider Instrument Cluster columns were confirmed via direct DOM measurement; mobile (375px) showed zero horizontal overflow; zero console errors throughout. `assessment.js` diff confirmed empty.

**Not attempted, per explicit scope:** no scoring, confidence, or recommendation logic changes of any kind; no changes to the unlock/illuminate segment-count math (`suitabilityUnlockedCount()`/`suitabilityIlluminatedCount()`); no changes to which states exist or when they transition (locked/unlocked/lit still driven by the exact same `unlockedCount`/`illuminatedCount` values as the prior pass) — only how those states are painted.

## Distribution / signing decisions (not yet executed, don't re-litigate without asking)
- Signing: Azure Trusted Signing (~$10/mo), not a traditional OV cert ($200-400/yr) — same trust outcome, cheaper. Requires a personal Azure identity separate from ADS (Eric's employer) — pending verification.
- Distribution: direct .exe download stays the primary path/CTA. Microsoft Store (MSIX) is secondary/optional, not the front door — target audience finds the site via web/forums before searching the Store.
- Scanner's server URL is hardcoded (`get_server_url()` in `main.rs`) — currently `https://ngpcx.com`. Any future domain/branding change must keep this endpoint stable regardless of what the public-facing site is called, since already-distributed exes can't be updated.
- **Scanner considered feature-complete pending code signing.** Two architecture reviews (general discovery/noise/metadata, then a device-focused follow-up) plus six implementation passes (Scanner Stabilization, Observation Enrichment, Scanner Enrichment, UserAssist Enrichment, Environment Integration Evidence, Observed Environment — see those sections above) concluded the scanner's discovery categories, noise reduction, and metadata/evidence quality are in a good, deliberately-stopped-here state — not a pause for lack of more ideas, an actual assessment that further discovery-breadth expansion would be scanning more because it's possible, not because it changes a purchase decision. This freeze has been reaffirmed multiple times already (Scanner Enrichment, UserAssist Enrichment, Observed Environment) and is reaffirmed again here: each follow-up was a single, scoped addition on a specific candidate a prior milestone's own research had flagged, not a reopening of general scanner scope. The remaining research-only findings across the enrichment milestones are the current record of what was deliberately left out and why (file-extension default handlers being the latest addition to that list). **No further scanner enhancements are planned before signing.** Next engineering effort shifts to evaluating the product (Workspace/assessment behavior) built on top of what the scanner now reports — the scanner itself isn't expected to gain new discovery scope for that.

## Known open items (check before assuming something isn't started)
- Prefetch app-matching had a `.exe`-suffix bug (fixed). `recently_used`/`days_ago` enrichment is still Prefetch-first, but `get_running_process_names()` (`main.rs`) provides a Prefetch-independent fallback signal (currently-running processes) — directly covers the SSD/Prefetch-disabled case, which is common enough that it's part of why app inventory is no longer mode-filtered at all (see App enumeration section) rather than trusting Prefetch-derived recency as an inclusion gate.
- `public/about.html` still has no personal "who's behind this" author bio (a maker/team bio, distinct from the product origin story the page now opens with) — deliberately omitted pending the user's own wording, don't write it without asking. Same for the donation copy's longer/more-personal version (also pending, current copy is the short placeholder-free "no ads" version).
- **Deferred (Phase 12, do not build without being asked): an "Understanding Your Results" educational page** — the companion piece to an actual scan report, explaining Native/Emulated/Unknown/Needs Attention, confidence scoring, how community findings factor in, why compatibility evolves over time, and recommended next steps for the visitor. `why.html` (built in Phase 12) is the general "why does this whole thing work this way" explainer; this future page would be the report-specific "here's what your results actually mean" companion — related but distinct, and not yet started.
- **OAuth verified end-to-end locally against real Google and GitHub providers**, then enabled and tested in Railway production too — both Local and Production OAuth app variants are registered and in use. Real login, real user row creation, session persistence (including across a full server restart), logout (session destroy + Passport's anti-fixation `regenerate()`), anon→auth migration (clean reassignment and collision/superseded both confirmed with real accounts), and live admin role grant/revoke (403 immediately on disable, 200 immediately on re-enable, no logout) all confirmed against the actual route code locally — not just fake-strategy tests. `ADMIN_SECRET` has not been retired (`ADMIN_SECRET_ENABLED` migration path, above) — both admin paths still coexist in production; that's a separate, deliberate decision, not yet made.
- **Testing gotcha, don't be fooled by it again:** `admin-research.html`/`admin-revisit.html` send `x-admin-secret` from `localStorage['ngpcx_admin_secret']` on every admin request if it's set — and it usually is, from ordinary prior use of those pages. `requireAdminAuthOrOAuth`'s shared-secret branch is checked *first*, so a stored secret silently short-circuits past the OAuth role/disabled check entirely. To actually exercise the OAuth-only path (e.g. to verify a disabled/demoted account loses access), clear that localStorage key first — otherwise a revoked OAuth admin will still appear to have access, and it's not a bug.
- Sign-in UI only ever linked to Google (`report.html`/`index.html`) — GitHub was fully functional server-side but had no way to reach it from the UI. Fixed: both providers now render (gated on `/api/auth-status`'s `providers` field, so an unconfigured provider never shows a dead link).
- The `reviewer` rank was renamed to `researcher` and is now live (Researcher volunteer workflow, admin-reviewed) — see User roles & Researcher workflow section. Direct catalog resolution stays intentionally minimal: Researchers reuse the existing `admin-research.html`/`admin-revisit.html` pages for that specific action rather than a dedicated new workspace (see that section, "Deferred") — this is distinct from Community Review (see that section), which *is* a dedicated new workspace, but for reviewing community evidence and recommendations, not for direct resolution.
- **Bug fixed: `req.user.id` crashed with a 500 when authenticated via the shared `ADMIN_SECRET`** (`POST /api/admin/users/:id/role`, `POST /api/admin/researcher-requests/:id/approve`/`decline`, all in `admin.js`). `requireAdminAuthOrOAuth`'s shared-secret branch calls `next()` directly and never populates `req.user` — it's only ever set by Passport's OAuth session. These three routes assumed `req.user` always existed (true for every route tested against the Phase 11 isolated harness, which always injected a fake OAuth-style `req.user` — the harness never exercised the pure-secret path against these specific new endpoints, which is exactly how this shipped unnoticed). Fixed by guarding every `req.user` read: the role route's self-change check is simply skipped under the shared secret (no "self" concept exists for a secret shared by multiple people), and `reviewed_by` is written as `NULL` instead of crashing. Found via real usage: logging into `admin-users.html` with the shared secret and trying to promote an account to admin returned "Request failed (500)".

## Style
- Dictation-first author, conversational but precise prose elsewhere in the project (docs, LinkedIn posts) — not relevant to code style, but worth knowing if asked to write user-facing copy.
- Security-practitioner audience for some users (Rochester Security Summit, Black Hat Europe submissions reference this project) — code and copy should hold up to scrutiny from technically sophisticated readers, not just casual users.



Bulk catalog state is reproducibly derived from committed source caches. Persistent SQLite stores that catalog plus accumulated operational knowledge from admin, community, and on-demand sources.




## Future direction: ARM Optimization Workspace (parked — hard blocker on hardware access, do not design/implement without being asked)

**The idea:** when the scanner detects it's running ON Windows on ARM hardware (not just checking readiness to migrate TO one), the workspace reframes its question from "should I buy an ARM PC?" to "how well-optimized is my ARM PC?" Same infrastructure (workspace, community, confidence model) — the recommendation logic and framing shift, not the architecture. Part of a broader lifecycle: Research → Purchase → Optimize → Contribute → Help the next buyer.

**The one genuinely new capability this requires:** per-app executable architecture detection (real PE-header inspection to determine if a specific installed binary is actually ARM64-native, x64, or x86) — the scanner does not have this today. Confirmed via the compatibility-model codebase audit: there's no PE-header parsing anywhere in `main.rs`, and `apps.architectures` is dead/hardcoded data, not real per-app detection. Don't assume this is "just exposing data already collected" — it isn't, it's new engineering (likely the `goblin` or `pelite` crate, a new `Cargo.toml` dependency).

**Correctly scoped (after a few rounds of narrowing — this is the resolved scope, not the first draft):**
- **ARM-only.** Per-app architecture inspection only makes sense to run when the scanner has confirmed it's on real ARM64 hardware — nothing on an x86/x64 machine can ever be ARM64-native, so there's nothing to observe there. This isn't a cut corner, it's the feature's own logic drawing its natural boundary.
- **Target set: everything NOT already confirmed Native by the database** — not "unresolved apps only" (an earlier, too-narrow draft). This single filter covers both real use cases at once: (a) apps marked Emulated → surfaces genuine upgrade opportunities to the user ("you're running the emulated build, a native one exists"), and (b) apps marked Unclassified → improves the database (reality may have moved past the last scrape). No separate list-loading step needed — this is just `classifyApps()`'s existing output, partitioned into "confirmed Native" vs. everything else.
- **Observed evidence corroborates, never determines compatibility.** An app observed running natively on ARM64 proves it *launches*, not that it's *fully functional* — the Logi Tune case (runs via Prism, but RightSight/Vibe 100 features are unsupported) is the concrete example. Scanner-observed evidence should raise confidence in an existing determination, not outrank a qualified Researcher or community finding, and should never be treated as equivalent to "fully verified."

**Hard blockers, not design questions — don't attempt to solve these on paper:**
1. No ARM64 hardware available to build or test against. Any PE-parsing logic designed without real hardware to verify it risks repeating the `is_arm`/`PROCESSOR_ARCHITEW6432` mistake — a fix that shipped without ever being confirmed against the hardware it targets, because none was available at the time.
2. Depends on `is_arm` detection (the `PROCESSOR_ARCHITEW6432` fix) being correct — it was applied but has never been verified against real ARM64 hardware either. Verify that first, independently, before this feature's trigger condition can be trusted.

**When to revisit:** once real ARM64/Snapdragon hardware is available for testing — not before. Until then, this stays a documented idea, not a prompt.

**Expected implementation impact:** likely scanner-only plus workspace presentation; no expected changes to the trust model or community workflow.